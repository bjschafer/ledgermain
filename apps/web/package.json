{
  "name": "@pf1/web",
  "version": "0.0.0",
  "private": true,
  "description": "PF1e character builder + live sheet (React + Vite). Stage 3.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "predev": "bun scripts/copy-refdata.ts",
    "dev": "vite",
    "prebuild": "bun scripts/copy-refdata.ts",
    "build": "vite build",
    "preview": "vite preview",
    "sample:build": "bun scripts/build-sample-character.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test ./test",
    "e2e": "playwright test",
    "screenshots": "playwright test --config screenshots/playwright.config.ts",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@pf1/engine": "workspace:*",
    "@pf1/schema": "workspace:*",
    "dexie": "^4.0.10",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@pf1/data-pipeline": "workspace:*",
    "@playwright/test": "^1.61.1",
    "@types/bun": "^1.1.14",
    "@types/node": "^22",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^5.2.0",
    "vite": "^6.4.3",
    "wrangler": "^4.105.0"
  }
}
