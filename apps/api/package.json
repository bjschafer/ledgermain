{
  "name": "@pf1/api",
  "version": "0.0.0",
  "private": true,
  "description": "Cloudflare Worker: dumb persistence for CharacterDoc blobs (Stage 5 cross-device sync, DESIGN.md §2.1). Never computes or interprets game data.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.9",
    "wrangler": "^4.107.0"
  }
}
