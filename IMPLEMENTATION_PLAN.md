# Implementation Plan: Sorcerer bloodlines, wizard schools, arcane bonds

Three PF1e spellcaster class features are currently unimplemented (audit, 2026-07-01):

- **Sorcerer bloodlines** — no schema field, no engine logic, no UI. The _only_ surviving surface is bonus-feat **slot counting** (`apps/web/src/model/feats.ts` reads the `bonusFeats` change from the prose-only "Bloodline Feat (SOR)" class feature). The spell associations already exist in vendored data (`spells.json` carries `learnedAt.bloodline` for 39 bloodlines across 220 spells in the current slice) but the pipeline does **not** invert them.
- **Wizard specialization schools** — no schema field, no slot mechanic (specialist +1 school slot/level, opposition spells cost 2 slots), no UI. "Arcane School" is a prose-only `ClassFeature` (`changes: []`) for the _slot_ mechanic. [Corrected 2026-07-03: per-school **power** data (Hand of the Apprentice, Intense Spells, etc.) IS vendored, under `class-abilities/wizard-schools/*.yaml` — this claim was wrong. Implemented; see Stage 4 correction below.]
- **Arcane bonds** — no schema field, no familiar skill bonuses, no concentration-without-bonded-object penalty, no UI. "Arcane Bond" is prose-only. No familiar stat blocks are vendored anywhere.

The shared root cause: Foundry models each as a single prose-only `ClassFeature` with no structured sub-entries for the actual choices (which bloodline, which school, familiar vs. bonded object). The placeholder `build.classFeatureChoices: unknown[]` (`packages/schema/src/character.ts:65`) exists but is untyped, always `[]`, and read by no code. We will **not** use it — each feature gets a _named_ field, mirroring the working cleric-domain precedent (`build.clericDomains` + `refData.domainSpellLists` + `PreparedSpell.kind === "domain"`).

### Ordering / tractability

The three features differ sharply in how much vendored data we can lean on, which sets the staging:

| Feature                              | Vendored data we can lean on                                                                                                                                                                                                              | Hand-authoring burden                              |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------- |
| Sorcerer bloodline **spells**        | ✅ `spells.json.learnedAt.bloodline` (39 bloodlines, 220 spells in the current slice — verified 2026-07-02; grows slightly once the keep-filter retains bloodline-only spells) — just needs a pipeline inversion mirroring the domain one | none                                               |
| Sorcerer bloodline **arcana/powers** | ❌ not vendored (prose-only feature)                                                                                                                                                                                                      | per-bloodline Change sets, clean-room              |
| Wizard school **slots/opposition**   | ❌ no `arcane-schools` pack read; only spell `school` tag (abj/evo/...)                                                                                                                                                                   | 8 school tags (trivial); school _powers_ are prose |
| Arcane bonds                         | ❌ no familiar stat blocks anywhere                                                                                                                                                                                                       | full familiar table, clean-room                    |

So: bloodline **spells** first (pure pipeline + schema + UI, mirrors domains exactly), then wizard **school slots** (schema + engine slot mechanic + UI; no new data needed), then **arcane bonds** (largest hand-authoring). Bloodline **arcana/powers** and school **powers** (the passive-ability effects) are deferred to a final stage — they're real but not table-blocking, and each school/bloodline has prose-only data we'd have to hand-transcribe clean-room.

> **Correction (2026-07-03)**: "school _powers_ are prose" (row 19) and "not vendored" (line 26, for schools) were wrong — see the Stage 4 correction note. Wizard school powers turned out to be structured `class-abilities/wizard-schools/*.yaml` data, not prose to hand-transcribe; same for cleric domain powers (not tracked in this table at all, since domains were already a "working precedent," but the same gap existed and is now closed). Bloodline arcana/powers (row 18) is unaffected by this correction — still genuinely prose-only.

### Licensing note (DESIGN §6)

Bloodline arcana/powers and familiar stat blocks are **not** in the vendored Foundry content and must be **hand-authored clean-room from the published PF1 rules**, never copied/transcribed from Foundry's GPL system code (the precedents are `packages/engine/src/tables.ts` and `packages/engine/src/feat-effects.ts`). Wizard school powers and cleric domain powers, by contrast, ARE vendored compendium _data_ (OGL / Paizo Community Use, same posture as feats/spells/classes elsewhere in this pipeline) — normalized directly, no hand-authoring or GPL-code involvement.

---

## Stage 1: Sorcerer bloodline spells (data + schema + UI)

**Goal**: Sorcerers pick a bloodline in the builder; bloodline bonus spells auto-populate the known list as the sorcerer levels (one per odd sorcerer level starting at 3), do **not** count against the spells-known cap, and render with a "bloodline" badge. Mirrors the cleric domain system end-to-end and is the template Stages 2–3 reuse.

**Scope** (in / out):

- IN: Pipeline inversion `learnedAt.bloodline` → `refData.bloodlineSpellLists` (mirror `normalize.ts:142-163` domain block exactly), emitted as `bloodline-spell-lists.json`. Extend the spell-slice keep filter (`normalize.ts:110-125`) so bloodline-only spells are retained (mirror the existing `hasDomain` term).
- IN: Schema `RefData.bloodlineSpellLists: Record<string, SpellList>` (mirror `refdata.ts:37`).
- IN: Schema `build.sorcererBloodline?: string` (mirror `build.clericDomains?: string[]` at `character.ts:57`).
- IN: Engine/Model: `bloodlineSpellsKnown(refData, bloodlineTag, sorcererLevel) -> string[]` in `apps/web/src/model/spellcasting.ts`. PF1 rule: a bloodline's level-`L` spell (1–9) is unlocked at sorcerer level `2L+1`. Returns spell ids the sorcerer has unlocked, sorted.
- IN: Builder UI: a `BloodlinePicker.tsx` rendered in `ClassesSection.tsx` for sorcerers (mirror `DomainPicker.tsx`). Dropdown of `Object.keys(refData.bloodlineSpellLists)`; chosen tag persisted via a `setSorcererBloodline` transition in `doc.ts` (mirror `setClericDomains` at `doc.ts:118`).
- IN: Known-list rendering (`SpellsSection.tsx` / wherever the sorcerer's known spells are listed): merge `bloodlineSpellsKnown` into the displayed known list with a "bloodline" badge; exclude them from the spells-known _cap_ check (they're bonus known, not chosen known).
- OUT: Bloodline **arcana** and **powers** (passive abilities like Draconic's breath weapon, +1 HP/HD from Fey, etc.) — deferred to Stage 4.
- OUT: Bloodline bonus **feat** selection — the _slot count_ is already correct (audit A6); picking _which_ bloodline feat fills the slot is a feat-picker concern that defers with Stage 4 (the feat list per bloodline isn't vendored).
- OUT: Wildblooded /crossblooded variants — non-core, defer.

**Schema change** (`packages/schema/src/refdata.ts`):

```ts
// Mirror domainSpellLists at lines 31-37.
/**
 * Per-bloodline spell lists, keyed by bloodline tag (e.g. "Draconic",
 * "Abyssal") → spell level → spell ids. Inverted from `Spell.learnedAt.bloodline`.
 * A sorcerer's chosen bloodline grants one bonus spell known per odd sorcerer
 * level starting at 3 (level-`L` bloodline spell unlocked at sorcerer level
 * `2L+1`); these do not count against the spells-known cap. Empty for non-sorcerer
 * slices. 39 bloodlines in the current vendored slice.
 */
bloodlineSpellLists: Record<string, SpellList>;
```

`packages/schema/src/character.ts` (inside `build:`):

```ts
/**
 * Sorcerer bloodline tag (key into `refData.bloodlineSpellLists`), chosen at L1.
 * Free-choice since the vendored data carries no sorcerer-heritage mapping —
 * matches the project's hybrid soft-warning posture (see clericDomains above).
 * Empty/undefined for non-sorcerers. The chosen bloodline grants bonus spells
 * known at odd sorcerer levels ≥3 (see model/spellcasting.bloodlineSpellsKnown).
 * Back-compat: documents without this field are unaffected.
 */
sorcererBloodline?: string;
```

`schemaVersion` stays at its current value — fields are optional + additive; no migration. (Same posture as the GM Grants stage.)

**Pipeline change** (`packages/data-pipeline/src/normalize.ts`):

Mirror the domain block at lines 142-163 verbatim, a few lines below it:

```ts
// --- per-bloodline spell lists (invert learnedAt.bloodline) -----------------
// A sorcerer's chosen bloodline grants bonus spells known at odd sorcerer
// levels ≥3, drawable from this list. Bloodline-only spells (e.g. a spell
// tagged "Abyssal" but not on the sorcerer class list) would otherwise be
// dropped by the slice filter above — keep any spell with a non-empty
// bloodline entry, mirroring the domain retain-term.
const bloodlineTags = new Set<string>();
for (const spell of spells) {
  for (const tag of Object.keys(spell.learnedAt.bloodline ?? {})) bloodlineTags.add(tag);
}
const bloodlineSpellLists: Record<string, SpellList> = {};
for (const tag of bloodlineTags) {
  const list: SpellList = {};
  for (const spell of spells) {
    const lvl = spell.learnedAt.bloodline?.[tag];
    if (lvl === undefined) continue;
    (list[lvl] ??= []).push(spell.id);
  }
  for (const lvl of Object.keys(list)) list[Number(lvl)]!.sort();
  bloodlineSpellLists[tag] = list;
}
```

Also extend the spell-slice keep filter (lines 110-125) — add a `hasBloodline` term so bloodline-only spells survive the slice (mirror the existing `hasDomain` term). In practice most bloodline spells are also on the sorcerer class list, but a strict mirror of the domain pattern is correct + forward-safe.

Wire into `RefData` returned object (line 234-249) and the `counts` block (lines 207-220, add `bloodlineSpellLists`). Add the emit row in `packages/data-pipeline/src/emit.ts` `FILES` (line 29-43):
`{ key: "bloodlineSpellLists", file: "bloodline-spell-lists.json" }`.

**Both RefData loaders must learn the new file** (the domain precedent touched all of these — verified 2026-07-02):

- `packages/data-pipeline/src/index.ts:32` — add `bloodlineSpellLists: readJson(dir, "bloodline-spell-lists.json")` (Node loader, used by engine tests).
- `apps/web/src/refdata/loader.ts` — add the fetch row (mirror `domain-spell-lists.json` at lines 37/52/68).
- `packages/data-pipeline/src/config.ts:29` — bump `SCHEMA_VERSION` 4 → 5 and extend its comment ("v5 adds `bloodlineSpellLists`"), same convention as the v4 domain bump.

Regen: `bun run data:build`. Review the diff — expect ~39 bloodline entries.

**Engine changes** — none. `compute()` is unchanged; bloodline spells are a builder/tracker display concern (they expand the known list, not a derived stat). The engine's spells-known **table** (the cap) is untouched — bloodline spells are _bonus_ known and sit outside the cap, enforced at the UI layer. (Same posture as `gmGrants`, which lives entirely in the web model layer.)

**Model transitions** (`apps/web/src/model/doc.ts`):

```ts
setSorcererBloodline(doc, tag | null): CharacterDoc   // null = clear
```

Mirror `setClericDomains` (doc.ts:118) shape; single tag (not array). Clamp to one tag; ignore blank/whitespace (mirror `setClericDomains` blank-stripping at `preparedSpells.test.ts:278-285`). No validation that the tag exists in `refData.bloodlineSpellLists` (soft-warning posture, same as domains).

**Model logic** (`apps/web/src/model/spellcasting.ts`):

```ts
/**
 * Bloodline bonus spells known at `sorcererLevel` for the given `bloodlineTag`.
 * PF1 rule: a bloodline's level-`L` spell (1-indexed spell level) is unlocked
 * at sorcerer level `2L+1`. Returns the ids of unlocked bloodline spells (only
 * those whose spell level ≤ floor((sorcererLevel-1)/2)). Empty if the tag is
 * unknown to refData or the sorcererLevel is below 3. Sorted by name.
 *
 * These are *bonus* spells known — the builder adds them to the displayed
 * known list automatically and they do NOT count against the spells-known cap.
 *
 * @example
 *   bloodlineSpellsKnown(ref, "Draconic", 7)  // → spells of level 1..3
 *   bloodlineSpellsKnown(ref, "Draconic", 2)  // → []  (starts at L3)
 */
export function bloodlineSpellsKnown(
  refData: RefData,
  bloodlineTag: string | undefined,
  sorcererLevel: number,
): { id: string; name: string; level: number }[];
```

Implement: read `refData.bloodlineSpellLists[tag]` keys 1-9; include level `L` only when `2L+1 <= sorcererLevel` (i.e. `L <= (sorcererLevel - 1)/2`). Returns `[]` gracefully when `tag` is undefined/unknown.

**UI** (`apps/web/src/components/builder/`):

- `BloodlinePicker.tsx` — dropdown (`<select>`) of `Object.keys(refData.bloodlineSpellLists).sort()`. One tag can be chosen/none. Mirrors `DomainPicker.tsx`'s free-choice + soft-warning shape (no deity lock).
- `ClassesSection.tsx` — render `BloodlinePicker` when the character has sorcerer levels (gate checks `casterModelFor("sorcerer"` or class list), next to the existing `DomainPicker` cleric branch (around line 197-200).
- Spells-known display: merge `bloodlineSpellsKnown` into the known list for the cap check (subtract from / exempt them), and render them with a "bloodline" badge (mirror whatever "domain" badge markup exists). They're read-only — not user-removable.
- Tracker: the spontaneous cast surface builds its castable list from `build.spells.known` (`PreparedSpellsPanel.tsx:276-289` `knownByLevel`) — merge `bloodlineSpellsKnown` there too, or the sorcerer can't cast their bloodline spells at the table.

**Success Criteria**:

- `bun run typecheck` green.
- `bun run data:build` regenerates `bloodline-spell-lists.json` with ~40 bloodline entries; `git diff` on the data dir reviewed.
- `bun run test` green; existing fixtures unchanged; new tests pass.
- Builder: a sorcerer 7 with `sorcererBloodline = "Draconic"` shows three bonus bloodline spells (levels 1, 2, 3) in the known list with a "bloodline" badge, and the spells-known cap counts them as exempt — i.e. the player can still add the same number of chosen known spells as a non-bloodlined sorcerer 7.
- Persistence: reload the page; the bloodline tag and the auto-granted known spells are still there (exercises Dexie + export/import JSON, unchanged since the field is additive).
- Soft warning: choosing an unknown bloodline tag (typed into a malformed import JSON) shows a soft warning, never hard-blocks.

**Tests** (red-first per the workflow):

- `packages/data-pipeline/test/normalize.test.ts` (or wherever pipeline tests live): `bloodlineSpellLists["Draconic"]` is non-empty and contains the expected known DD-spell at level 1; bloodline-only spells survive the slice (regression for the keep-filter extension).
- `apps/web/test/spellcasting.test.ts`: `bloodlineSpellsKnown(ref, "Draconic", 1)` → `[]`; `(ref, "Draconic", 3)` → one 1st-level spell; `(ref, "Draconic", 7)` → levels 1..3; `(ref, "Unknown" , 20)` → `[]` (soft fail, no throw); `(ref, undefined, 20)` → `[]`.
- `apps/web/test/doc.bloodline.test.ts` (new): `setSorcererBloodline` set, clear, blank-stripping.
- Regression: a doc **without** `sorcererBloodline` produces an identical builder display + spells-known cap to before (guards back-compat).
- One engine-fixture regression asserting `compute()` output is byte-identical with/without `sorcererBloodline` (the engine ignores the field by design — this test pins that).

**Files touched** (estimate):

1. `packages/schema/src/refdata.ts` — `RefData.bloodlineSpellLists` + JSDoc.
2. `packages/schema/src/character.ts` — `build.sorcererBloodline` + JSDoc.
3. `packages/data-pipeline/src/normalize.ts` — keep-filter term + inversion block + `RefData` return + `counts`.
4. `packages/data-pipeline/src/emit.ts` — one `FILES` row.
5. `packages/data-pipeline/src/index.ts` — one `readJson` row (Node loader).
6. `packages/data-pipeline/src/config.ts` — `SCHEMA_VERSION` 4 → 5.
7. `apps/web/src/refdata/loader.ts` — one fetch row (browser loader).
8. `packages/data-pipeline/data/bloodline-spell-lists.json` — regenerated (committed), plus `meta.json` regen.
9. `apps/web/src/model/doc.ts` — `setSorcererBloodline`.
10. `apps/web/src/model/spellcasting.ts` — `bloodlineSpellsKnown`.
11. `apps/web/src/components/builder/BloodlinePicker.tsx` — new.
12. `apps/web/src/components/builder/ClassesSection.tsx` — wire picker.
13. `apps/web/src/components/builder/SpellsSection.tsx` — merge bloodline spells into known + badge + cap-exemption.
14. `apps/web/src/components/tracker/PreparedSpellsPanel.tsx` — merge bloodline spells into the spontaneous castable list.
15. Three test files as above.

**Status**: Complete (2026-07-02). As-built notes:

- `bloodlineSpellLists` landed as v5 (`SCHEMA_VERSION` 4→5); 39 bloodlines, 2442 spells (18 more than before — bloodline-only spells retained by the extended keep-filter).
- `BloodlinePicker.tsx` uses a `<select>` dropdown (single tag) rather than the chip-toggle style `DomainPicker.tsx` uses for its two-tag choice — simpler UI for a single free-choice value.
- Bloodline spells render as a read-only reference block in `SpellsSection.tsx` (mirrors the existing `DomainSpellsBlock` pattern) rather than being merged into the toggleable `known` list — since they're never added to `doc.build.spells.known`, they're naturally exempt from the spells-known cap by construction (no separate exemption bookkeeping needed). Each spell row carries a `.tag-bloodline` badge.
- The tracker wiring landed in `PreparedSpellsPanel.tsx`'s `SpontaneousView` (the sorcerer's actual render path) rather than `PreparedView` — the plan's line reference (276-289) pointed at `PreparedView`'s `knownByLevel`, which only renders for prepared casters (wizard/cleric); sorcerer uses the separate spontaneous-caster view, whose analogous `knownByLevel` block got the merge instead.
- Engine regression test lives at `packages/engine/test/sorcererBloodline.test.ts` (new file, since no prior engine test built its own minimal doc fixture for this kind of ignored-field pin).

---

## Stage 2: Wizard specialization schools (slots + opposition)

**Goal**: A wizard picks one specialization school (or "Universalist") and two opposition schools (none if Universalist). The specialist gets **one bonus prepared slot per spell level (1–9, not cantrips)** that must be filled from their school's spell list; opposition-school spells cost **two slots** to prepare. UI renders school slots next to the existing normal slots (mirroring the cleric domain slot rendering). School _powers_ — the passive arcane-school abilities (diviner's foresight, evoker's elemental damage, etc.) — were originally scoped as **deferred** to Stage 4, but **are now implemented** (2026-07-03) — see the Stage 4 correction note.

> **PF1 RAW check (corrected 2026-07-02)**: a Universalist gets **no** bonus slot and no opposition schools — their compensation is the Hand of the Apprentice / Metamagic Mastery school powers. Only specialists (the eight non-"uni" tags) get the +1 school slot per level. This simplifies the slot logic: `kind: "school"` slots exist only when `wizardSchool` is set and ≠ `"uni"`.

**Scope** (in / out):

- IN: Schema `build.wizardSchool?: string` (school tag: `"abj" | "con" | "div" | "enc" | "evo" | "ill" | "nec" | "trs" | "uni"`). Free-choice; no "oppose X requires school Y" validation (soft-warning posture).
- IN: Schema `build.wizardOppositionSchools?: string[]` (≤2 tags; empty/omitted for Universalist).
- IN: Schema: extend `PreparedSpell.kind` (`character.ts:222`) from `"normal" | "domain"` to `"normal" | "domain" | "school"`. `"school"` slots may only hold spells whose `spell.school` tag equals the wizard's specialization; one per accessible spell level (mirror domain slot exclusivity).
- IN: Model: opposition-preparation cost. A prepared spell with `spell.school ∈ oppositionSchools` consumes 2 normal slots of its level (PF1 RAW). Implemented in the prepare/spell-accounting model (`apps/web/src/model/` — wherever domain slot counting lives).
- IN: Builder UI: `SchoolPicker.tsx` (8 schools + Universalist); `OppositionPicker.tsx` (pick ≤2; disabled when Universalist). Wire in `ClassesSection.tsx` next to the sorcerer-bloodline branch from Stage 1.
- IN: Tracker UI: `PreparedSpellsPanel.tsx` renders one "school slot" per accessible spell level (when specialist), sourced from `refData.spellLists["wizard"]` filtered by `spell.school === wizardSchool'.
- OUT (at the time): School **powers** (arcane-school passive abilities), including the Universalist's Hand of the Apprentice / Metamagic Mastery. **Now implemented** (2026-07-03, see Stage 4 correction) — a Universalist still gets **no bonus slot** (see RAW check above), but does get its school powers, same as any specialist.
- OUT: Archetype interactions that alter/swap arcane school (`wizard:bonded-wizard`, `wizard:familiar-adept`, etc. — prose-only in `archetype-features.json`). Surface as a soft warning only.

**Schema change** (`packages/schema/src/character.ts`):

```ts
/**
 * Wizard specialization school tag. One of the eight PF1 schools
 * ("abj","con","div","enc","evo","ill","nec","trs") or "uni" (Universalist —
 * no opposition schools, no bonus slot). Free-choice; the vendored Foundry data
 * has no per-school mapping of school features. Default undefined = Universalist
 * (back-compat: existing wizard docs load as Universalist).
 *
 * A specialist (any non-"uni" tag) gains one bonus prepared slot per accessible
 * spell level 1–9 (rendered with `PreparedSpell.kind === "school"`), exclusive
 * to spells of that school, plus two opposition schools (see
 * `wizardOppositionSchools`). A Universalist gains NO bonus slot (PF1 RAW —
 * their compensation is school powers, deferred).
 */
wizardSchool?: "abj" | "con" | "div" | "enc" | "evo" | "ill" | "nec" | "trs" | "uni";
/**
 * Two opposition school tags for a specialist wizard; empty/omitted for
 * Universalist. Opposition-school spells cost two normal slots to prepare
 * (PF1 RAW). Free-choice (no school-vs-opposition validation — soft-warning
 * posture, matching the cleric domain free-choice policy).
 */
wizardOppositionSchools?: string[];
```

`PreparedSpell.kind` (`character.ts:222`):

```ts
kind?: "normal" | "domain" | "school";
```

`schemaVersion` unchanged (additive, optional; back-compat docs load as Universalist).

**Engine changes** — none. Slot accounting is a builder/tracker model-layer concern, not a derived stat (mirrors how domain slots work — the engine's `compute()` is unaware of domain slots; the `PreparedSpellsPanel` + the model layer enforce exclusivity). Oppose-cost (2 slots) is enforced in the prepare model, not the engine.

**Model transitions** (`apps/web/src/model/doc.ts`):

```ts
setWizardSchool(doc, tag | null): CharacterDoc
setWizardOppositionSchools(doc, tags: string[] | null): CharacterDoc   // caps at 2; ignores blanks
```

Mirror `setClericDomains` shape + blank-stripping. `setWizardSchool("uni")` clears `wizardOppositionSchools` (a Universalist has none); `setWizardSchool(other)` leaves opposition alone (player must set them).

**Model logic** (`apps/web/src/model/` — wherever prepared-spell accounting lives):

- `isSchoolSlotEligible(spell, doc)` — true when `spell.school === doc.build.wizardSchool`. No school slots exist for Universalist or when no school is chosen (both mean `kind: "school"` slots simply don't render).
- `oppositionCost(spell, doc)` — returns 1 (normal) or 2 (when `spell.school ∈ doc.build.wizardOppositionSchools`). Refine the existing slot-counting loop so opposition-prepared spells consume 2.
- School slot capacity: one `kind: "school"` slot per accessible spell level 1–9 (never level 0), specialists only — mirror the cleric domain-slot accounting in `PreparedSpellsPanel.tsx` (`DomainSlotsSection`), which keeps domain slots out of the normal-slot capacity via `kind`.

**UI** (`apps/web/src/components/builder/`):

- `SchoolPicker.tsx` — 9-option `<select>` (8 schools + Universalist). When non-Universalist, reveal `OppositionPicker` (multi-pick ≤2 from the remaining 7 schools).
- `ClassesSection.tsx` — render both for wizards, next to the sorcerer-bloodline branch.
- `PreparedSpellsPanel.tsx` — render one "school slot" row per accessible spell level 1–9 (specialists only), mirroring the existing `DomainSlotsSection`. The slot is exclusive to in-school spells (filtered picker). No school-slot rows for Universalist.
- Tracker spell-cost surface: when preparing an opposition-school spell, the UI shows "costs 2 slots" badge and the prepare action decrements 2 normal slots.

**Success Criteria**:

- `bun run typecheck && bun run test` green; existing fixtures unchanged; new tests pass.
- Builder: a wizard 5 Evocation specialist picks Evocation + opposition {enchantment, necromancy}; the Spells panel shows one school slot per accessible spell level 1–3 (wizard 5 casts up to 3rd) exclusive to evo spells; preparing Burning Hands (evo) into a school slot works; preparing Sleep (enc, opposition) into a normal slot consumes 2 normal slots.
- Universalist wizard 5: no school slots, no opposition picker, opposition-cost is 1 for everything — mechanically identical to a doc with no `wizardSchool` at all.
- Persistence: reload page; school + opposition choices persist (additive optional fields, no migration).
- Back-compat: an existing wizard doc with no `wizardSchool` loads as Universalist with identical derived stats to before the change.

**Tests**:

- `apps/web/test/doc.school.test.ts` (new): set/clear/blank-strip for `wizardSchool` + `wizardOppositionSchools`; setting "uni" clears opposition; cap at 2 opposition schools.
- Model accounting tests: `oppositionCost(sleep, evocation-with-enc-opposition-doc) === 2`; `oppositionCost(burningHands, same-doc) === 1`; `isSchoolSlotEligible(burningHands, evocation-doc) === true`, `(sleep, evocation-doc) === false`; `(anySpell, universalist-doc) === false` (Universalist has no school slots).
- `apps/web/test/preparedSpells.test.ts`: extend an existing slot-accounting test (or add one) — specialist preparing an opposition spell into a normal slot consumes 2; into a school slot is rejected (in-school only).
- Regression: an engine fixture with `wizardSchool` set produces byte-identical `compute()` output (the engine ignores the field by design — pins that).

**Files touched**:

1. `packages/schema/src/character.ts` — two build fields + `PreparedSpell.kind` extension + JSDoc.
2. `apps/web/src/model/doc.ts` — two transitions.
3. `apps/web/src/model/` (prepare accounting module) — `isSchoolSlotEligible`, `oppositionCost`, `schoolSlotCapacity`.
4. `apps/web/src/components/builder/SchoolPicker.tsx` — new.
5. `apps/web/src/components/builder/OppositionPicker.tsx` — new (or fold into SchoolPicker).
6. `apps/web/src/components/builder/ClassesSection.tsx` — wire pickers for wizards.
7. `apps/web/src/components/tracker/PreparedSpellsPanel.tsx` — school-slot rows + opposition-cost badge.
8. Three test files as above.

**Status**: Complete. As-built notes:

- `SCHOOL_LABELS`/`SCHOOL_TAGS` (school tag → display label) ended up centralized
  in `apps/web/src/model/spellcasting.ts` rather than `SchoolPicker.tsx` — the
  builder's existing `SpellsSection.tsx` already had a local (and drifted —
  "Universal" vs "Universalist") copy for its browse-by-school filter chips;
  consolidated to one source of truth (plus a `schoolLabel()` helper for the
  untyped `Spell.school` string) instead of shipping a second copy.
- Opposition cost is accounted by weighting each normal-slot `PreparedRow` with
  a `cost` (1 or 2) and summing per level, rather than a separate slot kind —
  matches the plan's "refine the existing slot-counting loop" note. The
  per-level header now reads `usedCapacity/total prepared` (cost-weighted)
  instead of a raw instance count.
- Switching a specialist to Universalist (or to a different school) does not
  purge now-orphaned `kind: "school"` prepared instances from `live.spells`;
  they simply stop rendering (no UI section to show them in). Harmless
  (excluded from normal-slot capacity either way) but noted as a minor gap —
  a future "rest"/re-prepare already clears the whole loadout if it matters.
- Manually verified end-to-end in a real browser (Playwright, dev server): a
  wizard 5 Evocation specialist opposing Enchantment/Necromancy shows School
  Slots L1–L3, prepares Burning Hands into a school slot (1/1), preparing
  Sleep into a normal slot shows a "COSTS 2 SLOTS" badge and the L1 header
  reads 2/3 prepared; switching to Universalist hides both the opposition
  picker and the School Slots section.

---

## Stage 3: Arcane bonds (familiar + bonded object)

**Goal**: A wizard (or sorcerer with the Tattooed Sorcerer archetype — defer that interaction) records an arcane bond: a _familiar_ or a _bonded object_. Familiars grant their published master bonus (e.g. bat → +3 Fly, toad → +3 hit points). Bonded objects record the choice; their main RAW pain ("cast any spellbook spell 1/day"; "concentration DC 20 + spell level without the object") is surfaced as a play-tab tooltip, **not** modeled numerically in v1. Familiar stat blocks are hand-authored clean-room (none are vendored).

**Scope** (in / out):

- IN: Schema `build.arcaneBond?: { type: "familiar" | "object"; familiarKind?: string; bondedItemRef?: string }`. `familiarKind` keys into the hand-authored familiar table. `bondedItemRef` is an `ItemInstance.id` (player's own gear) for the bonded object; optional in v1 (player may record "bonded object" without specifying which item).
- IN: Hand-authored familiar table — new engine data file `packages/engine/src/familiars.ts` (mirrors the hand-authored shape of `tables.ts` / `feat-effects.ts`). The PF1 Core familiar list with each familiar's **master bonus** (corrected 2026-07-02 — the earlier draft listed 3.5e values; PF1 CRB values are): bat +3 Fly, cat +3 Stealth, lizard +3 Climb, monkey +3 Acrobatics, rat +2 Fortitude saves, raven +3 Appraise, viper +3 Bluff, toad +3 hit points, weasel +2 Reflex saves; hawk (+3 sight-based Perception in bright light) and owl (+3 sight-based Perception in shadows) are _conditional_ — model those two as a display note, not an always-on `Change`. Bonuses are expressed as `Change`s with engine target names (`skill.fly`, `skill.ste`, `skill.clm`, `skill.acr`, `skill.apr`, `skill.blf`, `fort`, `ref`, `hp` — all already-consumed targets, verified in `targets.ts`/`feat-effects.ts`) and type `"untyped"` (PF1 gives these bonuses no type; matches the `feat-effects.ts` precedent, e.g. Alertness). **Clean-room from published PF1 rules** (DESIGN §6) — never transcribe Foundry source.
- IN: Engine: apply the chosen familiar's skill-bonus `Change`s through the existing change-application path (`collect.ts` / `compute.ts` — wherever buff/feat changes get applied). The familiar is sourced from `build.arcaneBond.familiarKind` + the familiar table by id.
- IN: Builder UI: `ArcaneBondPicker.tsx` — toggle bond type; if familiar, a `<select>` over the hand-authored familiar table; if object, a free-text/item-ref picker (v1: free-text name; wiring to an existing `ItemInstance` deferred).
- IN: tracker UI: the sheet's "race/class features" panel surfaces "Familiar: bat — +3 Fly" (or the `note` for hawk/owl); bonded object shows "Bonded Object: <name or record>" plus an inline tooltip explaining the 1/day spontaneous-cast-from-spellbook and the no-object concentration penalty.
- OUT: Improved Familiar feat integration (the feat grants access to higher-tier familiars at level 5/7/etc.) — defer; in v1 the picker shows the base list regardless. Soft-warning only.
- OUT: The master's Alertness while the familiar is within arm's reach (situational, on/off at the table) — surface as a display note on the familiar row, not an applied `Change`.
- OUT: Familiar **HP/master-share mechanics** (familiar uses master's HP, half the master's level as HD, share saves, etc.) — these areNPC stat-block concerns the character sheet doesn't render. Defer indefinitely.
- OUT: Bonded object "cast any spellbook spell 1/day" as a play action — defer (live action, not a derived stat). Tooltip-only in v1.
- OUT: Concentration-without-bonded-object penalty as a live-computed DC — defer; the tooltip surfaces the formula. (Add to engine `concentration` target work already parked under audit C2.)

**Schema change** (`packages/schema/src/character.ts`):

```ts
/**
 * Arcane bond chosen at L1 by wizards (and by some sorcerer archetypes — defer
 * the archetype gate in v1). A familiar grants a published skill bonus (see
 * the hand-authored table in engine/familiars.ts); a bonded object records the
 * player's choice and surfaces RAW mechanics as a tooltip (the 1/day
 * spellbook-spell cast and the DC 20 + spell level concentration check when
 * casting without it are NOT modeled numerically in v1).
 *
 * `familiarKind` keys into the engine's fam-id table; `bondedItemRef` is a
 * reference into `build.gear` (an `ItemInstance.id`) — optional in v1 (a
 * player may record "bonded object" without pinning the item). Back-compat:
 * documents without `arcaneBond` are unaffected.
 */
arcaneBond?: {
  type: "familiar" | "object";
  /** Present iff `type === "familiar"`. Keys into engine familiar table. */
  familiarKind?: string;
  /** Present iff `type === "object"`; an `ItemInstance.id` from `build.gear`. */
  bondedItemRef?: string;
};
```

`schemaVersion` unchanged (additive optional).

**Engine changes** — apply the familiar's skill-bonus changes:

- New file `packages/engine/src/familiars.ts`: exports `FAMILIARS: Record<string, { name: string; changes: Change[]; note?: string }>`. Each familiar's `changes` is a small, hand-authored set (e.g. bat → `[{ target: "skill.fly", formula: "3", type: "untyped" }]`; toad → `[{ target: "hp", formula: "3", type: "untyped" }]`). `note` carries conditional/prose bonuses (hawk, owl) for display. Use the existing `Change` shape so the bonuses route through `stacking.ts` (untyped bonuses always sum — matches PF1 RAW for these).
- `collect.ts`: add a familiar block (mirror the feats block at `collect.ts:150-176`) — when `doc.build.arcaneBond?.type === "familiar"`, look up `FAMILIARS[doc.build.arcaneBond.familiarKind]` and `evalChange` its `changes`. Familiars with no entry in the table → soft warning, no crash.

**Model transitions** (`apps/web/src/model/doc.ts`):

```ts
setArcaneBond(doc, bond: { type: "familiar" | "object"; familiarKind?: string; bondedItemRef?: string } | null): CharacterDoc
```

`null` clears the bond. Setting `type: "familiar"` requires `familiarKind` (transition asserts non-blank; soft warning only if the kind isn't in the table). Setting `type: "object"` with `bondedItemRef` validates that the ref exists in `doc.build.gear` (if not — soft warning, not a hard block).

**UI** (`apps/web/src/components/`):

- `builder/ArcaneBondPicker.tsx` — toggle (two radio buttons). If familiar: dropdown over `Object.keys(FAMILIARS)` (imported from the engine). If object: text input for the bonded object's display name + optional `gear` picker (v1: name only).
- `builder/ClassesSection.tsx` — render for wizards (and once archetypes are wired, Tattooed Sorcerer — defer the archetype gate).
- `tracker/` (wherever the character's class-feature surface is rendered — likely the play-tab class-features panel): "Familiar: bat" row with the bonus provenance (e.g. "+3 Fly (bat familiar)"), or the conditional `note` for hawk/owl. Bonded object row with the tooltip explaining the raw mechanics (1/day cast + concentration DC 20 + spell level when casting without it).

**Success Criteria**:

- `bun run typecheck && bun run test` green; existing fixtures unchanged; new tests pass.
- Builder: a wizard with `arcaneBond = { type: "familiar", familiarKind: "bat" }` shows a +3 untyped Fly bonus with "Bat familiar" provenance in the skill panel; a toad familiar raises max HP by 3 with provenance (verify via fixtures).
- Bonded object with `bondedItemRef` set: the tooltip renders and the chosen gear item is flagged (v1: UI flag only — the engine doesn't enforce removing the bonded object, which is a play action).
- Persistence: reload page; the bond choice persists (additive optional field, no migration).
- Back-compat: an existing wizard doc with no `arcaneBond` produces byte-identical `compute()` output.
- Licensing: the new `familiars.ts` is authored from published PF1 rules; no Foundry source is transcribed (verify by the absence of any paste/clipboard evidence — the file should look like `tables.ts` / `feat-effects.ts`: hand-curated constants).

**Tests**:

- `packages/engine/test/familiars.test.ts` (new): hand-computed. bat → +3 Fly (`skill.fly`); cat → +3 Stealth (`skill.ste`); toad → max HP +3; rat → Fort save +2; weasel → Ref save +2. Choose a familiar whose kind isn't in the table → `compute()` returns the sheet unchanged (graceful soft-fail).
- `apps/web/test/doc.bond.test.ts` (new): `setArcaneBond` set/clear; familiar requires kind; blank-stripping; setting object with unknown gear-ref is accepted (soft warning only).
- Stacking fixture: wizard with cat familiar (+3 Stealth untyped) + a competence-typed Stealth item bonus → both apply and sum (untyped stacks with typed; verifies the familiar bonus routes through `stacking.ts` and shows provenance).
- Regression: an engine fixture without `arcaneBond` is byte-identical to before the change.

**Files touched**:

1. `packages/schema/src/character.ts` — `build.arcaneBond` + JSDoc.
2. `packages/engine/src/familiars.ts` — new, hand-authored.
3. `packages/engine/src/compute.ts` (or `collect.ts`) — apply familiar changes from `build.arcaneBond.familiarKind`.
4. `apps/web/src/model/doc.ts` — `setArcaneBond`.
5. `apps/web/src/components/builder/ArcaneBondPicker.tsx` — new.
6. `apps/web/src/components/builder/ClassesSection.tsx` — wire picker for wizards.
7. `apps/web/src/components/tracker/` — familiar/bonded-object surface + tooltip.
8. Two test files as above + one stacking fixture.

**Status**: Complete (2026-07-02). As-built notes:

- `bondedItemRef` (an `ItemInstance.id`) was impossible as planned — `ItemInstance` has no id field (gear is addressed by array index). Used the plan's own v1 fallback: free-text `bondedItemName`, stored raw when non-blank so a controlled input can carry mid-typing spaces.
- No separate tracker panel: the familiar's numeric provenance flows automatically through the sheet's skill/save/HP breakdowns (`"Bat (familiar)"` components), and `ArcaneBondPicker` itself carries the master-bonus summary, the hawk/owl conditional notes, and the bonded-object RAW text (1/day cast + DC 20 + spell level concentration). Alertness-while-adjacent is noted in the picker hint.
- Stacking fixture uses an active buff (competence Stealth) rather than an item, exercising the same collect→stack path.
- Verified end-to-end in the browser: bat Fly +0→+3, toad Max HP 6→9, rat Fort +0→+2, bonded-object name persists across reload (Dexie), re-clicking the active type chip clears the bond, no console/page errors.

---

## Stage 4 (partially done): Bloodline arcana/powers + school powers

**Goal**: Author the **passive abilities** that today are prose-only: a sorcerer's bloodline arcana (e.g. Draconic "+1 HP/HD", Fey "+2 vs enchantment) and bloodline powers (the named abilities unlocked at SL 1/3/9/15/20); a wizard's arcane school powers (diviner's foreknowledge, evoker's elemental damage, etc.); and — found in the same audit — a cleric's domain powers (Fire Domain's Fire Bolt/Fire Resistance, etc.), which this doc never separately called out as deferred.

> **Correction (2026-07-03)**: the premise "none are in the vendored Foundry content as structured data" below was **wrong** for wizard school powers and cleric domain powers — both live as fully structured `type: feat` entries under `class-abilities/wizard-schools/*.yaml` and `class-abilities/domains/*.yaml` respectively (name, description, level-gated `links.supplements`, `uses.maxFormula` for per-day powers), in the exact shape `transformClassFeature`/`transformClass` already handled for ordinary class features. **Implemented**: `packages/schema/src/refdata.ts` (`Domain`, `WizardSchool`, `RefData.domains`/`wizardSchools`), `packages/data-pipeline/src/transform/classes.ts` (`transformDomain`, `transformWizardSchool`, shared `resolveFeatureGrants`), `packages/engine/src/archetypes.ts` (`collectGrantedFeatures` — feeds both `resolveClassFeatures` display and `resources.ts`'s uses/day pools automatically), and the `DomainPicker`/`SchoolPicker`/`ClassFeaturesList` builder UI (origin-labeled entries, e.g. "Fire Bolt — Fire Domain"). Scope: top-level domains/schools only (35 domains, 9 schools) — subdomains and druid-domains are excluded (no structural link back to a parent domain in the source data), as are the elemental/focused-school variant rules.
>
> **Sorcerer bloodline arcana/powers remains genuinely unvendored** (prose-only) — the rest of this section's analysis still applies to that half only.

**Why still deferred (bloodline arcana/powers only)**:

- Each bloodline (~40) needs its arcana/powers hand-authored clean-room from published PF1 rules (not in the vendored Foundry content as structured data, unlike wizard schools/cleric domains above). That's dozens of small change-set entries with bespoke conditions (Draconic's "+1 HP/HD" needs a new `target`; Fey's "+2 vs enchantment" needs a bounded save-tag mechanism that doesn't exist yet).
- The bonuses route through the engine's change-application path (same path as `feat-effects.ts`), but several need new targets (`hd.bonus` for the +1 HP/HD; `saves.vs.<descriptor>` for descriptor-bounded saves) that aren't currently consumed in `compute.ts`.
- The UI work is small (render the auto-granted abilities in the build summary) but the data entry is the lion's share.

**Scope sketch** (not committing yet):

- New engine file `packages/engine/src/bloodlines.ts` mirroring `familiars.ts` / `feat-effects.ts` — each entry `{ id, name, levelUnlocked, changes: Change[], context?: ContextNote }`.
- Some entries need new engine targets: `hd.bonus` (+1 HP/HD), `saves.vs.enchantment` (+2 vs a descriptor). Adding new targets is a knock-on change to `compute.ts` — small, but each one needs a fixture. Evaluate whether to (a) add the targets, or (b) defer the abilities that need them and ship only the ones that fit existing targets (Draconic "+1/HD HP" is the most table-visible, since every sorcerer Draconic missing it has wrong max HP).
- UI: builder summary surfaces "Bloodline: Draconic — Arcana: +1 HP/HD, Powers: Claws (L1), Breath Weapon (L9, …)"; tracker renders them in the class-features panel (same `origin`-label pattern now used for domain/school powers).

**Decision deferred.** Reassess after the tester confirms which passive bonuses they actually miss in play — it may turn out only the +1 HP/HD (Draconic) and a couple of save bonuses are table-visible enough to justify the data-entry cost.

**Status**: Wizard school powers + cleric domain powers **Done** (2026-07-03). Sorcerer bloodline arcana/powers: Not Started (deferred per staging rationale above — genuinely hand-authoring, unlike the other two).

---

## Deferred items (carried forward from the previous plan)

- **Custom Powers** (build-time authored named abilities with typed `Change`s + resource pools, surfaced in play) — deferred per user from the GM Grants plan. Still deferred; no progress since.

### Same-vein gaps found while auditing this plan (2026-07-02) — NOT in scope here

A sweep of the other sliced classes for the same shape (choice-bearing, prose-only class features with no schema field) found two more, plus two non-gaps:

- **Barbarian rage powers** — `Rage Powers` class feature is prose-only (`changes: []`); no schema field for which powers were picked, no effects. Same treatment as this plan would apply: a named `build.ragePowers?: string[]` + hand-authored effects table. Deferred — reassess after Stages 1–3.
- **Rogue talents / advanced talents / master strike** — `Rogue Talents`, `Advanced Talents`, and `Master Strike` class features are prose-only (`changes: []`); no schema field for which talents were picked, no effects. Same treatment: a named `build.rogueTalents?: string[]` + hand-authored effects table. (Sneak Attack's dice progression is the one piece of Rogue that got hand-authored — `sneakAttackDice` in `packages/engine/src/tables.ts` — since it's unconditional and table-visible every session; talent selection is a separate, deferred concern.) Deferred — reassess after Stages 1–3.
- ~~**Fighter weapon training group choices**~~ — **Built (2026-07-06)**, see the dated #45 "Batch-extraction wave prep" section below: `build.weaponTrainingGroups` + `WeaponTrainingPicker.tsx` + `tables.ts`'s `weaponTrainingBonus`/`collect.ts`'s derivation.
- _Non-gap_: **Armor Training** carries structured `changes` in the vendored data and already applies via the class-feature path. **Bravery** is prose-only but choice-free and situational (+1 Will vs fear per 4 levels) — context-note material at most.
- _Non-gap_: **Channel Energy** is already modeled (`tables.ts` `channelEnergyDetail` + resource pools).

### Paladin/Ranger mechanical audit (issue #13 step 2, 2026-07-02)

Following the same treatment as Rogue's step 2 (`sneakAttackDice`): Smite Evil's attack/damage/AC scaling is hand-authored clean-room (`smiteEvilDetail` in `packages/engine/src/tables.ts`, wired into `resolveClassFeatures`'s `detail` field) since it's unconditional and table-visible every session it's active. Lay on Hands' healing dice similarly got a small hand-authored `layOnHandsDice` wired into `resources.ts`'s tag-gated detail (mirroring `channelEnergy`'s special case — Lay on Hands does carry a real `tag: "layOnHands"` upstream, so no new machinery was needed). Divine Grace and both classes' `uses.maxFormula` resource pools (Smite Evil uses/day, Lay on Hands uses/day) already worked for free via the generic changes-formula/resource pipeline, confirmed by fixture tests rather than assumed. Ranger's Combat Style Feat _bonus feat count_ (`floor((@class.unlevel + 2) / 4)` targeting `bonusFeats`) also already flows through the generic `classBonusFeats` pipeline in `apps/web/src/model/feats.ts` with no changes.

**Built (2026-07-03), the "situational bundle":** Favored Enemy, Favored Terrain, and Combat Style _feat choice_ are now implemented. Because none of these bonuses are always-on (they apply only vs. a specific creature type / in a specific terrain — the player judges at the table), they surface through the **saved-roll attachment path** rather than the always-on derived sheet, extending the same mechanism as the situational-feat attachments (`SITUATIONAL_FEAT_EFFECTS`). Shape:

- Rules tables + level→slot/budget math are hand-authored clean-room in `packages/engine/src/ranger.ts` (`FAVORED_ENEMY_TYPES`, `FAVORED_TERRAIN_TYPES`, `COMBAT_STYLES` — the two CRB styles plus the five from Ultimate Combat (Crossbow, Mounted Combat, Natural Weapon, Two-Handed Weapon, Weapon and Shield), each with its per-style feat tree (all 51 tree slugs verified to resolve to vendored feats); `favoredEnemySlots`/`favoredTerrainSlots`/`favoredBonusBudget`; `computeRanger` → `DerivedSheet.ranger`). Bonuses are _player-assigned per pick_ (`build.favoredEnemies`/`favoredTerrains` = `{type, bonus}[]`) because the RAW distribution (add a new type at +2 AND raise one existing by +2 at each milestone) is a choice, not derivable from level — the picker soft-validates against the budget but never hard-blocks.
- **Favored Enemy / Favored Terrain** attach to a saved roll (`SavedRoll.rangerBonuses`, resolved live from `sheet.ranger` in `apps/web/src/model/savedRolls.ts` — favored enemy folds attack+damage, terrain folds the roll total; a since-removed pick degrades to a reminder chip). Builder UI: `RangerPicker.tsx`.
- **Combat Style feat choice** (`build.combatStyle`) waives the _hard_ prereq block for that style's feat tree (`prereqs.ts` `bypassBlockedSlugs`, fed by `model/ranger.combatStyleFeatSlugs`; the feat picker shows a "⚑ combat style" note). The bonus-feat _count_ already worked (see above).

Still deferred:

- **Divine Bond** (paladin) — weapon bond (temporary weapon enhancement bonus) or mount (special mount table, similar shape to the druid/ranger animal-companion system built below, but paladin's _mount_ stat progression differs from the CRB "Animal Companion Base Statistics" table this project's `@pf1/engine` `companion.ts` implements — not reused as-is). Still deferred.
- **Mercy** (paladin) — Lay on Hands additionally cures a condition per mercy known, scaling in number with level. Would need `build.paladinMercies?: string[]` + a condition-cure effects table. Deferred.
- **Hunter's Bond (companion half done, 2026-07-05)** — the companion option is now built: `build.animalCompanion.source` includes `"hunters-bond"`, and `@pf1/engine` `companionEffectiveLevel` sums `rangerLevel - 3` (min 0, i.e. nothing before 4th) into the companion's effective druid level, alongside a druid's Nature Bond contribution if both are chosen (see below — a documented v1 simplification, not strict RAW). The OTHER Hunter's Bond option — sharing half the ranger's favored-enemy bonus with allies within 30 ft. as a move action — is NOT modeled (situational party-buff action, no home in this app's "no dice roller" posture); a ranger who picks that option instead simply leaves `source` without `"hunters-bond"` and sees no companion panel.
- _Non-gap_: **Divine Grace** already applies via the generic `changes[]` pipeline (`@abilities.cha.mod` → `allSavingThrows`, untyped) — no hand-authoring needed.
- _Non-gap (documented gap, not built)_: **Track** (ranger) carries a vendored `contextNotes[]` entry (`+max(1, floor(@class.unlevel/2))` to Survival checks to follow tracks) but this engine's `contextNotes` support (`packages/engine/src/conditions.ts`) is a separate, hand-authored condition mechanism — it does NOT consume RefData's vendored `contextNotes` from class features/items/races (`ClassFeature` in `packages/schema/src/refdata.ts` has no `contextNotes` field at all; the data-pipeline transform never captures it for class-abilities). Wiring vendored `contextNotes` generically would be new engine machinery, out of scope for this step. Track's skill bonus is therefore not modeled; a player must apply it manually.

### Monk mechanical audit (issue #13 step 1, 2026-07-02)

Only the core `monk` class is vendored (tag `monk`); `monk-unchained` (the optional-rules variant, tag `monkUnchained`) is intentionally skipped, same posture as skipping antipaladin when paladin was added. BAB/saves/HP/skills work off the vendored class def as usual (non-caster, no spellcasting block needed). Most of monk's numeric class features already flow through the existing generic pipelines with no hand-authoring, confirmed by fixture tests rather than assumed: Bonus Feat (Monk)'s count (`classBonusFeats`), Fast Movement's land-speed bonus (`applySpeedTarget`/`landSpeed`), and both Ki Pool's and Stunning Fist's `uses.maxFormula` resource pools (`deriveResourcePools`).

**Step 2 (2026-07-02)** followed the same treatment as Rogue's step 2: Unarmed Strike's damage die is hand-authored clean-room from the SRD's "Table: Monk Unarmed Damage" (Medium column only — `unarmedDamageDie` in `packages/engine/src/tables.ts`, wired into `resolveClassFeatures`'s `detail` field, matched by `cls.tag === "monk" && grant.name === "Unarmed Strike"`), since the vendored feature's own dice-bearing action formula (`sizeRoll(...)`) is out of scope for this engine's non-numeric-dice-eval formula DSL. Flurry of Blows got a similarly hand-authored, display-only `flurryOfBlowsLabel` summarizing total flurry attack count and the flat -2/monk-level-as-BAB mechanic — deliberately NOT wired into the live attacks/iteratives table (that would need a general "alternate full-attack routine at reduced effective BAB" mechanism benefiting every future full-BAB martial class, out of scope for monk alone). Also fixed the Unarmed Strike bonus-feat mis-attribution documented below.

Four real gaps/quirks were found during the step 1 audit:

- **~~Maneuver Training's CMB correction was inflated~~ — fixed.** Its vendored formula (`@class.unlevel - @attributes.bab.total`, untyped, target `cmb`) swaps medium-BAB CMB for full-monk-level CMB. `packages/engine/src/rolldata.ts`'s `buildRollData` didn't populate an `attributes.bab` path at all, and `collectModifiers()` ran _before_ `compute()` derived `bab` as a local variable — so `@attributes.bab.total` always resolved to the formula DSL's missing-path default of `0`. Fixed by hoisting the BAB-from-class-levels computation (it only depends on `doc.identity.classes`, not on any collected modifier) above roll-data construction in `compute()` and threading it through `buildRollData(..., bab)` into `attributes.bab.total`. See the fixture test in `packages/engine/test/compute.test.ts` for the corrected numeric example.
- **AC Bonus (Wis-to-AC) only correctly gates on armor** (shield/encumbrance are out of scope — see below), and no longer double-counts in CMD. Its vendored formula checks three conditions (`lt(@shield.type, 1)`, `lt(@armor.type, 1)`, `lt(@attributes.encumbrance.level, 1)`) before applying `wisMod + floor(unlevel/4)` to both `ac` and `cmd`. `@armor.type` is correctly populated by `rolldata.ts` from equipped gear, so the "not wearing armor" case works. `@shield.type` is never populated anywhere in `rolldata.ts` (missing path → `0`, i.e. always reads as "no shield"), and `@attributes.encumbrance.level` is hardcoded to `0` (no encumbrance model yet — issue #16's territory). So a monk wielding a shield or carrying a heavy load incorrectly still receives the bonus; building shield-slot tracking or encumbrance is out of scope here. **Issue #33 (fixed):** CMD used to double-count whenever a source carried both a generic `ac`-target change and its own explicit `cmd`-target change with the identical formula — the same untyped "generic"-category change fed `cmd` once via `computeAc`'s old `cmdAcBonus` (derived from any `dodge`/`deflection`/`generic` `ac.components`, an overly broad category bucket) and again via the explicit `cmd` change. This hit monk's AC Bonus, Iron Mask (both masterwork and common), and the Deflection Aura buff. Fixed in `compute.ts`'s CMB/CMD block: CMD's AC-sourced contribution is now derived by bonus **type** (RAW's eight named types — `deflection`/`dodge`/`circumstance`/`insight`/`luck`/`morale`/`profane`/`sacred`, via the `CMD_AC_TYPES` set) directly from the same `collected` "ac"-target modifiers `computeAc` reads, rather than from the AC category bucket; untyped/enhancement/racial/etc. AC bonuses are excluded from auto-derivation entirely (armor/shield/natural already were, since those live under separate `aac`/`sac`/`nac` targets). When a source has both an auto-eligible `ac` change and its own explicit `cmd` change, the `ac` copy is excluded by `sourceId`/`source` dedup, and the two pools are stacked together in one `resolveStack` pass so cross-pool same-type competition (e.g. an explicit cmd deflection bonus vs. a separate deflection ring) still resolves to the highest per type. See the "compute: CMD RAW-correct derivation, no double-counting (issue #33)" fixture block in `compute.test.ts` (Iron Mask, Deflection Aura, a plain-deflection auto-derive case, a dodge auto-derive case, an armor-exclusion case, and a two-deflection-sources stacking case), plus the updated monk AC-bonus test. Neither `cmb` nor `cmd` carries a provenance/components array on `DerivedSheet` (unlike `ac.components`), so the deduped auto-derivation has nothing to mark `applied: false` on — it's simply absent from the sum. Flat-footed CMD (losing Dex/dodge) was left out of scope, since it didn't fall out of this fix trivially and no vendored data exercises it yet.
- **Diamond Soul's Spell Resistance change has nowhere to land.** Its vendored formula (`10 + @class.unlevel`, target `spellResist`) is correctly formed, but Spell Resistance isn't a tracked stat anywhere in `DerivedSheet` or consumed as a `compute.ts` target — this app doesn't model SR at all yet. Not built here; would need a new `spellResist` field + compute wiring if ever prioritized.
- **~~Unarmed Strike's vendored `bonusFeats` change is mis-attributed~~ — fixed.** Its `changes[]` entry (flat `"1"`, target `bonusFeats`) represents the automatic Improved Unarmed Strike grant every monk gets at L1, but `classBonusFeats()`'s fixed-grant filter only skipped a class feature when its _name_ matched a real feat's name exactly (`"Unarmed Strike"` != `"Improved Unarmed Strike"`), so it wasn't recognized as an auto-granted feat and instead inflated `expectedFeatCount`'s free bonus-feat-slot budget by +1 at every monk level. Fixed in `apps/web/src/model/feats.ts` by adding a small `FEATURE_NAME_OVERRIDES` map (feature name -> the feat name it actually grants, currently just `"unarmed strike" -> "improved unarmed strike"`) consulted by both `grantedFeats()` and `classBonusFeats()` before their by-name lookups, rather than generic UUID-based feat-grant resolution (bigger, more invasive data-pipeline change, out of scope). See `apps/web/test/feats.test.ts`'s Monk Unarmed Strike block for the corrected numeric example and the assertion that "Improved Unarmed Strike" now surfaces via `grantedFeats()`.
- _Non-gap_: **Still Mind**'s vendored `contextNotes` entry is unconsumed — same shape and same non-gap as ranger Track above (`ClassFeature` has no `contextNotes` field at all).

### Druid mechanical audit (issue #13 step 2, 2026-07-03)

Cheaper than expected, same shape as bard: adding the class tag (`SLICE.classTags` + `CLASS_ARCHETYPE_FILES["druid"] = "Druid.csv"`) and regenerating data was step 1. Auditing all 15 of druid's vendored class features found every one of them prose-only (`changes: []`) except two, both of which already work through existing generic pipelines with zero hand-authoring, confirmed by fixture tests rather than assumed:

- **Nature Sense** carries real vendored `changes[]` (`+2` untyped to both `skill.sur` and `skill.kna`, specific skill ids — not the `skill.knowledge` compound-group alias bard needed) — already applies via the generic change-application path.
- **Wild Shape** carries a real vendored `uses.maxFormula` (`min(floor((@class.unlevel - 2) / 2), 8)`) — already becomes a resource pool via `deriveResourcePools`, same as Rage/Ki Pool/Bardic Performance. Its `detail` field is intentionally left `undefined`: unlike Smite Evil/Lay on Hands, Wild Shape's actual effect (turning into an arbitrary animal/elemental/plant statblock) has no bounded numeric summary to derive — the use-count is trackable, the transformation itself is not modeled.

Spellcasting: Druid is a Wisdom-based full prepared-divine caster whose spells-per-day table is numerically identical to Cleric's (itself an alias of the wizard table) — added as `DRUID_SPELLS_PER_DAY = WIZARD_SPELLS_PER_DAY` in `packages/engine/src/tables.ts` and a `druid` entry in `apps/web/src/model/spellcasting.ts`'s `CASTER_MODELS` (`preparesFromClassList: true`, `grantsAllCantrips: true`, no domain-style bonus slots). `spellListClassTags` picked up `"druid"` so `refData.spellLists.druid` inverts from vendored `learnedAt.class` the same way cleric's does.

Deferred, same posture as Rogue Talents / Barbarian Rage Powers (prose-only, choice-bearing, no vendored numeric formula):

- **Nature Bond (companion half done, 2026-07-05)** — the animal-companion option is now built (issue: animal companion system): `@pf1/engine` `companion.ts` (`deriveCompanion`, `BASE_COMPANIONS` — 14 hand-authored forms with a 1st-level stat block and a display-only 4th/7th-level size+attack growth step, `ANIMAL_COMPANION_PROGRESSION` — the CRB/APG "Table: Animal Companion Base Statistics" by effective druid level 1–20, reusing `tables.ts`'s `babForLevels`/`saveForLevels` for BAB/saves since the companion's own HD reproduces both exactly), `packages/schema` `build.animalCompanion`/`live.animalCompanion`, `apps/web/src/model/companion.ts` + `companionDisplay.ts`, the `AnimalCompanionPicker` (builder, alongside `FamiliarPicker`) and `CompanionPanel` (tracker, mirroring the redesigned `FamiliarPanel`'s compact StatSeal layout). A druid toggles `"nature-bond"` in `build.animalCompanion.source` to indicate the companion (not domain) option was chosen; `companionEffectiveLevel` uses the druid's own class level directly. Buff sharing (Share Spells) mirrors the familiar's `sharedBuffIds` routing exactly (AC/saves/skills/attack/damage/ability/speeds/init — the routing logic itself was extracted into `@pf1/engine` `shared-creature-buffs.ts` and is now shared by both `familiar.ts` and `companion.ts`, not forked). The Boon Companion feat (+4 effective level, capped at character level) is wired (`model/companion.ts` `hasBoonCompanionFeat`, resolved by the web layer since the pure engine module never takes `RefData`).

  **Still deferred**: Nature Bond's ONE-DOMAIN alternative (a druid choosing a cleric-style domain instead of a companion) is not built — same gap as originally noted: the app's existing `DomainPicker`/`build.clericDomains` is hardcoded to cleric's "pick two domains" behavior, and Nature Bond's domain spell list would need its own bonus-prepared-slot treatment. A druid who picks the domain option instead simply leaves `build.animalCompanion.source` without `"nature-bond"` (or has no `build.animalCompanion` at all) and sees no companion panel — no numeric consequence either way yet. Companion feat/skill-rank investment (the table's "Feats"/"Skills" columns) is surfaced as a bonus-count/six-physical-skill display only, not a full companion build/skill-picker — matches this module's "do not over-engineer" scope (see `companion.ts`'s doc comment for the full list of deliberate v1 simplifications: no primary/secondary natural-attack halving, Multiattack/Devotion as display chips only, no per-species skill-rank investment).

- **Wild Empathy** — a `1d20 + druid level + Cha modifier` check, structurally like a skill check but with no vendored `uses.maxFormula` (it's an at-will re-triable check, not a limited resource) and no existing "special check" display surface in the tracker (unlike Smite Evil/Lay on Hands, which hook into the resource-pool `detail` line). Would need a new UI concept, not just a hand-authored formula. Deferred; the ability's text renders as-is via `ClassFeaturesList.tsx`.
- _Non-gap_: **Spontaneous Casting (DRU)** (swap a prepared spell for Summon Nature's Ally) is prose-only and unmodeled, but this is consistent with the app never having modeled Cleric's parallel spontaneous cure/inflict casting either — not a druid-specific gap.
- _Non-gap_: **Chaotic/Evil/Good/Lawful Spells** (alignment-gated spell-list restriction) has no numeric content to hand-author; it's a restriction on which spells may be prepared, not a bonus.

### Arcanist mechanical audit + hybrid casting (issue #13/#35 follow-through, 2026-07-04)

Adding the class tag (`SLICE.classTags` + `spellListClassTags` + `CLASS_ARCHETYPE_FILES["arcanist"] = "Arcanist.csv"`, 11 archetypes ingested) and regenerating data was step 1, same as every prior class. The class def resolves 8 linked class features: Arcane Reservoir, Arcanist Exploits, Arcanist Spells, Cantrips, Consume Spells, Spellbooks (ARC), Greater Exploits, and Magical Supremacy (L20 capstone).

**Arcanist is the first genuinely HYBRID caster modeled** (ACG): she prepares a limited number of spells from an unbounded, wizard-shaped spellbook each day (the "Spells Prepared" table), then casts spontaneously from among those prepared spells by spending a per-level slot from a separate, sorcerer-shaped daily pool (the "Spells per Day" table) — casting never expends the specific prepared spell, only a slot. This needed a third table shape the engine didn't have yet:

- `packages/engine/src/tables.ts`: `ARCANIST_SPELLS_PER_DAY` (the cast-slot pool; column 0/cantrips always `null` — the ACG table has no 0-level column) and `ARCANIST_SPELLS_PREPARED` (the wizard-shaped daily readying cap, columns 0–9; NOT adjusted by ability score, same posture as the sorcerer/bard spells-KNOWN tables) plus a new `baseSpellsPrepared()` function/`SpellPreparedProgression` type, parallel to `baseSpellsKnown()`/`SpellKnownProgression` but semantically distinct — arcanist's spellbook is unbounded (wizard-style), only the daily-prepared SUBSET is capped, unlike a sorcerer's permanently-capped known list. Both tables cross-checked against aonprd.com and the legacy PRD mirror (two independent sources, exact match) — sanity anchor at L4: 4/2 first/second-level slots per day (6/3 with Int 20's bonus slots), 3/1 first/second-level spells prepared plus 6 cantrips prepared, matching the owner's Hero Lab sheet.
- `apps/web/src/model/spellcasting.ts`: `CasterModel.preparation` gained a third value, `"hybrid"`, plus an optional `preparedProgression` field (read by the new `preparedCapacityByLevel()` helper, mirroring `spellsKnownLimitsByLevel()`'s shape but never ability-adjusted). The `arcanist` `CASTER_MODELS` entry: `ability: "int"`, `progression: "arcanist"` (cast slots), `preparedProgression: "arcanist"` (prepare cap), `grantsAllCantrips: true` (same simplification already applied to wizard/cleric/druid — cantrips are capped by the prepared table's column 0 but treated as cast-at-will once readied, rather than modeling the RAW "prepared but not expended" nuance as a literal unlimited-cast toggle), `preparesFromClassList: false` (has its own curated spellbook, exactly like wizard). Because `preparesFromClassList` and `knownProgression` are both unset/false for arcanist, the BUILDER's `SpellsSection.tsx` needed **zero changes** — it already treats arcanist's spellbook exactly like a wizard's (unbounded add/remove, no known-limit advisory, since `isSpontaneous` checks `=== "spontaneous"` strictly and arcanist is `"hybrid"`).
- `apps/web/src/components/tracker/PreparedSpellsPanel.tsx`: new `HybridView`, recombining pieces of `PreparedView` and `SpontaneousView` rather than inventing new model machinery — a **Prepare** section (readies spells from the spellbook into `live.spells.prepared`, capacity from `preparedCapacityByLevel`, reusing `prepareSpell`/`unprepareSpell`/`removePreparedAt`/`clearPrepared` from `preparedSpells.ts` unchanged) feeds a separate **Cast** section (spends a slot from `spellSlotsByLevel`/`progression`, reusing `castSpontaneousSlot`/`restoreSpontaneousSlot`/`spontaneousSlotStatus`/`resetSpontaneousSlots` from `spontaneousSpells.ts` unchanged) that offers whatever is currently prepared at that level. Unlike `PreparedView`, prepared rows here have no per-instance Cast/Recover — casting only ever touches the shared slot pool, never marks a specific instance `expended`. `model/rest.ts`'s `restNewDay()` needed **zero changes**: it already calls both `restPreparedSpells` (a no-op for arcanist, since nothing is ever expended) and `resetSpontaneousSlots` for every caster class on the document.
- `packages/schema/src/character.ts` was **not touched** — the hybrid model needed no new document fields, only new interpretation of the existing `live.spells.prepared` + `live.spells.slotsUsed(ByClass)` shapes already built for wizard/sorcerer.

**Deferred, same posture as Rogue Talents / Barbarian Rage Powers** (prose-only, choice-bearing, no vendored numeric formula — no schema field, no picker):

- **Arcanist Exploits** (chosen at L1 and every 2 levels thereafter) and **Greater Exploits** (L11+) — the exploit list itself (Potent Magic, Quick Study, Dimensional Slide, etc.) isn't even linked via the class's `links.supplements` (only the "Arcanist Exploits"/"Greater Exploits" class-feature stubs are); each exploit is its own standalone `class-abilities` entry (e.g. `potent-magic.ByZBsqXi9uhbPSJn.yaml`, `quick-study-arc.bQIHXFmVTfQv6rD3.yaml`) with no vendored linkage back to the class at all. Building a picker would need a hand-curated exploit list (there is no data-driven way to enumerate "the arcanist exploit options"), same shape of effort as Rogue Talents. Deferred.
- **Consume Spells** and **Quick Study**'s move-action/full-round-action spell-slot-for-reservoir-point (and vice versa) exchange mechanics are not modeled beyond their `uses.maxFormula` resource pool (Consume Spells' use-count, `@abilities.cha.mod` — confirmed via fixture test) — the actual exchange (spend a prepared slot to gain a reservoir point, or spend a reservoir point to swap a prepared spell) is a table-adjudicated action this app doesn't walk the player through, same posture as Smite Evil/Lay on Hands not walking through their attack rolls.
- **Arcane Reservoir RAW nuance not modeled**: the pool's cap (`3 + arcanist level`, vendored `uses.maxFormula: "3 + @class.unlevel"`, confirmed 7 at L4 via fixture test) already rides the generic `deriveResourcePools` pipeline for free. The RAW _daily refill_ amount is actually smaller — `3 + floor(level / 2)` (present in the raw Foundry YAML as `system.uses.rechargeFormula`, e.g. 5 at L4, not the full 7 — "any points she had from the previous day are lost" before refilling) — but `normalizeUses()` in `packages/data-pipeline/src/transform/common.ts` only ever extracts `maxFormula`/`per`, so `rechargeFormula` isn't even in our vendored `ClassFeature.uses` shape today, for any class. This engine's generic resource-pool model also has no separate refill-vs-cap distinction (every other resource pool in this app refills to its cap on rest, e.g. Rage/Ki Pool/Bardic Performance), so v1 treats Arcane Reservoir as refilling to its full cap each day like every other pool — a minor RAW deviation (over-refills by `ceil(level/2)`, i.e. +2 at L4) rather than a missing feature. Flagged here rather than silently accepted; revisit only if a future class needs `rechargeFormula` badly enough to justify capturing it in the pipeline and adding refill-vs-cap machinery to `deriveResourcePools`.

### Archetype numeric effects, round 2 — top ~20 by play popularity (2026-07-05)

Extends the issue #7 `ARCHETYPE_FEATURE_EFFECTS` table (`packages/engine/src/archetype-effects.ts`) from its original 6-archetype slice (Weapon Master, Invulnerable Rager, Savage Barbarian, Wildborn, Cloistered Cleric, and the 6 ranger Combat Style reflavors — Bow Nomad/Horse Lord/Ilsurian Archer/Shapeshifter/Stormwalker/Toxophilite, counted individually below) to a hand-picked ~20-most-played slice across all 12 currently-supported classes (fighter, barbarian, rogue, ranger, paladin, monk, bard, cleric, druid, wizard, sorcerer, arcanist — magus/oracle and animal companions were being added by parallel agents and are deliberately untouched here). Selection method: enumerated every archetype actually vendored per class from `packages/data-pipeline/data/archetypes.json` first (the task's assumed candidates — Zen Archer, Qinggong Monk, Crossblooded/Wildblooded Sorcerer — turned out **not to be in the vendored third-party CSV compilation at all**, confirmed by grep; picked real substitutes from what's actually there instead), then picked by real-world popularity + whether the archetype's OWN replacement feature (not just what it swaps out) has a genuinely unconditional, Change-shaped number.

This is the hand-authored seed for issue #45 (future batch-extraction pipeline) — the audit notes below (which vendored features were checked and why they were/weren't modeled) are exactly the kind of judgment call a future extraction script would need a human-reviewed example set for.

**Real numbers added** (6 new entries, on top of the 11 pre-existing archetype ids):

- **Archer** (fighter) — Hawkeye: `+1` Perception at 2nd, `+1`/4 levels thereafter (`skill.per`), replacing Bravery (which carries no vendored number anyway). The companion "+5 ft. bow range increment" half of the same ability is NOT modeled — there's no engine target for a weapon's range increment at all (attack rolls don't model range), so only the modelable half is added rather than guessing at the other.
- **Crusader** (cleric) — Bonus Feat: a restricted-list bonus feat at 1st, 5th, and every 5 levels thereafter (`1 + floor(level/5)`, target `bonusFeats`, same reflavor pattern the 6 ranger Combat Style archetypes already established). Flagged in-code: the vendored prose's "maximum of six at 20th level" doesn't reconcile with its own enumerated schedule (which yields five) — modeled as five, discrepancy noted rather than silently "fixed" by inventing a sixth gate.
- **Sorcerer of Sleep** (sorcerer) — Pesh Expert: flat `+1/2 level` (min 1), untrained, on four named skills (Appraise, Craft (alchemy), Heal, Knowledge (local)) — same shape as Cloistered Cleric's Breadth of Knowledge. **Known composition gap** (pre-existing, not introduced here): this replaces "bloodline arcana," which isn't a normal `RefData.classFeatures` entry (it's hand-authored in `bloodlines.ts`, applied by a dedicated `collect.ts` loop with no archetype-swap awareness at all) — so a Sorcerer of Sleep with a bloodline picked still gets both this bonus AND their bloodline arcana. The vendored data can't pair this swap (no `pairedBaseFeatureUuid`), so the UI's existing "may replace an existing ability" ambiguous-swap warning already surfaces it to the player; not fixed here (would mean adding bloodline-arcana-aware suppression logic to `collect.ts`, out of scope for a merge-friendly, append-only pass with two other agents touching adjacent files).
- **Seeker** (sorcerer) — Tinkering: flat `+1/2 level` (min 1) on Disable Device (the ability's Perception-to-locate-traps half is scoped/conditional and left unmodeled, same bar as everywhere else in this file).
- **Nornkith** (monk) — Nimble Reflexes: flat `+2` Reflex saves at 3rd level, replacing Still Mind (no vendored number to suppress).

**Notes-only, audited and confirmed non-modelable** (documented here rather than in code — `ClassFeaturesList` already renders each archetype feature's full vendored prose description regardless of whether it has a table entry, so a notes-only archetype doesn't strictly need a code entry to be useful to a player; several were still given a `changes: []` + `detail` table entry anyway, purely to surface a terse scaling-number summary next to the prose — `archetypeHasModeledEffects` was tightened to require `changes.length > 0` so these don't falsely earn the picker's "M" badge):

- **Urban Barbarian** (barbarian) — Controlled Rage reflavors rage's ability bonuses, but the engine doesn't apply rage's baseline ability bonuses automatically at all today (rage's Str/Con bump is left to a manual player-added buff, same posture this file already takes for ki/grit/panache) — nothing to override.
- **Two-Handed Fighter** (fighter) — Overhand Chop (double Str bonus on one chosen 2H attack/charge) is per-attack situational, same bar as Power Attack/Deadly Aim's situational-only treatment in `feat-effects.ts`.
- **Scout** (rogue) — Scout's Charge / Skirmisher (sneak attack on a charge / on a move-then-attack) are conditional on the specific action taken that round.
- **Knife Master** (rogue) — Hidden Blade/Sneak Stab/Blade Sense are all scoped to a specific weapon category (and Blade Sense specifically depends on the ATTACKER's weapon, which the static sheet can't know).
- **Oath of Vengeance** (paladin) — Channel Wrath is a resource conversion (spend lay-on-hands for an extra smite), not a flat number.
- **Divine Hunter** (paladin) — Precise Shot as a bonus feat has no baseline static number of its own (see `feat-effects.ts`'s situational treatment of Precise Shot); its later features are all conditional ally auras.
- **Archaeologist** (bard) — Archaeologist's Luck is a REAL, precisely-scaling number (+1/+2/+3/+4 luck at 1st/5th/11th/17th) but it's an activated, swift-action, bardic-performance-style buff — and the engine has **no generic "activated performance buff" mechanism at all yet** (checked: base bardic performance today is only a rounds/day resource pool in `resources.ts`, with no toggle applying Inspire Courage's or anything else's numeric benefit while active). Recorded here as the concrete motivating example for that future feature rather than inventing bespoke one-off engine machinery for a single archetype.
- **Menhir Savant** (druid) — entirely activated/resource-gated (detection, per-use caster-level bumps, limited-use spell-likes).
- **Spell Sage** (wizard) — Focused Spells / Spell Study are both activated, resource-gated abilities (spend a daily use to spike caster level or borrow another class's spell) with no passive baseline number.
- **School Savant** (arcanist) — audited all 11 vendored arcanist archetypes; every one reworks the arcane reservoir/exploit/spells-known subsystems rather than granting a flat number. School Savant (the most-played, a direct analogue of the wizard arcane school) is the representative entry: correctly modeling it would mean wiring a whole new cross-class feature-grant path (mirroring `collectGrantedFeatures`'s domain/school handling) to let an arcanist inherit a wizard school's powers — out of scope for a table-entry pass.

**Composition with `activeArchetypeSwaps`**: every new real-number entry either (a) has a clean 1:1 paired swap the existing machinery already suppresses correctly (Archer/Hawkeye replacing Bravery), or (b) is a pure additive grant with no base-feature slot to suppress (Crusader's Bonus Feat, Sorcerer of Sleep's Pesh Expert, Seeker's Tinkering — none of these have a `pairedBaseFeatureUuid`, so there's no suppression to get right), or (c) is an ambiguous/unpaired swap already surfaced to the player via the existing "may replace an existing ability" warning (Nornkith's Nimble Reflexes vs. Still Mind — Still Mind has no vendored number anyway, so no leak). No changes were needed to `activeArchetypeSwaps` or `collect.ts`'s suppression logic itself; the new test suite adds one more concrete regression case (Archer/Bravery) alongside the three pre-existing ones (Two-Handed Fighter/Armor Training, Weapon Master/Armor Training, Wildborn/Damage Reduction).

**Existing-code finding, not fixed here**: monk's Maneuver Master (Ironskin Monk) archetype was investigated and deliberately DROPPED from this pass — its "Iron Skin" (natural armor) and "Tough as Nails" (DR) features both replace monk class features that carry REAL vendored `Change`s (`AC Bonus (MNK)`'s Wis-to-AC formula, and Fast Movement's `landSpeed` bonus) via ambiguous/unpaired swaps (no `pairedBaseFeatureUuid`). Unlike the Sorcerer of Sleep gap above (a hand-authored bloodline power the swap machinery was never built to reach), this is the SAME category of bug `archetypes.ts`'s `AMBIGUOUS_DR_REPLACEMENTS`/`barbarianDamageReductionReplaced` was hand-built to fix for barbarian DR — it would need an equivalent hand-maintained exclusion mechanism for monk AC/speed, which is real engine work beyond a table entry. Flagged for a future pass rather than silently shipping a double-counted AC/speed bonus.

**Full ~20 list** (11 pre-existing + 6 new-with-numbers + 9 new-notes-only = 26 archetypes total now have a table entry or an audited note, spanning all 12 classes): Weapon Master, Two-Handed Fighter, Archer (fighter) · Invulnerable Rager, Savage Barbarian, Wildborn, Urban Barbarian (barbarian) · Scout, Knife Master (rogue) · Bow Nomad, Horse Lord, Ilsurian Archer, Shapeshifter, Stormwalker, Toxophilite (ranger) · Oath of Vengeance, Divine Hunter (paladin) · Nornkith (monk) · Archaeologist (bard) · Cloistered Cleric, Crusader (cleric) · Menhir Savant (druid) · Spell Sage (wizard) · Sorcerer of Sleep, Seeker (sorcerer) · School Savant (arcanist).

---

### Magus mechanical audit (issue #13 step 2, 2026-07-05)

Adding the class tag (`SLICE.classTags` + `spellListClassTags` + `CLASS_ARCHETYPE_FILES["magus"] = "Magus.csv"`) and regenerating data was step 1, same as every prior class — the magus arcane spell list came for free from `learnedAt.class.magus`, already tagged upstream exactly like arcanist.

A plain int-based prepared-arcane caster, mechanically closer to wizard than to arcanist's hybrid model (the class prepares AND casts prepared spells — no separate slot-vs-prepared split).

- `packages/engine/src/tables.ts`: `MAGUS_SPELLS_PER_DAY` (own table, caps at 6th-level spells — columns 7-9 always `null`) + `"magus"` added to `SpellProgression`. Cross-checked against aonprd.com and d20pfsrd.com (the latter's raw table extraction was partial/garbled but every level it did return matched the aonprd table exactly, including the L20 all-5s row). `apps/web/src/model/spellcasting.ts`'s `CASTER_MODELS.magus` entry mirrors wizard's shape exactly (`grantsAllCantrips: true`, `preparesFromClassList: false`) — the builder's `SpellsSection.tsx` needed zero changes for the same reason arcanist's didn't.
- **Arcane Pool rode the generic resource-pool pipeline for free**: the vendored `arcane-pool` class feature carries a real `uses.maxFormula: "max(1, floor(@class.unlevel / 2)) + @abilities.int.mod"` — no hand-authoring needed (confirmed 7 at L7/Int 18 via fixture test).
- Spell Combat, Spellstrike, Spell Recall, Fighter Training, Knowledge Pool, Counterstrike, Medium/Heavy Armor, Improved/Greater Spell Combat, True Magus: all prose-only class features, displayed as-is via the existing `ClassFeaturesList` — no numeric modeling needed or attempted (same posture as e.g. Rage Powers' prose entries elsewhere).
- **Magus Arcana** (chosen at 3rd level and every 3 levels thereafter): like Arcanist Exploits at that class's launch, the arcana list itself isn't even linked via the class's `links.supplements` (only the "Magus Arcana" class-feature stub is) — each arcana (Arcane Accuracy, Broad Study, Critical Strike, Familiar, Maneuver Mastery, Wand Mastery, ...) is prose-only with no vendored numeric formula. **Deferred** — no engine table, no schema field, no picker — same "prose-only, choice-bearing, hand-curate later" posture Arcanist Exploits started with before its own follow-up (issue #42) built a picker; a future pass can mirror `arcanist-exploits.ts` + `ArcanistExploitPicker.tsx` for this exact reason.

---

### Oracle mechanical audit (issue #13 step 2, 2026-07-05)

Adding the class tag (`SLICE.classTags` + `spellListClassTags` + `CLASS_ARCHETYPE_FILES["oracle"] = "Oracle.csv"`) and regenerating data was step 1, same as every prior class — the oracle divine spell list came for free from `learnedAt.class.oracle`, already tagged upstream (14 classes total now).

A plain cha-based spontaneous caster casting from the cleric spell list. Its "Spells per Day" and "Spells Known" tables are **numerically identical to the sorcerer's** (verified against aonprd.com and d20pfsrd.com at levels 1/4/10/20, including the distinctive all-9s-known/all-6s-per-day L20 shape) — `CASTER_MODELS.oracle` sets `progression`/`knownProgression` to `"sorcerer"` rather than duplicating a byte-identical table under a new key, mirroring the existing `DRUID_SPELLS_PER_DAY = WIZARD_SPELLS_PER_DAY` precedent in `tables.ts`. The "adds every cure OR every inflict spell to her known list for free" mystery-linked choice is documented as a `learnGuidance` note only — not modeled as a separate field/toggle (nothing numeric to add: it's just a spells-known addition the player makes manually).

- **Mystery** (`packages/engine/src/oracle-mysteries.ts`, `build.oracleMystery`, `MysteryPicker.tsx` — mirrors the sorcerer-bloodline pattern): scope is the 10 APG "core" mysteries (Battle, Bones, Flame, Heavens, Life, Lore, Nature, Stone, Waves, Wind); the vendored pack ships dozens more from later splatbooks, out of scope. Unlike bloodline bonus spells (inverted from a real `learnedAt.bloodline` tag), a mystery's bonus-spell list has NO equivalent vendored tag to invert — but each mystery IS individually vendored as a standalone `class-abilities` entry (`life-mystery.*.yaml`, etc., just not linked from the Oracle class def), with its `Bonus Spells` prose embedding real `@UUID[Compendium.pf1.spells.<id>]` references and its `classSkills` field carrying real Foundry skill ids. Rather than a data-pipeline supplement (like the Aberrant bloodline backfill), the 90 bonus-spell entries (10 mysteries × 9 gates) and 10 classSkills lists are hand-copied verbatim from the vendored YAML directly into `oracle-mysteries.ts` (ids, not names — sidesteps the "Restoration, Lesser" vs. "Lesser Restoration" prose-vs.-real-name mismatch a name-based resolution would risk) — a deliberate, documented simplification versus the `RefData.bloodlineSpellLists`/`supplements.ts` build-time-resolution architecture; revisit only if this data drifts. `mysterySpellsKnown()` (`apps/web/src/model/spellcasting.ts`) mirrors `bloodlineSpellsKnown()`'s shape but a DIFFERENT unlock rule — one bonus spell at oracle level 2 and every two levels thereafter (RAW), not the bloodline's `2L+1`-by-spell-level formula. Mystery class skills are display-only (a `contextNotes`-shaped concern, not wired into `compute.ts`'s `classSkillSet`) — same documented limitation `traits.ts` already carries for trait-granted class skills; no new engine machinery invented.
- **Curse** (`packages/engine/src/oracle-curses.ts`, `build.oracleCurse`, `CursePicker.tsx`): scope is the 6 base APG curses (Clouded Vision, Deaf, Haunted, Lame, Tongues, Wasting). Also individually vendored (`lame-curse.*.yaml`, etc.), also not linked from the class def. Wasting's vendored `changes[]` (`-4`/`chaSkills`/`untyped`) is copied verbatim and wired through `collectModifiers` (gated on oracle level + `build.oracleCurse`) — though note `chaSkills` is one of this engine's documented UNAPPLIED targets (`targets.ts`'s `UNAPPLIED_TARGET_LABELS`), so like the vendored data itself, it's collected but doesn't move any skill total yet; a `contextNotes` reminder covers the RAW Intimidate exception the target can't express. Lame's speed penalty is prose-only upstream but cleanly Change-shaped: `if(gte(@attributes.speed.land.total, 30), -10, -5)` against `landSpeed`, using `@attributes.speed.land.total`'s existing pre-buff race-base-speed roll-data path (the same one the Sylph "Like the Wind" trait's `+5` uses) — fixture-tested (a Human oracle's 30 ft. base drops to 20 ft.). Haunted's 4 free known spells (Mage Hand/Ghost Sound at 1st, Levitate/Minor Image at 5th, Telekinesis at 10th, Reverse Gravity at 15th) reuse the exact same `{level, id, name}` shape as mystery bonus spells (`curseSpellsKnown()`, same file as `mysterySpellsKnown()`). The other 4 curses' tiered benefits (darkvision/blindsense/blindsight progression, deafness's Silent-Spell-like casting + scent/tremorsense, the bonus-language mechanic) are genuinely situational or duplicate an existing sense the static sheet doesn't model as a discrete stat — `contextNotes` only, never an over-applied flat number, same posture as bloodline powers' conditional entries.
- **Revelations** (a mystery's own menu of choosable powers, gained at 1st/3rd/7th/11th/... — genuinely choice-bearing, per-mystery, prose-heavy, ~8-10 options per mystery across 10 mysteries) — **deferred**, no engine table, no schema field, no picker, same posture as Magus Arcana above and Arcanist Exploits at that class's launch. `MysteryPicker.tsx` notes this in its hint text so the omission is visible in the UI, not silent.

---

### Alchemist + investigator mechanical audit (issue #13 step 2, 2026-07-07)

The class tags (`SLICE.classTags` + `spellListClassTags`) and their archetype CSV mappings landed as part of the "add remaining core/base/hybrid classes" data batch (31 classes total now) — both already carried `learnedAt.class` extract tags upstream, so their spell lists (levels 1-6, no 0-level entry) came for free the same way arcanist/magus/oracle's did.

Both are int-based 6-level PREPARED "extract" casters (APG/ACG) with **no 0-level extracts at all** — unlike wizard/cleric cantrips (prepared once, then cast at will), there is no 0-level tier to grant in the first place; `CASTER_MODELS.alchemist`/`.investigator` set `grantsAllCantrips: false`, same posture as paladin/ranger (also no cantrips) rather than wizard/magus/arcanist.

- `packages/engine/src/tables.ts`: `ALCHEMIST_EXTRACTS_PER_DAY` (own table, columns 1-6 populated, column 0 and columns 7-9 always `null`) + `"alchemist"`/`"investigator"` added to `SpellProgression`. Cross-checked against aonprd.com and d20pfsrd.com, both matching exactly at every level (L1 1, L4 3/1, L7 4/3/1, L10 5/4/3/1, L16 5/5/5/4/3/1, L20 all-5s). Investigator's extracts-per-day are numerically IDENTICAL to the alchemist's per both sources, so `INVESTIGATOR_EXTRACTS_PER_DAY = ALCHEMIST_EXTRACTS_PER_DAY` reuses the table rather than duplicating it byte-for-byte, same posture as `DRUID_SPELLS_PER_DAY = WIZARD_SPELLS_PER_DAY`. (Aside, not acted on: the numbers also happen to exactly match `BARD_SPELLS_PER_DAY`'s columns 1-6 — a coincidence of the SRD math, not a modeled relationship.)
- `apps/web/src/model/spellcasting.ts`'s `CASTER_MODELS.alchemist`/`.investigator` model extracts as ordinary prepared casters (`knownLabel: "Formula Book"`) — the self-targeting/infusion mechanic (an extract is a brewed, consumable, potion-like item rather than a spell cast directly, and can't normally be shared except via specific discoveries) is NOT modeled beyond the per-day preparation count; the `blurb` says so explicitly rather than promising fidelity it doesn't have.
- **Not arcane casters**: extracts have no verbal/somatic components and are not spells for arcane-spell-failure purposes — `packages/engine/src/compute.ts`'s `ARCANE_CASTER_TAGS` (currently `wizard`/`sorcerer`/`bard` only — arcanist/magus are ALSO absent from that set, a pre-existing gap this audit did not touch) correctly excludes both; they were deliberately NOT added to it.
- **Deferred, out of scope for this pass** (mirrors the "prose-only, choice-bearing, hand-curate later" posture Magus Arcana/Arcanist Exploits started with):
  - **Discoveries** (alchemist, chosen at 2nd level and every even level thereafter) and **Investigator Talents** (investigator, 3rd level and every 2 levels thereafter) — no engine table, no schema field, no picker.
  - **Mutagen** (alchemist) and **Studied Combat/Studied Strike, Inspiration** (investigator) class features are prose-displayed only via the existing `ClassFeaturesList`; their numeric effects (mutagen's AC/ability swings, inspiration's bonus-die pool) are not wired into `compute.ts` or `deriveResourcePools` in this pass — their `uses.maxFormula`/`changes[]` were not present in the vendored `class-features.json` slice at audit time, so there was nothing to ride the generic pipeline with yet.
  - Cross-formula-book learning rules (alchemist can copy from a wizard's spellbook; investigator can additionally copy from another investigator's or an alchemist's formula book) are `learnGuidance` prose only, same posture as every other class's spell-acquisition text in this registry — no enforcement.

---

### Witch + Shaman mechanical audit (issue #13 step 2, 2026-07-07)

Both class tags (`witch`, `shaman`) plus their spell lists were already vendored (a prior data-only pass added the remaining core/base/hybrid classes to the slice in one shot); this pass is purely the casting-model wiring, same shape as the Magus/Oracle audits above.

**Witch** (APG): a plain int-based prepared-arcane caster whose "Spells per Day" table is **numerically identical to the wizard's** (verified against aonprd.com's "Table: Witch" — exact match at every level, including the L20 all-4s row), so `CASTER_MODELS.witch` sets `progression: "witch"`, an alias of `WIZARD_SPELLS_PER_DAY` in `tables.ts` (`WITCH_SPELLS_PER_DAY = WIZARD_SPELLS_PER_DAY`), same posture as the existing `DRUID_SPELLS_PER_DAY = WIZARD_SPELLS_PER_DAY` precedent. Her familiar stores her spells the way a spellbook does (begins play with all 0-level witch spells plus 3 + Int-mod 1st-level spells, +2 more of any castable level per new witch level — confirmed via aonprd.com's Witch "Spells" class feature text), so the model is otherwise wizard-shaped (`grantsAllCantrips: true`, `preparesFromClassList: false`) with `knownLabel: "Familiar's Spells"` instead of "Spellbook". Witch is ARCANE (arcane spell failure applies) — added to `compute.ts`'s `ARCANE_CASTER_TAGS`. `"witch"` added to `apps/web/src/model/casterLevel.ts`'s `FULL_CASTER_TAGS` (CL = class level, no divergence). The pre-existing `FamiliarPicker.tsx` (`build.familiar`) is already class-agnostic by design (its own doc comment: "any class/feature that grants a familiar... uses this, so it's not gated behind a class check") — no change needed there; a witch can already add a tracked familiar today. Hexes, the patron, and the familiar-link mechanics themselves (losing spellcasting if the familiar dies, etc.) are **out of scope / deferred**, same "prose-only, choice-bearing" posture as Magus Arcana/Arcanist Exploits at their own launch.

**Shaman** (ACG): a plain wis-based prepared-divine caster whose "Spells per Day" table is **numerically identical to the cleric's/wizard's** (verified against aonprd.com's "Table: Shaman" — exact match at every level, including the L20 all-4s row), so `CASTER_MODELS.shaman` sets `progression: "shaman"`, an alias of `CLERIC_SPELLS_PER_DAY` in `tables.ts` (`SHAMAN_SPELLS_PER_DAY = CLERIC_SPELLS_PER_DAY`). Modeled cleric/druid-shaped (`grantsAllCantrips: true`, `preparesFromClassList: true`, `knownLabel: "Shaman List"`) — she "can prepare and cast any spell on the shaman spell list, provided that she can cast spells of that level" (aonprd.com), no curated known list to maintain. `"shaman"` added to `FULL_CASTER_TAGS` (CL = class level). Shaman is DIVINE, so intentionally **not** added to `ARCANE_CASTER_TAGS`. **Spirit Magic** (a limited number of bonus spontaneous casts per day, drawn from her prepared spells, granted by her chosen spirit) and spirit-specific **hexes** are **not modeled** — same posture as druid's unmodeled spontaneous summoning note; flagged in the `CASTER_MODELS.shaman` blurb so the omission is visible in the UI, not silent.

Engine fixtures: L7 witch (Con 14, Int 18) — BAB +3, saves Fort+4/Ref+2/Will+5, HP 44 (d6 HD), Spellcraft 14 (class skill). L5 shaman (Con 12, Wis 16) — BAB +3, saves Fort+2/Ref+1/Will+7, HP 33 (d8 HD), Heal 11 (class skill).

---

### Archetype prose→Change extraction pipeline, pilot slice (issue #45, 2026-07-06)

Issue #45's proposal (batch LLM extraction + classifier tier + confidence/provenance + UI badge) was piloted end-to-end against **fighter only** — all 67 vendored fighter archetypes, 383 features — measured against the 3 fighter entries the hand-authored table (`archetype-effects.ts`, issue #7) already covers as ground truth (Weapon Master, Archer, Two-Handed Fighter). This section is the conventions writeup the issue asked for, so the next wave (other classes) is mechanical rather than re-derived from scratch.

**What got built:**

- `packages/engine/src/archetype-effects-extracted.ts` — same `ArchetypeFeatureEffect` shape as the hand-verified table, plus `confidence: "high" | "medium" | "low"` and `provenance: string` (the exact source sentence). 17 real entries.
- `packages/engine/src/archetype-classification.ts` — the committed, reviewable audit: all 383 fighter features, each bucketed `numeric` / `situational` / `subsystem` / `blocked` with a one-line reason. This is the artifact a reviewer (or a future wave) reads before touching code.
- `packages/engine/src/archetype-effects-resolve.ts` — `resolveArchetypeFeatureEffect(id)` is the ONE place precedence lives: hand-verified table checked first, falls back to extracted, so an id present in both can never double-apply. `collect.ts`, `archetypes.ts`, and `apps/web/src/model/feats.ts` all resolve through it instead of touching either table directly.
- `archetypeModeledEffectTier()` (replacing the old boolean `archetypeHasModeledEffects`, kept as a back-compat wrapper) returns `"verified" | "extracted" | "none"`, driving a hollow/outlined `M` badge variant (`.badge-modeled--extracted` in `styles.css`) distinct from the hand-verified solid-fill badge, plus a visible (non-hover) "extracted" text note next to a feature's `detail` in `ClassFeaturesList` — hover-only `title` tooltips remain as a _secondary_ affordance, never the only signal.

**Classification rubric applied (fighter pilot):**

- **`numeric`** — an unconditional bonus, or one gated on a condition the engine can actually check (`@armor.type` — 0 none/1 light/2 med/3 heavy, per the Savage Barbarian precedent), expressible via a real `targets.ts` target. A bonus scoped to a _specific maneuver_ (bull rush/disarm/etc. only, not general `cmb`/`cmd`), a _specific weapon_ (only expressible via `attack.weapon.<group>`/`damage.weapon.<group>`, which key off the player's own per-weapon `.group` tag — see the weapon-group finding below), a _specific enemy state_, or a _specific action that round_ does NOT qualify, even with a clean, real number — same honesty bar `traits.ts` already established (courageous/birthmark/fencer). This bar turned out to exclude the large majority of "Weapon Training, but restricted to one group" reflavors (Archer's own Expert Archer among them) — see the weapon-group finding.
- **`situational`** — a real number that fails the `numeric` bar above for scoping/conditionality reasons. Never given a `Change`; the vendored prose (already rendered in full by `ClassFeaturesList`) stays the player's source of truth.
- **`subsystem`** — grants an unrelated ability, resource, proficiency, or choice-list, or removes a penalty the engine never modeled in the first place (so there's nothing to reduce). No Change-shaped number exists to extract.
- **`blocked`** — a genuine composition trap, not just "nothing to extract": an UNPAIRED archetype feature that claims to replace only **part** of an atomic, single-formula base grant. Fighter's Armor Training (`mDexA`/`acpA`) and Bonus Feats (FGT) (`bonusFeats`) are each ONE level-based formula, not discrete per-tier/per-level grants — so "this replaces armor training 3" (one tier of four) can't be safely modeled either way: pairing the whole grant would over-suppress tiers RAW keeps, and backfilling a number for the archetype's own replacement would double it on top of the base formula's still-unsuppressed output (since it stays fully applied, unpaired). Recorded rather than guessed at. **This is the same shape of trap as the Ironskin Monk case** already on file (an ambiguous swap displacing a base feature with real vendored changes) — just triggered by partial-tier atomicity instead of an ambiguous multi-feature swap. Fixing it for real means splitting the base feature into discrete per-tier/per-level grants, which is engine work, not a table entry — flagged for a future pass. 39 fighter features landed here (30 partial Armor Training tiers, 9 partial Bonus Feats instances). **Notably, fighter had zero true Ironskin-Monk-style cases** (an ambiguous swap of an entire multi-feature grant) — every fighter archetype's Armor-Training-replacing feature either pairs cleanly 1:1, or (if unpaired) doesn't itself carry an overlapping number, so the ONLY composition trap this class surfaced was the partial-tier-atomicity variant above.

**Confidence rubric:** `high` = literal/near-literal reflavor of an already-modeled mechanism, or a single clearly-worded fully-general scaling bonus. `medium` = a non-obvious cadence derived from prose (irregular schedule, delayed onset), or a bonus with a second textual condition the engine can't check at all (encumbrance, "or using a shield") that got silently dropped rather than guessed at — flagged in the entry's `detail` string so the UI doesn't imply full RAW fidelity. `low` was reserved but unused this pass.

**Precedence rule:** hand-verified always wins for a given feature id; the two tables are typed and resolved separately so this can't drift (see `resolveArchetypeFeatureEffect` above). No fighter id needed this in practice — the two tables don't overlap today — so the precedence fixture test constructs a synthetic overlap rather than relying on real data ever colliding.

**Findings that should change how the next wave runs:**

1. **The weapon-group-tagging gap is the single biggest thing that shrank the extractable surface.** `attack.weapon.<group>`/`damage.weapon.<group>` (used by Weapon Focus/Specialization) key off a per-weapon, player-set free-text tag (`doc.build.weapons[i].group`), not a semantic "fighter weapon-training group" vocabulary (bows, hammers, spears, ...) — and base Fighter's own Weapon Training class feature carries `changes: []` upstream, so it isn't modeled anywhere in the engine today either. Extracting an archetype's "Weapon Training, but only for hammers" reflavor would be net-new engine-adjacent modeling (asking the player to manually co-tag every weapon in a whole category), not a table-entry extraction — so ~46 features (every `WeaponTraining5`-paired feature) defaulted to `subsystem` for this reason alone. **Before running another class**, decide whether to (a) accept this gap permanently for group-scoped bonuses, or (b) build the "weapon-training group" plumbing properly (a real `build.*` selection + a documented tag convention) — it would unlock a meaningful chunk of fighter's archetype surface immediately if built. **FIXED (2026-07-06), see the "Batch-extraction wave prep" section below** — option (b) was built: `attack.weapon.<group>`/`damage.weapon.<group>` now also match a weapon's vendored, normalized `WeaponRef.weaponGroups` (`packages/engine/src/weapon-groups.ts`), in addition to the free-text tag.
2. **Conditional `Change`s on the `dr`/`eres` targets are unsafe** — unlike `ac`/`skill.*` (always-rendered totals a zero-value component just disappears into), `defenses.ts` only materializes the whole "Defenses" stat-group when at least one dr/resistance/sr entry exists at all, so a conditional dr formula that evaluates to 0 still creates a component and (if it's the only one) a spurious "DR/— 0" seal. Found via Warlord's Sun-Bronzed Skin, which was extracted, hand-computed, and then deliberately dropped back to `situational` once this rendering behavior was caught by its own fixture test. **Avoid conditional Changes on `dr`/`eres` targets** in future waves; `ac`/`skill`/speed targets don't have this problem. **FIXED (2026-07-06), see the "Batch-extraction wave prep" section below** — `defenses.ts`'s `groupByQualifier`/`computeSr` now drop any qualifier whose winning value isn't positive, so a conditional dr/eres/spellResist Change that evaluates to 0 is safe to author; future waves no longer need to avoid this shape.
3. **Atomic (single-formula) base-feature grants create the `blocked` bucket.** Any base class feature whose whole multi-tier/multi-level progression is ONE `Change` (Armor Training, Bonus Feats (FGT)) will produce partial-instance "blocked" cases wherever an archetype's prose talks about replacing just one tier/level of it. Worth checking up front, per class, which base features are atomic like this before classifying — it's a fast way to pre-populate a chunk of the `blocked` bucket without reading every feature's full prose first.
4. **Blind extraction wasn't fully possible in this pilot** — the task's own "read first" list included `archetype-effects.ts` (to learn the entry shape and honesty bar), which happens to already contain the 3 fighter ground-truth entries. This agent's extraction pass for those 3 archetypes' OTHER features (not just the already-known entries) still turned up nothing beyond what the hand table has, so the comparison is at least evidence of consistency, but a truly blind pass would need a reviewer who hasn't read the seed table. **Future waves: have the classification/extraction pass done by an agent that is explicitly NOT shown the target class's existing hand-authored entries**, if any exist, before diffing.

**Precision measurement (the point of the pilot):** re-deriving Weapon Master (Armor Training reflavor), Archer (Hawkeye's Perception half), and Two-Handed Fighter (Overhand Chop, notes-only) from prose alone reproduced the hand table's entries exactly — same targets, same formulas, same situational calls on every other feature of those 3 archetypes. Zero wrong-number or wrong-target misses; zero cases where hand-authoring caught something extraction missed, or vice versa, within the ground-truth set. (Caveat: see finding 4 above — this isn't a fully blind result.) Across the other 64 fighter archetypes, 17 new `numeric` entries were extracted (7 `high` confidence, 9 `medium`, one entry — Sun-Bronzed Skin — extracted and then correctly self-rejected per finding 2), 5 of which were independently spot-verified against aonprd.com and matched the vendored prose and this agent's formulas exactly (noted in what is now `archetype-extracted/fighter.ts`'s classification table — see the restructure below — as "spot-verified against aonprd.com," still machine-tier in code — promotion mechanics are a future decision, per the issue). 39 features were classified `blocked` (a real, if narrow, composition-safety bucket this pilot needed to invent, not previously documented outside the Ironskin Monk case). The remaining 325 features split `situational`/`subsystem` via a disclosed, heuristic-assisted pass (documented in that file's own header) rather than individual hand-verification — a deliberate scoping choice, since that boundary doesn't affect engine correctness (neither bucket emits a `Change`).

**Recommendation:** proceed to batch waves for other classes, but fix finding 1 (weapon-group tagging) or explicitly accept it as a standing limitation before starting, since it's the single largest source of "real number, not extracted" in this pilot. Also worth deciding, before scaling: whether the `situational`/`subsystem` heuristic split needs individual hand-verification for classes with fewer archetypes than fighter's 67 (smaller classes can afford the exhaustive pass this one couldn't).

### Batch-extraction wave prep (issue #45, 2026-07-06)

Prep work for the batch waves, per the pilot's own recommendation: fixed both process-doc-blocking findings, then restructured the pipeline's artifacts so parallel per-class wave agents can't conflict on the same file.

**1. Weapon-group targeting fix (finding 1).** `attack.weapon.<group>`/`damage.weapon.<group>` (`computeWeaponAttacks` in `compute.ts`) now match a weapon on TWO independent paths, unioned and deduplicated so a coincidental overlap never double-applies:

- `WeaponInstance.group` — the existing free-text, per-weapon tag (Weapon Focus/Specialization's mechanism), matched by exact string equality, unchanged.
- `WeaponInstance.weaponGroups` — new, an array of Foundry's semantic weapon-category tags, snapshotted (normalized) from `WeaponRef.weaponGroups` at pick-time in `addWeaponFromRef` (`apps/web/src/model/doc.ts`). Hand-entered custom weapons have none and keep working via `group` alone.

New `packages/engine/src/weapon-groups.ts`: `WEAPON_GROUPS` (the 15 groups actually present in `weapons.json` today — axes, blades-heavy, blades-light, bows, close, crossbows, double, firearms, flails, hammers, monk, polearms, spears, thrown, tribal — re-enumerate with `jq -r '.[] | .weaponGroups[]?' packages/data-pipeline/data/weapons.json | sort -u` if the vendored data ever changes) and `normalizeWeaponGroup` (camelCase → kebab-case slug, e.g. `"bladesHeavy"` → `"blades-heavy"` — the convention any future `attack.weapon.<group>`/`damage.weapon.<group>` target's `<group>` suffix must use). **Extraction waves: validate every group name you emit against `WEAPON_GROUPS`/`isKnownWeaponGroup`** — an unrecognized slug silently never matches any weapon.

**2. The dr-at-0 wart fix (finding 2).** `defenses.ts`'s `groupByQualifier` (dr/eres) and `computeSr` (spell resistance) now drop any qualifier whose winning value isn't positive, instead of materializing a zero-value `DefenseEntry`/`Defenses.sr`. **Conditional `dr`/`eres`/`spellResist` Changes are now safe to author** — the earlier guidance to avoid them is retracted. Regression tests in `defenses.test.ts` reproduce the exact Sun-Bronzed Skin shape (armored → no Defenses section at all; unarmored → DR 5/— shows correctly; a real DR source on the same qualifier isn't suppressed by a coincident zero entry). Sun-Bronzed Skin itself was promoted from `situational` back to `numeric` and extracted (see `archetype-extracted/fighter.ts`) now that the underlying wart is fixed.

**3. Per-class file restructure**, so N wave agents can work in parallel without touching each other's files:

- `packages/engine/src/archetype-extracted/<class-tag>.ts` — one file per class, owning BOTH of that class's pipeline artifacts: `<CLASS>_ARCHETYPE_EFFECTS_EXTRACTED` (machine-extracted `Change`-shaped effects) and `<CLASS>_ARCHETYPE_FEATURE_CLASSIFICATION` (the full per-feature audit, every feature of every vendored archetype for that class). Both are `Readonly<Record<string, ...>>` keyed by the feature's own `RefEntity.id`. `packages/engine/src/archetype-classification.ts` and `archetype-effects-extracted.ts` (the pilot's flat, fighter-only files) were retired into `archetype-extracted/fighter.ts` in this shape.
- `packages/engine/src/archetype-extracted/types.ts` — shared types (`ExtractedArchetypeFeatureEffect`, `ExtractionConfidence`, `ArchetypeFeatureClassificationEntry`, `ArchetypeFeatureClassificationBucket`) and the `c()` `Change`-literal helper. No class file should ever need to touch this file, and no class file imports another class's file.
- `packages/engine/src/archetype-extracted/index.ts` — the aggregator: merges every class's two tables into the flat `ARCHETYPE_FEATURE_EFFECTS_EXTRACTED`/`ARCHETYPE_FEATURE_CLASSIFICATION` that `archetype-effects-resolve.ts` (effects) and the engine barrel (classification) already expose. **A wave agent adding class `X` creates exactly one new file (`archetype-extracted/x.ts`) and adds exactly one import line + two object-spread lines to this file** — nothing else in the pipeline changes, and key collisions across classes can't happen because every key is prefixed with its own class tag (`"<classTag>:<archetypeSlug>:<featureSlug>:<level>"`).

`archetype-effects.ts` (the hand-verified table, issue #7) and `archetype-effects-resolve.ts` (the precedence module) were NOT split per-class — they're either owner-maintained or a small fixed module with no per-class growth, so there's nothing to conflict on.

**4. Fighter weapon training group choices, built.** The deferred item (originally logged 2026-07-02) is done: `build.weaponTrainingGroups?: string[]` (one `WEAPON_GROUPS` slug per tier — 5th/9th/13th/17th), `tables.ts`'s `weaponTrainingBonus(level, tierIndex)` (clean-room from the published progression — `1 + floor((level - grantLevel) / 4)`, the same shape every archetype reflavor below already uses), `collect.ts` deriving the resulting `attack.weapon.<group>`/`damage.weapon.<group>` Changes for any fighter level, and a small `WeaponTrainingPicker.tsx` (one `<select>` per unlocked tier, mirroring `CursePicker`/`MysteryPicker`).

Composition finding, worth carrying into every future class that gets this treatment: **the generic paired-swap check (`pairedBaseFeatureUuid`/`activeArchetypeSwaps`) is unreliable for suppressing a NEWLY-modeled base feature.** It works fine for Armor Training/Damage Reduction (`barbarianDamageReductionReplaced`) because every archetype pairing to those features genuinely does replace the mechanic. For Weapon Training, Foundry's vendored data pairs nearly EVERY fighter archetype's "Weapon Training"-slot feature to the base uuid — including several that are byte-identical unmodified reflavors (Aerial Assaulter, Pack Mule, Rondelero Duelist, Two-Weapon Warrior — Foundry's compendium apparently marks a feature-slot as "reflavored" per archetype regardless of whether the text actually changed) and one that's purely additive (Warlord adds one more selectable group without restricting the normal free choice). Using the generic check as the suppression signal would have incorrectly zeroed out Weapon Training entirely for fighters with any of those archetypes. Fixed with a hand-curated `WEAPON_TRAINING_REPLACEMENTS` set in `archetypes.ts` (`weaponTrainingReplaced`) built from the same prose-reading pass as the reclassification below, rather than trusting the vendored pairing generically. **Before wiring a similarly "atomic, previously-empty-changes" base feature for another class, check whether its archetype pairings are this loose before reusing the generic swap check.**

**5. Fighter re-classification after the weapon-group fix.** All ~41 fighter archetype features the pilot had bucketed `situational`/`subsystem` under the generic placeholder note "weapon-group-scoped attack/damage bonus (or unrelated ability grant) — see weapon-group-tagging process note" (most were never individually read — a disclosed heuristic shortcut at the time) were re-read against the vendored prose. Results, now reflected in `archetype-extracted/fighter.ts`'s classification table with individually-reasoned notes replacing the placeholder:

- **10 promoted to `numeric` and extracted** (`FIGHTER_ARCHETYPE_EFFECTS_EXTRACTED`): Archer (Expert Archer, bows), Crossbowman (Crossbow Expert, crossbows), Dragoon (Spear Training, spears, capped +4/+8), Foehammer (hammers), Polearm Master (Polearm Training, BOTH spears and polearms — a rare weapon vendored in both, e.g. Tiger Fork, would double-count; not fixed, flagged), Tribal Fighter (Tribal Weapon Training, tribal), Unarmed Fighter (monk group only — "natural" isn't a vendored `WEAPON_GROUPS` entry, so that half of the archetype's own bonus stays unmodeled, same "model only the modelable half" posture as Hawkeye), Ustalavic Duelist (Duelist Training, light blades, capped +4), Brawler (Close Combatant, close weapons, capped +5/+7 — see the mispaired-uuid finding below), Spear Fighter (partial: only the guaranteed 5th-level spears pick; the archetype's own free-choice 9th/13th/17th picks are a real, if narrow, gap — `weaponTrainingReplaced` suppresses the generic picker for this archetype too, to avoid double-counting the 5th-level tier, so those tiers aren't modelable through either path today).
- **8 reclassified `subsystem` → `situational`** (the pilot's blind heuristic had defaulted them to `subsystem`, but each is actually a REAL number scoped to something the engine can't check — same honesty bar as everywhere else, not a weapon-group problem after all): Dirty Fighter (Maneuver Training, dirty-trick-specific CMB/CMD), Free Hand Fighter (Singleton, hand-state), Mobile Fighter (Leaping Attack, move-then-attack action), Opportunist (Alchemical Onslaught, a weapon category with no `WEAPON_GROUPS` entry), Shielded Fighter (shield-bash-specific), Steelbound Fighter (Steelbound Awakening, tied to one unrecorded specific weapon), Swarm Fighter (Share Space, positioning-state), Titan Fighter (Unstoppable Momentum, oversized-weapon state).
- **The remaining ~23 confirmed genuinely non-extractable** on individual reading — either entirely unrelated grants (Aerobatics' Fly-for-Acrobatics swap, bonus spellcasting, bonus feats, rerolls, rage, activated buffs/stances with no engine toggle mechanism, magic-item enhancement boosts) or scoped by something outside `WEAPON_GROUPS`'s vocabulary (Aquanaut's damage-TYPE scoping, Savage Warrior's "natural weapons" with no fallback group to model). Four of these (Aerial Assaulter, Pack Mule, Rondelero Duelist, Two-Weapon Warrior) turned out to be byte-identical, unmodified Weapon Training reflavors — not reflavors at all — so they get no per-archetype entry and are simply modeled generically by the new base picker like any other fighter. Warlord's "adds Barsoomian as an option" and Cavern Sniper's "choose bow OR crossbow" both stay unmodeled for the same reason base Weapon Training's own free choice does: no build-side field records which option was actually taken.
- **New finding, not fixed here:** Brawler's Close Combatant feature's vendored `pairedBaseFeatureUuid` resolves to ARMOR Training's uuid, not Weapon Training's, despite its own prose saying "replaces weapon training 1 and 2." This is a vendored-data mispairing (both uuids exist in the compendium; the archetype-features CSV linked the wrong one) — sidestepped for Weapon Training's suppression via the hand-curated `WEAPON_TRAINING_REPLACEMENTS` set (`fighter:brawler` is in it directly, not derived from the pairing), but it means the EXISTING generic Armor Training swap-suppression (`activeArchetypeSwaps`) likely incorrectly zeroes out a Brawler character's Armor Training too, a pre-existing bug unrelated to this pass. Flagged for a future fix (would need a similar hand-corrected exclusion applied on the Armor Training side, mirroring `AMBIGUOUS_DR_REPLACEMENTS`'s shape); not fixed here to stay scoped to weapon-group targeting.
- **Also found, out of scope for this pass:** Armor Master's "Armored Defense" (DR by armor type — light/medium/heavy) was mis-tagged by the pilot's blind heuristic as weapon-group-scoped; it isn't. It's a real, clean, `@armor.type`-checkable number (same shape as the hand-verified table's Savage Barbarian precedent) that a future non-weapon-group-focused audit pass could extract — noted in its classification entry rather than silently left mis-described, but not extracted here (outside this pass's weapon-group scope).

---

### Batch-extraction waves complete — full archetype coverage (issue #45, 2026-07-06)

Six parallel wave agents (rogue / druid+monk / bard+sorcerer / ranger+arcanist / paladin+oracle / barbarian+wizard+magus) finished the per-feature classification of **every vendored archetype feature across all 13 archetype-bearing classes** (fighter was the pilot). Final tallies from `ARCHETYPE_FEATURE_CLASSIFICATION` (2,460 features total):

| class     | features  | numeric | situational | subsystem | blocked |
| --------- | --------- | ------- | ----------- | --------- | ------- |
| barbarian | 149       | 12      | 37          | 92        | 8       |
| bard      | 347       | 17      | 35          | 286       | 9       |
| druid     | 360       | 25      | 47          | 288       | 0       |
| fighter   | 383       | 29      | 119         | 196       | 39      |
| magus     | 150       | 8       | 16          | 124       | 2       |
| monk      | 60        | 1       | 5           | 52        | 2       |
| oracle    | 79        | 4       | 7           | 67        | 1       |
| paladin   | 247       | 15      | 61          | 166       | 5       |
| ranger    | 266       | 16      | 78          | 169       | 3       |
| rogue     | 241       | 24      | 49          | 165       | 3       |
| sorcerer  | 36        | 2       | 2           | 30        | 2       |
| wizard    | 108       | 1       | 8           | 79        | 20      |
| arcanist  | 34        | 0       | 0           | 34        | 0       |
| **all**   | **2,460** | **154** | **464**     | **1,748** | **94**  |

135 of the 154 numeric features are machine-extracted (`ARCHETYPE_FEATURE_EFFECTS_EXTRACTED`); the rest are hand-verified `archetype-effects.ts` entries, which always win via `archetype-effects-resolve.ts`.

**Cross-wave process findings** (each wave's per-class file carries the per-feature details):

- **Check base-feature pairing reality per class first.** Rogue/druid base features carry NO vendored changes (nothing to double-count → tiny/zero blocked buckets); wizard has NO `pairedBaseFeatureUuid` anywhere in the class (every swap of the atomic Bonus Feats formula → blocked; one cause, not 20 separate bugs). The fighter pilot's atomic-partial-tier trap is real but class-specific.
- **Same-archetype evolving numbers**: when a later feature upgrades an earlier feature's own number (Aquatic Druid's Natural Swimmer 3rd → Seaborn 9th swim speed), fold both into ONE conditional formula — two untyped Changes would sum and over-grant.
- **Mispaired-additive vendored swaps are a live-bug category**: a purely additive feature paired to a base uuid suppresses the whole base feature with nothing backfilled. `MISPAIRED_ADDITIVE_FEATURES` in `archetypes.ts` (Sable Company Marine's Hippogriff Companion — fixed 2026-07-06) is the exclusion list for that shape; `WEAPON_TRAINING_REPLACEMENTS` handles the inverse (over-eager pairing of unmodified reflavors). Brawler's wrong-uuid pairing remains issue #46.
- **Engine gaps that cost otherwise-clean extractions** (future target/mechanism candidates): archetype-authored `bonusSkillRanks` is silently inert (`apps/web` skill budget only reads it off races); `chaSkills`-style group targets remain UNAPPLIED; no activated-performance-buff mechanism exists (bard — the Archaeologist's Luck precedent); weapon range increments, `critConfirm`, `reach`, and natural/unarmed-attack targets don't exist; `resources.ts` hardcodes `classTag === "cleric"` for `channelEnergyDetail`, so a paladin's Channel Positive Energy never gets a computed detail line.
- **Choice-menu features can't be auto-applied even with clean numbers**: oracle "replaces your Nth-level revelation" features stay `subsystem` because revelations are an untracked player choice; only guaranteed capstones (Final Revelation) were extractable. A Magus Arcana picker and an Oracle Revelations picker (arcanist-exploits pattern) would each unlock a batch of currently-`subsystem` entries.

---

### Feat classification + extraction pass (issue #45, 2026-07-06)

Applied the same batch-extraction pipeline shape to **feats** — `packages/data-pipeline/data/feats.json` carries **zero** vendored `changes[]` (confirmed: 0/390), so every feat's mechanical effect (if any) is either hand-authored in `feat-effects.ts` (10 static/choice entries + 7 situational + 6 pool, pre-existing) or was previously unmodeled. All 390 feats — the entire upstream pack, no more exist — were classified; the extractable subset was extracted.

**New files** (`packages/engine/src/`):

- `feat-classification.ts` — `FEAT_CLASSIFICATION`, every feat keyed by name slug (`featNameSlug`, not RefEntity id — feat ids are opaque Foundry UUIDs; see feat-effects.ts's own rationale), with a bucket and one-line reason. Buckets adapted from the archetype rubric: `numeric` / `choice-numeric` (new, for feats parameterized by a player choice) / `situational` / `pool` (new, for Extra-X-style resource-pool-max feats) / `subsystem` / `blocked`.
- `feat-effects-extracted.ts` — `FEAT_EFFECTS_EXTRACTED`, the machine-extracted sibling of `feat-effects.ts`'s hand-verified `FEAT_EFFECTS`, same `confidence`/`provenance` shape as `archetype-extracted/`.
- `feat-effects-resolve.ts` — `resolveFeatEffect(slug)`, the single precedence point (hand wins over extracted), mirroring `archetype-effects-resolve.ts` exactly. `collect.ts` and `apps/web/src/model/feats.ts`'s `featChoiceDescriptor` both resolve through it now instead of reading `FEAT_EFFECTS` directly, so a machine-extracted choice-numeric feat (Greater Weapon Focus) gets the same `featChoices` picker UI as its hand-verified counterpart (Weapon Focus) for free.

**Tallies:**

| bucket         | count |
| -------------- | ----- |
| numeric        | 17    |
| choice-numeric | 6     |
| situational    | 148   |
| pool           | 7     |
| subsystem      | 204   |
| blocked        | 8     |
| **all**        | 390   |

13 of the 17 numeric + 3 of the 6 choice-numeric + 1 of the 7 pool entries are newly machine-extracted this pass (the rest are the pre-existing hand-verified entries in `feat-effects.ts`, reclassified here for completeness):

- **Skill-pair family** (numeric, high confidence): Acrobatic, Athletic, Deceitful, Animal Affinity, Deft Hands, Magical Aptitude, Persuasive, Self-Sufficient, Stealthy — all unconditional CRB "+2/+2 on a skill pair, +4 at 10+ ranks in either" feats, structurally identical to the already-hand-verified Alertness entry. Intimidating Prowess (add Str mod to Intimidate, unconditional) rounds out the numeric set.
- **Choice-numeric** (reusing the existing `featChoices` "weapon"/"skill" picker, per the task's own constraint — extract only when the choice-UI shape already fits): Greater Weapon Focus / Greater Weapon Specialization (literal "+1/+2 more, stacks" extensions of Weapon Focus/Specialization, same `attack.weapon.<group>`/`damage.weapon.<group>` targets), and Master Craftsman (partial — only the flat +2 chosen-skill bonus is extracted; the caster-level-substitution-for-item-creation clause has no engine equivalent and is deliberately left unmodeled, hence `medium` confidence vs. the others' `high`).
- **Pool**: Extra Arcane Pool (+2 to the magus's Arcane Pool max, stacks) — found by auditing every "Extra X" feat (34 total) against which `classFeatures` tags actually carry a `uses.maxFormula` pool; only `arcanePool` was previously unwired among them (the rest — grit, panache, bane, mercy, mental focus, martial flexibility, mesmerist tricks, ninja tricks, rogue/slayer talents, discoveries, rage powers, revelations, magus arcana, investigator talent, focus power, phrenic amplification, gnome spell-like uses, item slots, shapechange features, nanite surges — carry no `uses.maxFormula`-based pool in the vendored data at all, so raising their "max" has no engine hook without new resources.ts machinery; classified `subsystem`, not guessed at).

**Deliberate downgrades to `blocked`** (real number, but stacking-suspect or no engine target exists at all — the task's own guidance to prefer downgrading over a wrong sheet number): Improved Natural Armor (RAW stacks per additional take, but the engine's `nac`→`"natural"` AC type is a non-stacking bucket — two same-type Changes wouldn't sum the way multiple takes require); Improved Natural Attack (no per-natural-attack damage-die field, and which attack was chosen isn't tracked); Ability Focus (choice of special attack + no DC target); Spell Focus / Greater Spell Focus (per-school spell save DC — no such target exists anywhere in `targets.ts`); Spell Penetration / Greater Spell Penetration (caster-level-vs-SR check bonus — same target gap); Breadth of Experience (applies to the entire Knowledge skill group + Profession — no group-level skill target, only per-skill `skill.<id>`).

**Methodology** (disclosed, matching the archetype pilot's own posture): every `numeric`/`choice-numeric`/`pool`/`blocked` entry was individually read against the vendored feat description and cross-checked against the published rules. The bulk `situational`/`subsystem` split for the remaining ~332 feats (11 Monster-tag, 9 racial-tag, 7 Teamwork-tag, 26 Signature Skill / Skill Unlock-tag, ~27 non-pool "Extra X", the 22 maneuver-scoped Improved/Greater combat-maneuver feats, 12 Critical-tag, 10 Metamagic-tag, 25 Item Creation-tag, and a general regex-assisted pass for a "+N"/"-N" pattern in the remaining prose) was a heuristic-assisted pass, same disclosed trade-off as `archetype-extracted/fighter.ts`'s pilot — this boundary doesn't affect engine correctness since neither bucket ever emits a `Change`.

**Notable judgment calls**: Combat Expertise (attack-for-AC tradeoff, same shape as Power Attack/Deadly Aim) was left `situational` rather than wired into `SITUATIONAL_FEAT_EFFECTS`, since that map's `SituationalFeatEffect` shape has no AC-delta field today — flagged as a small, cheap follow-up rather than built here. Critical Focus's `+4` confirm-roll bonus was classified `situational` alongside the rest of the Critical-tag family (matching traits.ts's Anatomist precedent: `critConfirm` is a real, unapplied target, but confirm rolls aren't a running sheet total) rather than given its own carve-out. A Magus Arcana picker and an Oracle Revelations picker would each unlock further "Extra X" pool feats in a future wave, same finding the archetype pass already surfaced.

**Tests**: `packages/engine/test/feat-effects-extracted.test.ts` (hand-computed fixtures for the skill-pair family, Intimidating Prowess, Greater Weapon Focus/Specialization stacking, Master Craftsman, Extra Arcane Pool including multi-take stacking, and the hand-vs-extracted precedence rule) and `packages/engine/test/feat-classification.test.ts` (completeness — every vendored feat has exactly one classification entry — plus bucket-validity and spot-check assertions).

### Pathfinder Unchained classes mechanical audit (issue #13 step 2, 2026-07-07)

Step 1 (vendoring `barbarianUnchained`/`monkUnchained`/`rogueUnchained`/`summonerUnchained`, plus 9 other alternate/occult classes) landed separately; this pass is step 2 for the four Unchained classes only. Much of the numeric surface rides the existing generic pipelines for free, confirmed by fixture tests rather than assumed — several of these classes share vendored `featureId`s with their chained counterparts, so the same `changes[]`/`uses.maxFormula`/`grantsBuffs` Changes apply automatically once the class tag is on the character.

**Barbarian (Unchained)**: BAB/saves/HD identical tiers to chained barbarian. Rage is a genuinely different buff — confirmed via the vendored data itself: "Rage (UC)" grants a DISTINCT buff ("Rage (Unchained)", not the chained "Rage"), whose `changes[]` already carry the correct Unchained numbers (+2, scaling to +3 at 11th/+4 at 20th, on melee attack/melee damage/thrown damage/Will; -2 AC; no Str/Con score change at all) — this needed zero hand-authoring, just a fixture test (`packages/engine/test/barbarian-unchained.test.ts`) proving the numbers and that the buff toggle is wired through the generic resource-pool `linkedBuffIds` mechanism. Damage Reduction shares its vendored featureId with chained barbarian's own (`RENIeTVjWB7Mq6Mw`) and reuses the identical `barbarianDamageReduction` table — extended `defenses.ts`'s `barbarianLevel()` and `archetypes.ts`'s `barbarianDamageReductionReplaced()` to sum `barbarian` + `barbarianUnchained` levels (a character would only ever have one, but summing is correct regardless of which), and extended `AMBIGUOUS_DR_REPLACEMENTS` with barbarianUnchained's own separate "Invulnerable Rager" archetype id (`barbarianUnchained:invulnerable-rager` — a distinct `RefData.archetypes` entry from chained's, confirmed to have the same ambiguous/unpaired "Invulnerability" swap at level 2). Danger Sense (shared featureId with Rogue Unchained's own) carries an empty vendored `changes: []` — same unmodeled "vs. traps only" posture chained rogue/barbarian's own Trap Sense already has (`archetype-extracted/rogue.ts`/`barbarian.ts`), not a regression. Rage Powers / Greater Rage / Tireless Rage / Mighty Rage are prose-only (`changes: []`) — deferred, same posture as chained Rage Powers (already logged above).

**Monk (Unchained)**: FULL BAB with good Fort/Ref but POOR Will (unlike chained monk's medium BAB/good-all-three) — comes straight off the vendored class def's own tiers, no hand-authoring; fixture test proves both the BAB and the save pattern. Unarmed Strike and AC Bonus (Wis-to-AC) share vendored featureIds with chained monk (`a4SPdPuOFdmfJdHN`/`4zVndIJ8NMWWzl1T`) and apply through the exact same generic pipelines already exercised by the chained-monk fixtures in `compute.test.ts` — `unarmedDamageDie`'s detail-keying in `archetypes.ts` was extended with a `classTag === "monkUnchained"` branch (the classFeatures-list `detail` field is keyed by classTag+name, so the shared featureId alone doesn't cover the unchained tag). Flurry of Blows is a genuine rewrite (a bonus attack at the monk's TRUE base attack bonus — no -2 penalty, no "BAB = monk level" swap-in — stacking to a 2nd bonus attack at 11th), hand-authored as `flurryOfBlowsUnchainedLabel` in `tables.ts`, distinct from chained's `flurryOfBlowsLabel`. Ki Pool arrives at 3rd level (not 4th like chained) but is the same Wis-based formula and rides the fully generic `uses.maxFormula` resource-pool pipeline with zero hand-authoring. Style Strikes ALSO carries a real vendored `uses.maxFormula` (`ceil(@class.level / 14)` per round) and derives its own resource pool for free — the use-COUNT is trackable; which style strikes were chosen (their individual effects) is a prose-only, choice-bearing subsystem and is deferred, same posture as Ki powers (also prose-only, `changes: []`).

**Rogue (Unchained)**: BAB/saves/HD identical tiers to chained rogue. Sneak Attack (UC)'s dice progression is identical to chained rogue's `sneakAttackDice` (same table, extended `archetypes.ts`'s detail-keying to match this class's distinctly-named vendored feature, "Sneak Attack (UC)"). Finesse Training (UC) grants Weapon Finesse as a fixed feat at 1st level — same "bonusFeats change with a mismatched feature name" shape Monk's Unarmed Strike already needed a `FEATURE_NAME_OVERRIDES` entry for (`apps/web/src/model/feats.ts`); added `"finesse training (uc)": "weapon finesse"`. The Dex-to-damage-with-one-chosen-weapon-group half of Finesse Training (3rd/11th/19th level) is **deferred**: reusing the fighter Weapon Training-style `build.weaponTrainingGroups` picker infrastructure isn't a cheap fit here (that system feeds a flat `attack.weapon.<group>`/`damage.weapon.<group>` Change; swapping Str-for-Dex specifically in weapon damage math is a different code path entirely, not a bonus stack), so this is left prose-only rather than half-built. Danger Sense — see Barbarian (Unchained) above (shared featureId, same unmodeled posture as chained Trap Sense). Debilitating Injury, Rogue Talents, and Advanced Talents (ROG) are prose-only, choice-bearing subsystems — deferred, same posture as chained Rogue Talents (already logged above).

**Summoner (Unchained)**: BAB/saves/HD identical tiers to base summoner. Its "Spells per Day"/"Spells Known" tables are numerically IDENTICAL to the base summoner's (verified against aonprd.com's "Summoner (Unchained)" class page) — added `SUMMONER_UNCHAINED_SPELLS_PER_DAY`/`_KNOWN` as aliases of the existing `SUMMONER_SPELLS_PER_DAY`/`_KNOWN` in `tables.ts` (same posture as `DRUID_SPELLS_PER_DAY = WIZARD_SPELLS_PER_DAY`) and a `summonerUnchained` entry in `CASTER_MODELS`; also added to `casterLevel.ts`'s `FULL_CASTER_TAGS` and `compute.ts`'s `ARCANE_CASTER_TAGS` (Cha-based arcane spontaneous caster, same as base summoner). What DOES differ is the SPELL LIST itself — already vendored as its own 352-spell `refData.spellLists.summonerUnchained` slice, not a re-export of the base summoner's list — confirmed with a concrete example (`Haste`/`Slow` sit at 2nd level on the base summoner list but 3rd level on the Unchained list). Eidolon, Life Link, Bond Senses, Aspect, Merge Forms, Twin Eidolon, etc. remain a separate, deferred subsystem — same posture as the base summoner's own eidolon (already deferred), not a new gap introduced here.

**Vendored-data bugs found** (for issue #47):

1. **Rogue's Edge (UC) carries a spurious `bonusFeats` Change.** Its vendored `changes[]` entry (`floor(@class.unlevel / 5)`, target `bonusFeats`) reads as "gains a bonus feat at 5th/10th/15th/20th level," but the published ability (gained at those exact levels) grants "skill unlock powers" for a chosen skill — nothing about feats at all. Left unaddressed, this would inflate `expectedFeatCount`'s free bonus-feat-slot budget by +1/+2/+3/+4 at those levels. Fixed by excluding this specific feature by name in `classBonusFeatSlots` (`apps/web/src/model/feats.ts`), same shape as the pre-existing fixed-grant exclusion just above it. Skill unlock powers themselves remain an unmodeled, deferred subsystem either way.
2. **Rage (Unchained)'s vendored buff description mentions temporary hit points that aren't in its `changes[]`.** The buff's own description text ("You also gain 2 temporary hit points per Hit Die...") and the separate Greater Rage (UC)/Mighty Rage (UC) class-feature descriptions (which describe the temp-HP amount scaling to 3/HD at 11th and 4/HD at 20th) all agree this is real PF1 Unchained RAW (confirmed independently against d20pfsrd.com's "Unchained Barbarian" page) — NOT a vendoring error, and NOT the "no temp HP" claim this task's own brief made (that brief was wrong on this specific point; the vendored data and the published rules agree with each other, just not with the brief). Left unmodeled here regardless: the engine has no temporary-hit-points concept anywhere (`live.hp` has no `tempFromBuff`-style field, no expiry-on-rest-vs-expiry-on-combat-end distinction) — building one is a cross-cutting HP-tracking feature, not a one-line fix, and out of scope for this pass.

**Tests**: `packages/engine/test/barbarian-unchained.test.ts`, `packages/engine/test/monk-unchained.test.ts`, `packages/engine/test/rogue-unchained.test.ts`, `packages/engine/test/summoner-unchained.test.ts` (hand-computed BAB/save/HP/resource-pool/classFeature fixtures, scoped by classTag per the "name collisions broke a prior wave's test" lesson), plus additions to `apps/web/test/feats.test.ts` (Finesse Training grant + Rogue's Edge (UC) exclusion) and `apps/web/test/spellcasting.test.ts` (`CASTER_MODELS.summonerUnchained`).

---

### Antipaladin, Ninja, Samurai mechanical audit (13-class expansion follow-through, 2026-07-07)

Engine/model support for the three ALTERNATE classes (APG antipaladin, UC ninja, UC samurai) added alongside the 13-class data vendoring pass. All three needed almost no hand-authoring — a fixture-first audit (`packages/engine/test/antipaladin.test.ts`, `ninja.test.ts`, `samurai.test.ts`) confirmed most of each class's numeric features already ride the generic `changes[]`/`uses.maxFormula` pipelines:

**Antipaladin** (mirror of paladin): high BAB, good Fort/Will, d10, prepared divine quarter-caster. `tables.ts`'s `PROGRESSIONS.antipaladin = PALADIN_RANGER_SPELLS_PER_DAY` (verbatim vendored confirmation: "His base daily spell allotment is the same as that of a paladin"); `CASTER_MODELS.antipaladin` in `apps/web/src/model/spellcasting.ts` copies paladin's shape (cha-based, `preparesFromClassList: true`, no cantrips). Left OUT of `casterLevel.ts`'s `FULL_CASTER_TAGS` and `compute.ts`'s `ARCANE_CASTER_TAGS`, mirroring paladin's own (pre-existing) exclusions exactly — same posture, not a new gap. Added a `CLASS_ALIGNMENT_RESTRICTIONS.antipaladin` entry ("Any evil", the mirror of paladin's "Lawful good") to `apps/web/src/model/alignment.ts`. Mechanically:

- **Unholy Resilience** (Divine Grace's antipaladin twin) is a real vendored `Change` (`max(0, @abilities.cha.mod)` -> `allSavingThrows`) — fully generic, zero hand-authoring.
- **Smite Good** carries `uses.maxFormula` byte-identical to Smite Evil's, but (like Smite Evil) no vendored `actions[]` for its attack/damage/AC line — hand-authored `smiteGoodLabel` in `tables.ts` (reuses `smiteEvilDetail`'s math, swaps the "vs. evil"/"vs. good" suffix), wired into `resources.ts`'s pool `detail` (`feature.tag === "smiteGood"`) and `archetypes.ts`'s classFeature `detail` (name+classTag), both mirroring the existing Smite Evil/paladin branches.
- **Touch of Corruption** (Lay on Hands' twin) and **Channel Negative Energy** (`uses.source: "touchOfCorruption"`) both carry real vendored `actions[].damage.formula` dice — the existing generic `actionBasedDetail`/linked-features pass derives "melee touch · Nd6 negative · Channel Negative Energy: Nd6 (DC NN Will)" with zero hand-authoring, same pipeline that already serves paladin's Lay on Hands/Channel Positive Energy.
- Spell-lists.json anomaly (issue #47 candidate): the vendored `antipaladin` spell list carries a stray `"6"` key holding one level-7 spell ("Plague Bearer") — antipaladin's own progression table only ever exposes levels 1–4, so `accessibleSpellLevels`-gated UI never reads that key; confirmed benign (no code fix needed), logged for the data audit.

**Deferred** (prose-only, choice-bearing, no vendored numeric formula — same posture as Rogue Talents/Barbarian Rage Powers): Cruelty (mirrors paladin's Mercy), Fiendish Boon (mirrors Divine Bond), and the Aura family (Evil/Cowardice/Despair/Vengeance/Sin/Depravity) beyond their bare presence in the class-feature list.

**Ninja** (rogue chassis): BAB medium, poor Fort/Will, good Ref, d8, no trapfinding/trap sense (confirmed absent from the vendored feature list). Sneak Attack uses the identical rogue progression — `archetypes.ts`'s name+classTag dispatch extended from `classTag === "rogue"` to `(classTag === "rogue" || classTag === "ninja")`, reusing `sneakAttackDice` wholesale (no new table). Ki Pool (NIN) is genuinely Cha-based per the vendored `uses.maxFormula` (`floor(@class.unlevel / 2) + @abilities.cha.mod`) — NOT Wis like the monk's — fully generic, confirmed by fixture rather than assumed.

**Deferred**: Ninja Tricks / Master Tricks (the talent-menu subsystem, same posture as Rogue Talents — no schema field, no picker).

**Samurai** (cavalier chassis): BAB high, good Fort, poor Ref/Will, d10. Resolve (`ceil(@class.unlevel / 2)`/day) and Honorable Stand (`1 + if(gte(@class.unlevel, 16), 1)`/day) both ride the generic `uses.maxFormula` pipeline with zero hand-authoring; Bonus Feat (SAM) carries the identical vendored `bonusFeats` Change as cavalier's Bonus Feat (CAV).

**Vendored-data bug found (issue #47 candidate)**: **Challenge (SAM) carries NO `uses` block at all**, unlike cavalier's byte-identical Challenge (CAV) (`uses.maxFormula: "1 + floor((@class.unlevel - 1) / 3)"`, same SRD wording verbatim, including the "to a maximum of seven times per day at 19th level" clause). Per this project's established posture for a vendored gap on an otherwise-plain numeric feature (Shifter Aspect's precedent — left as-is, not synthesized), this is documented rather than hand-authored; `samurai.test.ts` asserts the current (missing) pool explicitly so a future data-pipeline fix is caught, not silently masked.

Mount (SAM) / Order (SAM) are deferred exactly like cavalier's own Mount/Order (neither is modeled at all for cavalier either — no `build.cavalierOrder`/companion-source wiring exists for cavalier, so samurai gets the identical non-treatment, not a new gap).

**Also found**: RAW, the "Extra Lay On Hands" feat additionally grants antipaladin's Touch of Corruption +2 uses/day ("For example, the Extra Lay On Hands feat grants an antipaladin 2 additional uses of the touch of corruption class feature" — vendored description, verbatim), but `feat-effects.ts`'s `FEAT_POOL_EFFECTS["extra-lay-on-hands"]` only targets featureTag `"layOnHands"` (paladin's), not `"touchOfCorruption"` — `FeatPoolEffect` has no multi-target shape today. Left unfixed (narrow edge case, would need a small type extension); noted here as a follow-up rather than built.

**Files touched**: `packages/engine/src/tables.ts` (antipaladin spell progression alias, `smiteGoodLabel`), `packages/engine/src/index.ts` (export), `packages/engine/src/archetypes.ts` (ninja Sneak Attack + antipaladin Smite Good dispatch), `packages/engine/src/resources.ts` (antipaladin Smite Good pool detail), `apps/web/src/model/spellcasting.ts` (`CASTER_MODELS.antipaladin`), `apps/web/src/model/alignment.ts` (`CLASS_ALIGNMENT_RESTRICTIONS.antipaladin`). New tests: `packages/engine/test/{antipaladin,ninja,samurai}.test.ts`, plus antipaladin coverage appended to `apps/web/test/spellcasting.test.ts` and `apps/web/test/alignment.test.ts`. No `compute.ts`/`casterLevel.ts` changes needed (verified, not assumed) — none of the three classes is a full/arcane caster or diverges from paladin's existing (pre-existing-gap) CL exclusion.

---

### Mesmerist, Occultist, Spiritualist mechanical audit (OCCULT classes, 13-class expansion follow-through, 2026-07-07)

Engine/model support for the three Occult Adventures 6-level PSYCHIC casters. All three are spontaneous casters whose Spells per Day tables (and, for mesmerist/spiritualist, Spells Known tables) are numerically IDENTICAL to the bard's — verified directly against aonprd.com's live class pages, all 20 rows, both tables where published — so `tables.ts` aliases the bard tables (`MESMERIST_SPELLS_PER_DAY = BARD_SPELLS_PER_DAY`, etc.) rather than duplicating them, same posture as `oracle: sorcerer`/inquisitor-summoner-skald/hunter above. Psychic magic is NOT arcane — none of the three are in `compute.ts`'s `ARCANE_CASTER_TAGS` (no arcane spell failure); all three ARE plain members of `casterLevel.ts`'s `FULL_CASTER_TAGS` (CL = class level from 1st, no divergence, verified against aonprd.com — same posture as warpriest/hunter).

**Mesmerist** (Cha): a fixture-first audit turned up more free mechanics than expected —

- **Consummate Liar** carries a real vendored `changes[]` (`max(1, floor(@class.unlevel/2))` untyped bonus to `skill.blf`) — fully generic, zero hand-authoring, confirmed by fixture (`sheet.skills.blf.total`).
- **Mesmerist Tricks** carries `uses.maxFormula: "max(1, floor(@class.unlevel/2)) + @abilities.cha.mod"` — a real resource pool, rides `deriveResourcePools` generically. (Which specific tricks are implanted — the trick menu itself — is a deferred, prose-only, choice-bearing subsystem, same posture as Rogue Talents.)
- **Towering Ego** (L2) carries `changes[]: @abilities.cha.mod -> will` — an untyped Will-save bonus, fully generic; this surfaced unprompted in the fixture (a mesmerist's Will save is base + Wis mod + Cha mod, not just + Wis mod as initially assumed) and is now locked in by `occultAdventuresCasters.test.ts`.
- **Painful Stare** and **Hypnotic Stare** both carry empty `changes[]`/`uses` upstream (no fixed "target" to apply a vendored Change to) — hand-authored `painfulStareBonus`/`painfulStareLabel` (`max(1, floor(level/2))` dmg) and `hypnoticStarePenalty`/`hypnoticStareLabel` (-2 Will, -3 at 8th) in `tables.ts`, wired into `archetypes.ts`'s classFeature `detail` dispatch (name+classTag, same shape as Smite Evil/Smite Good). Both are intentionally display-only summaries (contextNote-style) — no live per-target modifier, since this app doesn't model per-encounter stare targeting.
- **Mesmerist Tricks** the picker (which tricks are implanted) is deferred; touch of hypnotic bonds/triggers is prose-only.

**Occultist** (Int): modeled as a spontaneous caster per the task brief. **Mental Focus** carries `uses.maxFormula: "@class.unlevel + @abilities.int.mod"` — a real resource pool, fully generic, zero hand-authoring. Occultist is the one exception to every other spontaneous `CasterModel` in this registry: it has NO `knownProgression` at all, because — confirmed directly against aonprd.com — the SRD publishes no generic "Spells Known" table for this class; an occultist's known spells are instead granted entirely by her implement schools (one spell per accessible spell level per school selected). **Implements/Resonant Powers/Focus Powers are deferred**: the vendored pipeline cache does carry the 8 `*-resonant-school` class-abilities entries (`evocation-resonant-school.yaml`, `divination-resonant-school.yaml`, etc.) mentioned in the task brief, but they are NOT referenced from the Occultist class's own `links.supplements` (confirmed by id cross-check against the raw YAML) — they're standalone entries with no data-driven link back to the class, so wiring an implement-school picker + resonant/focus powers is a real subsystem build (hand-curated school list + spell-grant + focus-power activation), not a quick win. Deferred, same posture as the summoner's eidolon; noted here for a future follow-up issue.

**Spiritualist** (**Wis**, not Cha — the one divergence from the mesmerist/occultist Cha/Int pattern, confirmed against aonprd.com's spellcasting requirement text). **Phantom** (the spiritualist's eidolon-like companion) is deferred — same posture as the summoner's eidolon; the engine's familiar/animal-companion infrastructure (`packages/engine/src/familiar.ts`, `companion.ts`) is the likely future implementation path, not built now.

**Files touched**: `packages/engine/src/tables.ts` (`MESMERIST_SPELLS_PER_DAY`/`OCCULTIST_SPELLS_PER_DAY`/`SPIRITUALIST_SPELLS_PER_DAY` bard-table aliases, `MESMERIST_SPELLS_KNOWN`/`SPIRITUALIST_SPELLS_KNOWN` bard-table aliases, `painfulStareBonus`/`painfulStareLabel`/`hypnoticStarePenalty`/`hypnoticStareLabel`), `packages/engine/src/index.ts` (exports), `packages/engine/src/archetypes.ts` (mesmerist Painful/Hypnotic Stare classFeature `detail` dispatch), `apps/web/src/model/spellcasting.ts` (`CASTER_MODELS.mesmerist`/`.occultist`/`.spiritualist`), `apps/web/src/model/casterLevel.ts` (`FULL_CASTER_TAGS` addition). New tests: `packages/engine/test/occultAdventuresCasters.test.ts` (BAB/saves/Painful-Hypnotic-Stare/Mental-Focus/Mesmerist-Tricks fixtures against real `loadRefData()`), plus mesmerist/occultist/spiritualist coverage appended to `packages/engine/test/spellsPerDay.test.ts`, `apps/web/test/spellcasting.test.ts` (including a "class appears in the builder's caster flow" check via `casterClassesOf`), and `apps/web/test/casterLevel.test.ts`. No `compute.ts` changes needed (verified, not assumed) — psychic magic deliberately stays out of `ARCANE_CASTER_TAGS`.

### Psychic, Medium, Kineticist mechanical audit (13-class expansion follow-through, 2026-07-07)

Engine/model support for the three OCCULT ADVENTURES classes (psychic, medium, kineticist) added alongside the 13-class data vendoring pass, mirroring the antipaladin/ninja/samurai wave's fixture-first posture (`packages/engine/test/psychicDisciplines.test.ts`, `mediumSpells.test.ts`, `kineticistBurn.test.ts`, `apps/web/test/occultCasters.test.ts`, `doc.psychicDiscipline.test.ts`).

**Psychic** (int-based spontaneous FULL 9-level caster): low BAB, good Will, d6. **Added to `casterLevel.ts`'s `FULL_CASTER_TAGS`** (the known per-class-wave gotcha — arcanist shipped with CL displaying 0 for exactly this reason; regression-tested at CL 1/7/20 in `occultCasters.test.ts`). Deliberately NOT added to `compute.ts`'s `ARCANE_CASTER_TAGS` — psychic magic is neither arcane nor divine, no arcane spell failure from armor. Spells per day AND spells known are both numerically identical to the sorcerer's tables (verified against the raw "Table: Psychic"/"Table: Psychic Spells Known" on legacy.aonprd.com, all 20 levels — this contradicts the folk assumption that psychics know more spells than sorcerers; checked twice), so `tables.ts` gets `PSYCHIC_SPELLS_PER_DAY = SORCERER_SPELLS_PER_DAY` and `CASTER_MODELS.psychic` reuses `knownProgression: "sorcerer"`. Mechanically:

- **Psychic Disciplines** — the same vendoring shape as oracle mysteries: the vendored pack ships 26 standalone `*-discipline.*.yaml` ClassFeature entries NOT linked from the class def (they don't reach `RefData.classFeatures`), so `packages/engine/src/psychic-disciplines.ts` hand-authors the structured parts (bonus-spell ids copied verbatim from each entry's `@UUID[...]` prose references — all 108 verified present in `RefData.spells` — plus the "Phrenic Pool Ability: Wisdom|Charisma" line), scoped to the 12 core Occult Adventures disciplines (Abomination, Dream, Enlightenment, Faith, Ferocity, Haunted, Lore, Pageantry, Pain, Rebirth, Self-Perfection, Tranquility; 3rd-party ones like Mindtech/Bleaching/Hag-Called skipped, matching `oracle-mysteries.ts`'s scope note). New `build.psychicDiscipline` schema field + `setPsychicDiscipline` doc transition + `DisciplinePicker.tsx` (MysteryPicker's twin, wired psychic-only into ClassesSection); `disciplineSpellsKnown` in `model/spellcasting.ts` (own helper — the unlock cadence is psychic level 1, 4, 6, ..., 18, different from both bloodlines' `2L+1` and mysteries' every-2-from-2nd), surfaced read-only/cap-exempt in `SpellsSection` and merged castable into `PreparedSpellsPanel`'s known list (filed under the spell's own castable level via `levelMap`, not the unlock level).
- **Phrenic Pool** rides the vendored `uses.maxFormula` (`floor(@class.unlevel / 2) + @abilities.cha.mod`) — but that formula **hardcodes Charisma regardless of discipline** (vendored-data simplification; issue #47 candidate — RAW the ability is discipline-determined, and 6 of the 12 core disciplines use Wisdom). `resources.ts` corrects it: when the chosen discipline is Wisdom-based, the phrenic-pool formula (only) is evaluated with the cleric-house-rule's scoped cha→wis alias. No discipline chosen keeps the vendored formula.
- **Deferred** (prose-only, choice-bearing — same posture as Oracle Revelations/Arcanist Exploits at launch): **Phrenic Amplifications** (the psychic's own pick-menu at 1st, 3rd, +4/level) and **discipline powers** (each discipline's 1st/5th/13th-level abilities); Detect Thoughts/Telepathic Bond/Telepathy etc. remain bare feature-list rows.

**Medium** (cha-based spontaneous 4-level caster): medium BAB, good Will, d8. `MEDIUM_SPELLS_PER_DAY`/`MEDIUM_SPELLS_KNOWN` hand-typed clean-room from the raw "Table: Medium"/"Table: Medium Spells Known" (legacy.aonprd.com, quoted verbatim — L4 1/-/-/-, L13 3/2/1/1, L20 4/4/3/2); `CASTER_MODELS.medium` is a bard-shaped spontaneous model with its own progression keys. Kept OUT of `FULL_CASTER_TAGS` (casts nothing before 4th level — the documented bloodrager posture; a flat classLevel would wrongly show CL 1-3).

- **Deferred — spirit machinery** (the class's defining subsystem): spirits are chosen at each day's SEANCE — a live/daily choice, not a build choice — so the oracle-mystery picker pattern (a `build.*` field) doesn't transplant cheaply; an honest live-state picker (`live.*` field + seance/rest wiring) is follow-up scope. Note for that follow-up: the vendored pack DOES ship the six legendary spirit entries (`archmage-spirit`/`champion-spirit`/`guardian-spirit`/`hierophant-spirit`/`marshal-spirit`/`trickster-spirit.*.yaml`) plus spirit-power entries as standalone class-abilities YAMLs — the same "not linked from the class def, hand-author from the cached YAMLs" shape `psychic-disciplines.ts` just used (pinned by a `mediumSpells.test.ts` assertion so a pipeline change is caught). Spirit Bonus/Spirit Surge/Shared Seance/Taboo/Influence are all deferred with it (Spirit Bonus's vendored `changes[]` is empty — nothing numeric to wire without knowing the channeled spirit anyway).

**Kineticist** (con-based, casts NO spells — mostly a subsystem class, shipped honestly): medium BAB, good Fort/Ref, d8, light armor. No spell list vendored (correct — it doesn't cast), no `CASTER_MODELS` entry, not a caster tag anywhere; `kineticistBurn.test.ts` pins all three exclusions.

- **Burn** rides in FREE as a live resource pool from the vendored `uses.maxFormula: "3 + @abilities.con.mod"` (`per: "charges"`) — checked against the real data before any hand-authoring, per the wave's audit rule. The nonlethal rule ("1 point of nonlethal per character level per point of burn, unhealable until a full night's rest") has no vendored hook — hand-authored as the pool's `detail` line (`burnDetailLabel` in `tables.ts`, wired via `resources.ts`'s `feature.tag === "burn"` branch, using TOTAL character level per RAW and the level-tiered per-round acceptance cap via `burnPerRoundLimit`). Deliberately does NOT auto-apply the nonlethal damage to `live.hp.nonlethal` — accepting burn is a table-time choice with unmodeled riders (gather power, forced burn); the note tells the player what to apply.
- **Kinetic Blast** is a display detail, not an attack builder (the monk unarmed-strike-die pattern): the vendored Physical/Energy Kinetic Blast features carry real dice-bearing action formulas (`(ceil(@class.unlevel / 2))d6 + ...` including an elemental-overflow `min(@resources.burn.value, ...)` rider), but dice terms don't numerically evaluate in this engine — `kineticBlastDetail` in `tables.ts` (clean-room: physical `ceil(L/2)d6 + ceil(L/2) + Con`, energy `ceil(L/2)d6 + Con/2`, touch) supplies the classFeatures `detail` via `archetypes.ts`'s name+classTag dispatch, Con-resolved when abilities are available.
- **Deferred — the big one** (needs its own follow-up issue): **elements / wild talents / infusions / elemental defense / elemental overflow** — the element choice (`Elemental Focus`, vendored with empty `changes[]`) gates everything (which blast, which defense, which talent menu), and none of it is Change-shaped upstream. Also deferred: gather power, internal buffer, metakinesis.

**Vendored-data bugs found (issue #47 candidates)**: (1) Phrenic Pool's `uses.maxFormula` hardcodes `@abilities.cha.mod` — RAW it's discipline-determined (Wis for 6 of the 12 core disciplines); corrected engine-side in `resources.ts` rather than editing vendored data. (2) The per-discipline/per-spirit class-ability YAMLs come through the transform as nothing at all (not linked via the class def's `links`, so they never reach `RefData.classFeatures`) — same known shape as oracle mysteries/curses, worked around by hand-authoring, noted in case the pipeline ever grows a standalone-classFeat pass.

**Files touched**: `packages/schema/src/character.ts` (`build.psychicDiscipline`), `packages/engine/src/tables.ts` (psychic/medium progressions + kineticist display helpers, appended), `packages/engine/src/psychic-disciplines.ts` (new), `packages/engine/src/resources.ts` (phrenic-pool wis alias + burn detail), `packages/engine/src/archetypes.ts` (kinetic blast details), `packages/engine/src/index.ts` (exports), `apps/web/src/model/casterLevel.ts` (psychic in `FULL_CASTER_TAGS`), `apps/web/src/model/spellcasting.ts` (`CASTER_MODELS.psychic`/`.medium`, `disciplineSpellsKnown`), `apps/web/src/model/doc.ts` (`setPsychicDiscipline`), `apps/web/src/components/builder/DisciplinePicker.tsx` (new), `ClassesSection.tsx` (wiring), `SpellsSection.tsx` + `tracker/PreparedSpellsPanel.tsx` (discipline bonus spells). New tests: `packages/engine/test/{psychicDisciplines,mediumSpells,kineticistBurn}.test.ts`, `apps/web/test/{occultCasters,doc.psychicDiscipline}.test.ts`.

---

### Witch hexes/patron + alchemist discoveries/mutagen (issue #65, 2026-07-08)

Built the deferred choice-bearing subsystems for the two classes flagged at the "Witch + Shaman mechanical audit" entry above (`hexes, the patron, and the familiar-link mechanics themselves ... are out of scope / deferred`) and the "Alchemist + investigator mechanical audit" entry (`Discoveries ... — no engine table, no schema field, no picker`), following the exact oracle-revelations/oracle-mysteries/magus-arcana pattern (clean-room engine table + `build.*` field + thin picker + hand-computed fixture tests). Sources: aonprd.com's book-scoped legacy PRD mirror pages (`legacy.aonprd.com`), which let content be split by exact source book — used throughout to keep scope honestly APG/UM-core rather than pulling in the modern aggregated AoN pages' later-splatbook content.

**Witch — Patron** (`build.witchPatron`, `packages/engine/src/witch-patrons.ts`): single-tag pick at L1, never revisited (`setWitchPatron` in `doc.ts`, mirrors `setOracleMystery` exactly). 17 APG/Ultimate Magic core patrons with verified, complete 9-spell bonus lists (witch level 2, 4, ..., 18) — Agility, Animals, Deception, Elements, Endurance, Healing (UM), Light (UM), Moon (UM), Plague, Strength, Transformation, Trickery, Water, Wisdom, Shadow, Time (UM), Vengeance (UM). Explicitly excluded and noted why: "Protection" (a real patron, but from Heroes of the High Court, not APG/UM) and "Wards"/"Portals" (not real PF1 patrons at all — checked against AoN's full 52-patron index). Unlike `oracle-mysteries.ts`'s bonus spells (real vendored Foundry `_id`s lifted from the mystery's own `@UUID[...]` prose), a patron's bonus-spell list has **no vendored linkage whatsoever** (the Witch class def only links a generic "Patron Spells" stub feature) — so `WitchPatronBonusSpell` carries only a spell NAME, resolved against `RefData.spells` at runtime by case-insensitive name match (`apps/web/src/model/spellcasting.ts`'s new `patronSpellsKnown`, with a `WeakMap`-cached name index), degrading to a synthetic `patron:<slug>` id when a name doesn't resolve (a few of the highest-level patron spells sit outside the pipeline's current slice) — same graceful-degradation posture `mysterySpellsKnown` already documents for its own id fallback. Wired into `SpellsSection.tsx` (new `PatronSpellsBlock`, read-only/cap-exempt reference) and `PreparedSpellsPanel.tsx` (merged into the castable known list, filed under the spell's own resolved level like the psychic-discipline branch, not the unlock level).

**Witch — Hexes** (`build.witchHexes`, `packages/engine/src/witch-hexes.ts`): flat picker (NOT patron-scoped — a patron only grants bonus spells; every hex is available to every witch), gained at 1st level and every even level thereafter (`model/witchHexes.ts`: `1 + floor(level/2)`, plus one per "Extra Hex" feat occurrence — same convention as `expectedMagusArcanaCount`). 27 APG-core hexes: 14 regular (1st+), 8 Major (10th+, soft-gated via `minLevel`), 5 Grand (18th+) — Ultimate Magic's further 21 hexes are out of scope, noted for a follow-up. Hex save DC (`10 + 1/2 witch level + Int mod`, confirmed as a blanket class-level rule, not per-hex) lives in `tables.ts` `witchHexDC`. Classification: audited every hex against the oracle-revelations honesty bar and found none clear it for a real `Change` — Cauldron's flat +4 Craft (Alchemy) bonus targets a player-named parameterized skill with no guaranteed matching entry; Flight's only truly passive component is feather-fall/swim (the fly-speed pieces are limited daily activations, not a permanent speed); Ward is a static ally buff but targets an ally the witch chooses at activation, not herself. So every hex is `displayOnly: true` with `changes: []`, DC/duration/nested-choice spelled out in `contextNotes`. `HexPicker.tsx` mirrors `MagusArcanaPicker` (flat, searchable, soft `minLevel` annotation, tier badge).

**Alchemist — Discoveries** (`build.alchemistDiscoveries`, `packages/engine/src/alchemist-discoveries.ts`): flat picker, gained at 2nd level and every even level thereafter (`model/alchemistDiscoveries.ts`: `floor(level/2)`, plus one per "Extra Discovery" feat occurrence). 41 discoveries: the full 29-entry APG core list, plus 10 Ultimate Magic entries (the Cognatogen line, Vestigial Arm, Preserve Organs, Tumor Familiar, Wings, Spontaneous Healing, Healing Touch, Lingering Spirit) and 2 Ultimate Combat entries (Nauseating Flesh, Poison Conversion) worth covering for their honest mechanical shape; the remaining ~35 UM/UC discoveries (mostly more bomb-type riders, Alchemical Simulacrum/Zombie, Bottled Ooze, ...) are out of scope, noted for a follow-up. Every entry is `displayOnly: true` — bomb-type riders are mutually exclusive per RAW (noted in `contextNotes`, not enforced), and the handful of near-misses (Feral Mutagen's extra natural attacks, Precise Bombs' splash exclusion, Preserve Organs' crit-negation percentage) have no safe Change target in this engine, same honesty-bar call as the witch hexes above. `DiscoveryPicker.tsx` mirrors `MagusArcanaPicker` exactly.

**Alchemist — Mutagen** (base class feature, NOT a discovery — no schema field needed): audited `resources.ts`'s existing `resolveGrantsBuffs` pipeline BEFORE hand-authoring anything, per the task brief, and found the vendored Mutagen `ClassFeature` (`uses.maxFormula: "1"`, `per: "charges"`) already carries `grantsBuffs` pointing at 3 real vendored buffs — "Mutagen, Str"/"Mutagen, Dex"/"Mutagen, Con", each RAW-correct (+4 to the physical score, -2 to the linked mental score, +2 natural armor, `10 * @item.level` minute duration) — confirmed by a scratch script running `deriveResourcePools` against a synthetic alchemist doc before writing any code. So Mutagen **already surfaces as a toggleable resource pool with all 3 linked buffs today, with zero new code** — the tracker's `ResourcesPanel`/`LinkedBuffToggle` (the exact "Rage toggle" mechanism named in the task brief) renders one activate/deactivate button per physical-ability variant, letting the player pick which stat to boost at activation time exactly as instructed. Pinned with a new fixture test (`resources.test.ts`) so a future data/pipeline change can't silently regress it. **Cognatogen** (Ultimate Magic discovery) is confirmed RAW to share the exact same +4/-2/+2-natural-armor shape, boosting a mental score instead — but there is no vendored buff for it at all (checked `buffs.json` — zero "Cognatogen" hits), and reproducing Mutagen's toggle would mean inventing a wholly new discovery→buff linking mechanism that doesn't exist today; deferred with the exact numbers spelled out in its discovery's `contextNotes` rather than promising a toggle that isn't there, per the task's own "if cheap, else defer with a note" instruction.

**Alchemist — Bombs**: the vendored Bomb `action.damage` formula is a flat, non-scaling `"1d6"` — confirmed against the pinned data slice it does NOT encode APG's "+1d6 every odd alchemist level" scaling or the flat Intelligence-modifier addend, even though the resource pool (`uses.maxFormula: "@class.unlevel + @abilities.int.mod"`, correct) and the save DC (`10 + floor(@class.unlevel/2) + @abilities.int.mod`, correct) both already ride the vendored formula fine. Added `bombDamageDetail` to `tables.ts` (clean-room: `1 + floor((level-1)/2)` d6 + flat Int-mod addend) and a `feature.tag === "bomb" && classTag === "alchemist"` override branch in `resources.ts`, same posture as the existing Smite Evil/Burn hand-authored overrides — the resource-pool row now shows e.g. `3d6+3 fire (DC 15 Ref)` for a level-5 alchemist instead of the vendored-but-wrong flat `1d6`.

**Schema fields added** (`packages/schema/src/character.ts`, appended after `psychicDiscipline`, no restructuring): `build.witchPatron?: string`, `build.witchHexes?: string[]`, `build.alchemistDiscoveries?: string[]`. `GrantedFeature.origin.kind` (both `archetypes.ts` and `character.ts`'s `DerivedClassFeature.origin`) extended with `"hex"` and `"discovery"`.

**Files touched**: `packages/schema/src/character.ts` (3 new `build.*` fields + `origin.kind` union), `packages/engine/src/witch-patrons.ts` (new), `packages/engine/src/witch-hexes.ts` (new), `packages/engine/src/alchemist-discoveries.ts` (new), `packages/engine/src/tables.ts` (`witchHexDC`, `bombDamageDetail`, appended), `packages/engine/src/resources.ts` (bomb detail override), `packages/engine/src/archetypes.ts` (hex/discovery grant blocks + `origin.kind`), `packages/engine/src/collect.ts` (hex/discovery modifier-collection blocks, both currently no-ops per the all-`displayOnly` posture), `packages/engine/src/index.ts` (exports), `apps/web/src/model/doc.ts` (`setWitchPatron`), `apps/web/src/model/witchHexes.ts` (new), `apps/web/src/model/alchemistDiscoveries.ts` (new), `apps/web/src/model/spellcasting.ts` (`patronSpellsKnown` + name-index resolver), `apps/web/src/components/builder/{PatronPicker,HexPicker,DiscoveryPicker}.tsx` (new), `ClassesSection.tsx` (wiring), `SpellsSection.tsx` (`PatronSpellsBlock`) + `tracker/PreparedSpellsPanel.tsx` (patron bonus spells merge). New tests: `packages/engine/test/{witchHexes,witchPatrons,alchemistDiscoveries,bombDamage}.test.ts` plus new cases in `resources.test.ts` (Mutagen linked buffs, Bomb damage override).

### Shaman spirits/hexes + Hunter animal focus/companion (issue #65, 2026-07-08)

Deferred-choice subsystems for the two remaining classes IMPLEMENTATION_PLAN.md's Witch + Shaman audit and the ACG 13-class vendoring pass had left as documented omissions: shaman's Spirit Magic + spirit-specific hexes, and hunter's Animal Focus + companion wiring.

**Shaman spirit (`packages/engine/src/shaman-spirits.ts`, `build.shamanSpirit`, `SpiritPicker.tsx` — mirrors `oracle-mysteries.ts`/`MysteryPicker.tsx` closely, the oracle's structural analogue per the vendored `spirit.yaml`'s own text: "If the shaman takes levels in another class that grants a mystery... the spirit and mystery must match")**. Scope: the 8 Advanced Class Guide "core" spirits (Battle, Bones, Flame, Heavens, Life, Nature, Stone, Waves — verified against aonprd.com's Spirits-Shaman index; the vendored pack ships several more from later splatbooks — Ancestors, Dark Tapestry, Frost, Lore, Mammoth, Slums, Tribe, Wind, Wood — out of scope, same posture as `oracle-mysteries.ts`). Each spirit's own vendored YAML (`packs/class-abilities/shaman-spirits/*.yaml` — individually vendored but NOT linked from the Shaman class def, same "hand-author from the cached-but-unlinked YAML" shape as oracle mysteries/psychic disciplines) carries a **Spirit Magic spell list that is NOT byte-identical to the matching oracle mystery's bonus-spell list** — verified during authoring (e.g. the Waves spirit's 1st spirit-magic spell is Hydraulic Push, not the Waves mystery's Touch of the Sea) — so it's hand-copied from the shaman-specific YAML rather than reused from `ORACLE_MYSTERIES`. Unlike a mystery's `level` field (an ORACLE class-level threshold), a `ShamanSpiritMagicSpell.level` IS the spell's own level (1st-9th, per the vendored prose's own "1st -", "2nd -", ... labeling) — RAW "she has one spell slot per day of each shaman spell level she can cast," so availability gates on `accessibleSpellLevels(CASTER_MODELS.shaman, shamanLevel)` rather than a fixed unlock-level table (`shamanSpiritSpellsKnown` in `apps/web/src/model/spellcasting.ts`, wired into both `SpellsSection.tsx` (builder reference list) and `tracker/PreparedSpellsPanel.tsx`'s prepare-from-class-list merge, since shaman is `preparesFromClassList: true` and the spirit-magic spells are not otherwise guaranteed to already be on the base shaman list). **Honest limitation, called out in `CASTER_MODELS.shaman`'s `learnGuidance`**: RAW lets a shaman cast ONE of those spirit-magic spells per spell level per day SPONTANEOUSLY without expending a normal prepared slot — that per-level daily-cast bookkeeping is NOT tracked as its own `deriveResourcePools` pool (a single fungible per-day counter would misrepresent 9 independent per-level uses, and this app's resource-pool shape has no per-spell-level breakdown); the spirit-magic list is surfaced as a reminder of which spells are freely swappable, not a live-tracked counter. Each spirit's 1st-level Spirit Ability (e.g. Battle's "Battle Spirit" ally-buff aura, Bones'/Flame'/Stone's/Waves' scaling melee-touch-attack powers, Heavens' "Stardust", Life's "Channel", Nature's "Storm Burst") is note-tier/`displayOnly` — verified against aonprd.com's per-spirit pages — surfaced via `collectGrantedFeatures`/`resolveClassFeatures` (new `origin.kind: "spirit"`) same as oracle revelations; a real resource pool exists for exactly ONE of the eight (Battle's own vendored `battle-spirit-ability.QGEEtV5NqZbomKE6.yaml` carries `uses.maxFormula: "3 + @abilities.cha.mod"`), but wiring a pool for that one spirit and not the other seven would be an inconsistent half-measure, so all eight stay prose-only (Battle's formula is cited in its `summary` text instead).

**Shaman hexes (`build.shamanHexes`, `ShamanHexPicker.tsx` — mirrors `oracle-revelations.ts`/`RevelationPicker.tsx`)**: gained at 2nd level, then 4th, 8th, 10th, 12th, 16th, 18th, and 20th (verified directly against the vendored `hex-sha.PW7CGt2iZsQFeZUI.yaml`'s own text and cross-checked on aonprd.com — **NOT** the "every 4 levels from 2nd" cadence the task brief assumed, and **NOT** shared with witch's own Hex progression, which a sibling agent was building in parallel — deliberately no shared hex module built here, per the task's explicit instruction). Each spirit grants 5 exclusive hexes (40 total across the 8 spirits), hand-authored from aonprd.com's per-spirit pages as note-tier `displayOnly` entries (`shaman-spirits.ts`'s `ShamanSpiritHex` has no `changes[]` field at all — a numeric witch/shaman hex-effects table is a separate, larger undertaking, out of scope here). Budget math (`model/shamanHexes.ts`) mirrors `model/oracleRevelations.ts`'s shape exactly, including the "Extra Hex" stackable-feat count (shared feat between witch and shaman). **Deferred, documented**: Wandering Spirit (4th level — temporarily bond with a different spirit each day) and Wandering Hex (6th level, two hexes/day at 14th — temporarily borrow a hex from that other spirit) are both re-chosen DAILY, not fixed build picks, so neither gets a `build.*` field — same "live/daily choice, no build.\* field" posture IMPLEMENTATION_PLAN.md already documents for the medium's spirits.

**Hunter Animal Focus — a near-total freebie**: before hand-authoring anything, the vendored `animal-focus.4r83TfLHXrRmXtIn.yaml` was checked for an existing `uses.maxFormula` per the task brief's explicit instruction — and unlike shaman spirits/oracle mysteries, Animal Focus (unlike those) actually IS linked directly into the Hunter class def (`level: 1`), with `uses.maxFormula: "@class.unlevel"` (minutes/day = hunter level) AND `links.supplements` resolving to **all 12 vendored "Animal Focus (\<Animal\>)" buffs**, each carrying a correctly-scaling `changes[]` formula keyed on `@item.level` (e.g. Bear: `2 + floor((@item.level - 1) / 7) * 2` enhancement Con — reproduces the CRB's `+2`/`+4`@8/`+6`@15 progression exactly). The existing generic `deriveResourcePools`/`resolveGrantsBuffs` pipeline (built for Rage) already surfaces this as a `linkedBuffIds`-carrying pool with ZERO hunter-specific code — confirmed by a new fixture (`resources.test.ts`, "hunter 5 — Animal Focus pool exposes all 12 Animal Focus buff links for free") — and the tracker's existing `ResourcesPanel`/`LinkedBuffToggle` already renders an activate/deactivate chip for each, exactly like Rage's toggle. Snake's AoO-only attack/AC bonus and Bat's darkvision/Mouse's evasion/Wolf's scent (situational or sense-shaped, not general sheet numbers) are carried as `contextNotes` on their vendored buffs rather than a general `ac`/`attack` Change — verified in the vendored data itself, not a simplification made here. Frog's Acrobatics-to-jump-only bonus is likewise a vendored `contextNotes` entry (only the Swim portion is a real `skill.swm` Change) — the vendored data already draws this "whole skill vs. specific use" line correctly.

**Companion-side Animal Focus (`live.animalCompanion.focusBuffId`, `model/companion.ts` `setCompanionFocus`/`animalFocusBuffs`, `CompanionPanel.tsx`)**: PF1 RAW lets a hunter apply an aspect to her companion independently of her own (can differ, doesn't count against her daily minutes, persists until changed) — modeled as DISPLAY-ONLY bookkeeping (a `<select>` chip naming which of the 12 vendored Animal Focus buffs is active on the companion), per the task's explicit steer, rather than wiring a second buff-routing path alongside `sharedBuffIds` (which assumes the shared buff is also active on the master, not true here). `animalFocusBuffs(refData)` reads the 12 buffs directly by name pattern (`"Animal Focus ("`) rather than a hand-authored table — the numbers are already fully vendored, so duplicating them would only add drift risk.

**Hunter's own Animal Companion — a real gap, found and fixed**: `AnimalCompanionPicker.tsx` previously gated ONLY on druid (Nature Bond) or ranger level ≥4 (Hunter's Bond) — the ACG Hunter class's OWN "Animal Companion (HUN)" class feature (distinct from, and confusingly similarly-named to, the ranger's "Hunter's Bond" — verified against the vendored `animal-companion-hun.V7cGG7vKN0BY2Bhb.yaml`, linked into the Hunter class def at 1st level) had NO wiring at all: a hunter picking a companion source saw no picker and no companion panel, ever. Fixed: new `AnimalCompanionBuild.source` tag `"hunter-companion"` (append-only to the existing union), wired into `companionEffectiveLevel`'s `baseCompanionEffectiveLevel` (hunter level 1:1, **no −3 offset** — "The hunter's effective druid level is equal to her hunter level," unlike the ranger's `hunters-bond` `level − 3, min 0`), `AnimalCompanionPicker.tsx` now shows an "Animal Companion (hunter N)" chip for any hunter level ≥1 (a hunter has a companion from 1st level, unlike a ranger's 4th). The Hunter's own vendored text explicitly endorses stacking with another companion source ("If a character receives an animal companion from more than one source, her effective druid levels stack") — reproduced by the existing additive-sum posture, now fixture-tested for a druid/hunter multiclass. **Precise Companion / teamwork-feat sharing between hunter and companion — deferred**, same "no teamwork-feat modeling anywhere in this app" posture as every other teamwork feat.

**Files touched**: `packages/schema/src/character.ts` (`build.shamanSpirit`/`shamanHexes`, `AnimalCompanionBuild.source` +`"hunter-companion"`, `AnimalCompanionLiveState.focusBuffId`, `DerivedClassFeature.origin.kind` +`"spirit"`/`"hex"`), `packages/engine/src/shaman-spirits.ts` (new), `packages/engine/src/archetypes.ts` (spirit ability + hex grants in `collectGrantedFeatures`), `packages/engine/src/companion.ts` (`hunter-companion` source), `packages/engine/src/index.ts` (exports), `apps/web/src/model/doc.ts` (`setShamanSpirit`), `apps/web/src/model/shamanHexes.ts` (new), `apps/web/src/model/spellcasting.ts` (`shamanSpiritSpellsKnown`, `CASTER_MODELS.shaman` blurb update), `apps/web/src/model/companion.ts` (`hunter-companion` toggle, `setCompanionFocus`/`hunterLevel`/`animalFocusBuffs`), `apps/web/src/components/builder/{SpiritPicker,ShamanHexPicker}.tsx` (new), `ClassesSection.tsx` + `AnimalCompanionPicker.tsx` (wiring), `SpellsSection.tsx` + `tracker/PreparedSpellsPanel.tsx` (spirit magic bonus spells), `tracker/CompanionPanel.tsx` (Animal Focus chip). New tests: `packages/engine/test/shamanSpirits.test.ts`, `packages/engine/test/companion.test.ts` (hunter-companion fixtures, appended), `packages/engine/test/resources.test.ts` (Animal Focus linkedBuffIds fixture, appended), `apps/web/test/shamanHexes.test.ts` (new), `apps/web/test/spellcasting.test.ts` + `apps/web/test/companion.test.ts` (appended).

### Gunslinger/Swashbuckler deeds + Cavalier/Samurai orders (issue #65, 2026-07-08)

Deferred martial subsystems for the two near-twin class pairs. Fixture-first (`packages/engine/test/{deeds,cavalierOrders}.test.ts`, `apps/web/test/doc.cavalierOrder.test.ts`) — first confirmed the premise the task brief asked to verify rather than assume: **grit (gunslinger) and panache (swashbuckler) pools already derive from the generic `uses.maxFormula` resource-pool pipeline** (`resources.ts`) — `Grit`/`Panache` class features carry `uses.maxFormula: "max(1, @abilities.wis.mod)"`/`"max(1, @abilities.cha.mod)"` in the vendored data already; no hand-authored pool logic was added for either.

**Gunslinger/Swashbuckler deeds** (`packages/engine/src/deeds.ts`, new): deeds are NOT choice-bearing (every gunslinger/swashbuckler gets every deed of her level automatically) — so this is a pure reference table + level filter (`deedsForClass`), not a picker, no `doc.build.*` field. One asymmetry worth flagging: gunslinger deeds ARE individually vendored as real `ClassFeature` entries with full prose (already rendered today via `ClassFeaturesList`/`sheet.classFeatures`, no engine work needed for that path) — but swashbuckler deeds are NOT (the class def only links a single generic "Swashbuckler Deeds" stub, same "stub only" shape `oracle-revelations.ts` documents for Oracle's Revelation stub). `deeds.ts` hand-authors a matching table for BOTH classes anyway, for parity and because it adds something the vendored gunslinger prose doesn't carry in structured form: a per-deed `actionType` and `cost` (grit/panache points) as real fields, plus a terse one-line `summary`. New tracker panel `DeedsPanel.tsx` (read-only, no `doc.build.*` write-back — sits next to `ResourcesPanel` in `Tracker.tsx`) renders both tables grouped by level tier, gated on the character having levels in either class.

**Precise Strike** (swashbuckler 3rd level, add swashbuckler level to damage with a light/one-handed piercing melee weapon while panache >= 1) is the one deed with a genuine flat numeric effect. **Route taken: context-note tier, not an automatic Change** — the vendored weapon-group vocabulary (`weapon-groups.ts`'s `WEAPON_GROUPS`: axes, blades-heavy/light, bows, close, crossbows, double, firearms, flails, hammers, monk, polearms, spears, thrown, tribal) is a weapon-FAMILY taxonomy, not a damage-TYPE/encumbrance-category one, and neither `WeaponRef` nor `WeaponInstance` carries a piercing/slashing/bludgeoning field at all (confirmed by reading `refdata.ts`) — "light or one-handed piercing" cuts across several groups with no clean automatic target to attach a `damage.weapon.<group>` Change to. `preciseStrikeBonus(level, doubled?)` computes the live number; `DeedsPanel` shows it inline ("Currently: +7 damage... verify your weapon is light/one-handed piercing... and panache >= 1") rather than folding it into the sheet's attack/damage totals, matching the project's honesty bar for target-scoped effects the engine can't check automatically (same posture as Smite Evil's alignment gate).

**Cavalier/Samurai orders** (`packages/engine/src/cavalier-orders.ts`, new; `build.cavalierOrder` schema field, shared by both classes): like deeds, orders are NOT structured data anywhere in the vendored pack (only generic "Order"/"Order (SAM)" stubs) — hand-authored from AoN, mirroring `oracle-revelations.ts`'s posture. Scope: the six APG orders (Cockatrice, Dragon, Lion, Shield, Star, Sword — verified against AoN's `CavalierOrders.aspx`, matches the task brief's list) plus the samurai's own UC orders (Warrior, Ronin). A single shared `build.cavalierOrder` field (not split per class) because RAW lets a samurai freely pick a cavalier order instead of Warrior/Ronin — `ordersForClass("samurai")` returns all 8, `ordersForClass("cavalier")` the 6. New `setCavalierOrder` doc transition + `OrderPicker.tsx` (mirrors `MysteryPicker`, wired cavalier/samurai into `ClassesSection`), free-choice/soft-warning posture (no edict/alignment hard validation).

Each order grants three things, each modeled per the honesty bar:

- **Order skills** (2 bonus class skills) — display-only. Same documented gap `oracle-mysteries.ts`'s `classSkills` already carries: `compute.ts`'s `classSkillSet` only unions `RefData.classes[].classSkills` + race, with no hook for a sub-choice to add more — not new machinery, the existing gap.
- **Challenge rider** — context-note tier (target-scoped, per the task brief's own call). Verified against AoN that every one of the 8 orders' riders follows the identical "Table: Cavalier's Order" progression (`1 + floor((level - 1) / 4)`, flat +1 per 4 levels) but the bonus TYPE/target differs per order (melee damage vs. AC vs. saves vs. allies' attacks vs. DR, several further scoped to "while threatening"/"while mounted"). `challengeRiderAt(level)` computes the live number, `challengeRiderText(order, level)` substitutes it into the order's template; shown as reference text in `OrderPicker`'s preview, never folded into AC/attack/damage automatically.
- **Order abilities at 2nd/8th/15th** — prose summary only (`displayOnly: true`, `changes: []`), same convention as oracle revelations/arcanist exploits. All 24 (8 orders x 3 tiers) are either narrative grants or self-buffs conditioned on a trigger (demoralize, aid another, charge, a specific save) the engine has no hook for — verified against AoN per-order pages, none is a flat always-on number.

**Samurai's missing Challenge pool — the call made and why**: `Challenge (SAM)` still carries no `uses` block at all in the vendored data (unlike cavalier's byte-identical `Challenge (CAV)`, which has `uses.maxFormula: "1 + floor((@class.unlevel - 1) / 3)"` — same SRD wording verbatim). Per the explicit task instruction to check whether a `supplements.ts` hand-correction is warranted: **left as-is, not synthesized**, following `samurai.test.ts`'s own documented precedent (itself following Shifter Aspect's precedent) that a vendored gap on an otherwise-plain numeric feature is documented and pinned by a test rather than silently patched — `samurai.test.ts`'s existing assertion (`Challenge (SAM) derives NO pool`) needed no change; this wave adds no new information that would flip that call.

**Mount (CAV/SAM)**: deferred entirely (issue #68, companion stat blocks) — one-line note, no engine/schema work, matching cavalier's own pre-existing non-treatment (neither class had Mount modeled before this wave either).

**Files touched**: `packages/schema/src/character.ts` (`build.cavalierOrder`, append-only), `packages/engine/src/deeds.ts` (new), `packages/engine/src/cavalier-orders.ts` (new), `packages/engine/src/index.ts` (exports, append), `apps/web/src/model/doc.ts` (`setCavalierOrder`, append), `apps/web/src/components/builder/OrderPicker.tsx` (new), `apps/web/src/components/builder/ClassesSection.tsx` (wiring), `apps/web/src/components/tracker/DeedsPanel.tsx` (new), `apps/web/src/components/tracker/Tracker.tsx` (wiring), `apps/web/src/styles.css` (`.order-preview`/`.order-abilities`/`.deed-summary`/`.deed-live-number` rules, appended to the existing shared "picker preview box" selectors). New tests: `packages/engine/test/{deeds,cavalierOrders}.test.ts`, `apps/web/test/doc.cavalierOrder.test.ts`.

### Inquisitor Judgments + Skald Raging Song / Inspired Rage (issue #65, 2026-07-08)

Built the deferred activated-ability subsystems for the two remaining classes with no "flagship stance toggle," reusing the generic resource-pool + linked-buff-toggle infrastructure that already powers Rage and bard's Inspire Courage (`packages/engine/src/resources.ts`'s `deriveResourcePools`/`linkedBuffIds`, `apps/web/src/model/buffs.ts`'s `toggleLinkedBuff`, `ResourcesPanel.tsx`'s `LinkedBuffToggle`).

**The gap that motivated new plumbing**: both classes' pool (uses/day or rounds/day) is ALREADY fully and correctly vendored — Inquisitor's "Judgment" feature (`uses.maxFormula: "1 + floor((@class.unlevel - 1) / 3)"`) and Skald's "Raging Song" (`uses.maxFormula: "3 + @abilities.cha.mod + (floor(@class.unlevel - 1) * 2)"`) both verified against aonprd.com to match RAW exactly, zero hand-authoring needed for the pools themselves. But unlike Rage/Inspire Courage/Aura of Protection, the flagship ABILITIES have no vendored buff to resolve `linkedBuffIds` against: Judgment's `grantsBuffs` UUIDs point at `Compendium.pf1.class-abilities.Item.*` entries that never made it into the data pipeline at all (confirmed absent from `class-features.json`), and Inspired Rage carries no `grantsBuffs` at all. So `linkedBuffIds` (which only resolves real `RefData.buffs` entries) had nothing to attach to.

**New engine plumbing** (`packages/engine/src/toggle-buffs.ts`, new): `ToggleBuffOption` — the "no vendored buff exists" counterpart to a linked buff: `{ id, name, changes, contextNotes }`, hand-authored instead of resolved from `RefData.buffs`. `DerivedResourcePool` gains a new `tableOptions?: ToggleBuffOption[]` field (append-only) alongside `linkedBuffIds`, populated in `resources.ts`'s existing per-feature special-casing (same `feature.tag === "X" && classTag === "Y"` shape already used for Smite Evil/Smite Good/Burn) for the Judgment and Raging Song pools specifically. `ActiveBuff` gains a new `effectTag?: string` field (`packages/schema/src/character.ts`, append-only) — the toggle-matching key for a `ToggleBuffOption` (`"judgment:destruction"`, `"ragingSong:inspiredRage"`), used instead of `buffId` (documented as "source buff id from `RefData.buffs`", a contract these hand-authored options don't satisfy). `apps/web/src/model/buffs.ts`'s new `toggleTableBuff` is `toggleLinkedBuff`'s twin, keyed by `effectTag` instead of `buffId`; `ResourcesPanel.tsx`'s new `TableBuffToggle` component mirrors `LinkedBuffToggle`'s rendering/styling exactly (same `res-linked-buff` CSS class), reading `pool.tableOptions` instead of resolving `pool.linkedBuffIds` against `refData.buffs`.

A key simplification available here that vendored buffs like Inspire Courage don't get: since these `changes[]` formulas are hand-authored, they reference `@classes.inquisitor.level` / `@classes.skald.level` directly rather than `@item.level`/`casterLevel` — sidestepping the multiclass caster-level ambiguity `toggleLinkedBuff`'s `casterLevel` parameter (`Math.max(1, sheet.level)`, i.e. TOTAL character level, not class level) has for vendored buffs. `toggleTableBuff` sets no `casterLevel` at all; the buff scales purely off the character's own class levels at compute time, evaluated by `collect.ts`'s active-buffs pass exactly like any other buff.

**Inquisitor Judgments** (`packages/engine/src/judgments.ts`, new) — seven Core/APG judgments, verified against aonprd.com's live Inquisitor page (2026-07-08):

- **Destruction** (+1 sacred weapon damage, `1 + floor(@classes.inquisitor.level / 3)`), **Justice** (+1 sacred attack), **Protection** (+1 sacred AC), **Purity** (+1 sacred all-saves) — the latter three all `1 + floor(@classes.inquisitor.level / 5)` — and **Resiliency** (DR `1 + floor(level/5)`/magic) are all real formula-driven `Change`s, fixture-tested through `compute()` at multiple levels (attack/AC/save/weapon-damage deltas, plus a DR fixture matching `defenses.test.ts`'s exact shape).
- **Resistance** (energy resistance 2 + 2/3 levels against ONE energy type chosen at activation) is `changes: []`, context-note only — same judgment call as the existing `arcanist-exploits.ts` "Energy Shield" entry: applying resistance to all five energy types at once would be a rules error, not a simplification, and this table has no picker for the type choice.
- **Smiting** (weapons count as magic/alignment/adamantine to overcome DR at various levels) has no single numeric target at all — context note only, per task scope.
- Every judgment carries a shared context note that evil inquisitors get profane instead of sacred bonuses (neutral choose one) — noted, not modeled as an alignment-based type switch.
- Judgment count: `maxSimultaneousJudgments(level)` — 1 normally, 2 at 8th (Second Judgment), 3 at 16th (Third Judgment, both automatic class features, verified from the vendored class feature list, not player choices). NOT hard-enforced — all 7 toggles are independently clickable; the pool's `detail` line states the current cap and that swapping isn't automatic, matching the task brief's "at minimum one toggleable at a time with a note" floor.

**Skald Raging Song / Inspired Rage** (`packages/engine/src/raging-song.ts`, new) — verified against aonprd.com's live Skald page (2026-07-08): +2 morale Str, +2 morale Con, +1 morale Will, -1 AC at 1st; Will scales `1 + floor(level/4)` (every 4 levels from 4th); Str/Con scales `2 + 2*floor(level/8)` (jumps at 8th and 16th, not every level) — a single `ToggleBuffOption` (`SKALD_INSPIRED_RAGE`) on the Raging Song pool, fixture-tested at L1/8/16/20. Modeling posture, all note-tier per task scope:

- RAW targets ALLIES, not the skald — this app tracks one character, so the toggle applies Inspired Rage to the skald's OWN sheet, exactly like Rage applies to a barbarian's own sheet (context note explains the simplification).
- The ally skill/ability restriction (no Cha/Dex/Int skill checks except Acrobatics/Fly/Intimidate/Ride, no patience/concentration abilities) is noted, not enforced.
- Song of Marching (3rd), Song of Strength (6th), Dirge of Doom (10th), Song of the Fallen (14th) — the other raging-song performance types — are deferred entirely, not built.
- Rage Powers granted via raging song (3rd+) are deferred with an honest note: the barbarian's own Rage Powers have no picker in this app yet either (pre-existing deferral), so skald inherits the identical non-treatment rather than a bespoke partial one, per the task brief's explicit instruction not to build a rage-power table here.
- No-fatigue-after-song is automatically correct (nothing here applies fatigue in the first place), needs no special-casing.

**Files touched**: `packages/engine/src/toggle-buffs.ts` (new), `packages/engine/src/judgments.ts` (new), `packages/engine/src/raging-song.ts` (new), `packages/engine/src/resources.ts` (`tableOptions` field + Judgment/Raging Song wiring, both additive), `packages/engine/src/index.ts` (exports), `packages/schema/src/character.ts` (`ActiveBuff.effectTag`, additive), `apps/web/src/model/buffs.ts` (`toggleTableBuff`), `apps/web/src/components/tracker/ResourcesPanel.tsx` (`TableBuffToggle` + `tableOptions` wiring). New tests: `packages/engine/test/{judgments,ragingSong}.test.ts` (pool derivation + `compute()` fixtures at multiple levels), `toggleTableBuff` coverage appended to `apps/web/test/buffs.test.ts`.

### Bloodrager bloodlines/bloodrage + Brawler Martial Flexibility (issue #65, 2026-07-08)

Deferred-subsystem build for two ACG/UC classes, following the sorcerer-bloodline (`bloodlines.ts`) and Rage-linked-buff (`resources.ts`/`model/buffs.ts`) patterns.

**`casterLevel.ts` grows a level-gated override shape.** The module's prior binary "classLevel or 0" switch (`FULL_CASTER_TAGS`) couldn't represent "casts nothing before level N, then CL = classLevel with no offset" — the shape both bloodrager AND medium need (medium's gate was already flagged in the file's doc comment; **verified against aonprd.com: medium casts nothing before 4th level too**, same as bloodrager, not from 1st like psychic). Added `LEVEL_GATED_CASTER_TAGS: Record<string, number>` (tag → minimum class level) with `bloodrager: 4` and `medium: 4`; `casterLevelForClass` checks `FULL_CASTER_TAGS` first, then the gated table. `isCasterTag` now also returns true for gated tags. **Engine `rolldata.ts`'s `@cl` needed NO code change**: its doc comment already flags `cl: maxClassLevel` as a "single-class assumption" with no offset applied for ANY class (not even paladin/ranger) — for a single-classed bloodrager/medium this already equals `classLevel` with no `-3`-style offset, matching the new gate's post-gate value exactly, and `BLOODRAGER_SPELLS_PER_DAY`/`MEDIUM_SPELLS_PER_DAY` already return null/zero below the gate so no spell formula ever evaluates `@cl` pre-gate in practice. Both modules verified in sync; regression-tested in `apps/web/test/casterLevel.test.ts` and `occultCasters.test.ts` (updated — it previously asserted medium's CL was flatly 0 at ANY level, which is now correctly 0 only below 4th).

**Bloodrager Bloodrage**: verified it does NOT already work end-to-end like Rage — the vendored "Bloodrage" class feature has a correct `uses.maxFormula` (`4 + @abilities.con.mod + ((@class.unlevel - 1) * 2)`, rounds/day, so the resource pool derives for free) but an EMPTY `grantsBuffs` (unlike barbarian's "Rage" feature, which points at the vendored "Rage" buff). Reusing "Rage" directly would also be numerically wrong (its formulas hardcode `@classes.barbarian.level`). New `packages/engine/src/bloodrage.ts` hand-authors a `BLOODRAGE_BUFF` constant (clean-room from the vendored feature's own description text, confirmed number-for-number identical to core Rage's progression — `+4/+6/+8` Str/Con morale at 1st/11th("Greater Bloodrage")/20th("Mighty Bloodrage"), `+2/+3/+4` Will morale, flat `-2` AC — just keyed to `@classes.bloodrager.level`). Since injecting a synthetic entry into `RefData.buffs` would mean patching two separate loaders (data-pipeline's Node `loadRefData()` engine tests read from, and the browser `refdata/loader.ts` fetch) or a disproportionate `data:build` supplement for one buff, `BLOODRAGE_BUFF` stays OUTSIDE `RefData.buffs` entirely — `resources.ts` special-cases the Bloodrage pool's `linkedBuffIds` to a synthetic id (`BLOODRAGE_BUFF_ID`), and `ResourcesPanel.tsx`'s `LinkedBuffToggle` falls back to a small `SYNTHETIC_LINKED_BUFFS` map when a linked-buff id isn't in `refData.buffs` (`toggleLinkedBuff` already accepts any `Buff`-shaped object, not specifically a `refData`-sourced one, so no change needed there). Fixture-tested in `packages/engine/test/bloodragerBloodline.test.ts`.

**Bloodrager bloodline picker** (`build.bloodragerBloodline` + `build.bloodragerBloodlineVariant`, new `BloodragerBloodlinePicker.tsx`, mirrors `DisciplinePicker.tsx`): all 9 ACG bloodrager bloodlines (Abyssal, Arcane, Celestial, Destined, Draconic, Elemental, Fey, Infernal, Undead — verified via AoN's bloodrager bloodline list, which also carries 15 later-splatbook bloodlines deliberately out of scope, same posture as the sorcerer table's 10-CRB-bloodlines scope). New `packages/engine/src/bloodrager-bloodlines.ts`, hand-authored clean-room from AoN (`BloodragerBloodlineDisplay.aspx?ItemName=<Bloodline>`) since bloodline powers/bonus-feat lists/bonus-spell lists are prose-only upstream (only the base "Bloodrage"/"Bloodrager Bloodline"/"Bloodline Feat (BLO)"/"Bloodline Spells (BLO)" class features are vendored):

- **Power gates are 1st/4th/8th/12th/16th/20th** (verified per-bloodline against every fetched AoN page — NOT the sorcerer's 1st/3rd/9th/15th/20th), no separate "arcana" ability (a bloodrager's 1st-level power IS its first bloodline power).
- **Numeric (`Change`) extraction**: conservative — most bloodrager powers explicitly read "while bloodraging" or "when entering a bloodrage" (conditional on live buff-toggle state this pass doesn't wire per-power) or are X/day activated abilities; those are note-tier (`contextNotes`) or, where RAW gives an explicit day-limit, a `resourcePool` (Destined Strike 3/day, Elemental Strikes 3/day, Hellfire Strike 3→5/day at 12th, Draconic Breath Weapon 1/day, Elemental Form 1/day, Incorporeal Bloodrager 1/day, Defy Death 1/day, One with Nature 3/day). Only powers whose text carries no bloodrage-conditional qualifier became flat `Change`s, same bar `bloodlines.ts` already applies to the SAME-named sorcerer powers for consistency: Abyssal/Celestial's scaling energy resistances, Draconic's +1 natural armor, Destined's scaling luck AC/save bonus (Fated Bloodrager — a genuinely always-on power, unlike its sorcerer namesake's surprise-round-only version), Infernal's scaling fire resistance + flat 20th-capstone acid/cold resistance, Undead's flat cold resistance (Death's Gift). **Disclosed ambiguity** (in the file's doc comment): the primary sourcebook (not available to this session) is the only way to fully resolve whether a few of these RAW-adjacent-but-unqualified "Resistances" powers are genuinely always-on for a bloodrager specifically, vs. gated to bloodrage state like most of the class's kit — flagged rather than silently guessed past.
- **Bonus feats** (`bonusFeatSlugs`, verified per-bloodline from AoN — bloodrager lists differ from the same-named SORCERER bloodline's list, e.g. Draconic bloodrager gets Cleave+Skill Focus(Fly) where sorcerer gets Quicken Spell+generic Skill Focus): wired into the SAME generalized feat-slot machinery sorcerer bloodlines use (`model/featSlots.ts`/`feats.ts`, issue #57) via a new `{ kind: "bloodragerBloodline"; bloodline }` `FeatSlotType` variant — the vendored "Bloodline Feat (BLO)" class feature already supplies the correct slot COUNT (`floor(@class.unlevel / 3) - 1`, vendored `changes`, verified working); this only restricts which feats fill those slots. NOT "note-tier only" as originally scoped — the existing slot-type machinery made full wiring cheap.
- **Bonus spells known** (7th/10th/13th/16th bloodrager level, one spell each — a fixed 4-entry schedule, structurally incompatible with `RefData.bloodlineSpellLists`'s sorcerer-shaped `2*spellLevel+1` cadence): hand-authored NAME-ONLY per bloodline (`BloodragerBonusSpell.name`), resolved against `refData.spells` by exact case-insensitive name match at read time in new `model/spellcasting.bloodragerBonusSpellsKnown` (falls back to a synthetic id, never a crash, on a name that doesn't resolve — same soft-degradation posture as `mysterySpellsKnown`). Display-only preview in the picker for now — not yet spliced into the known-spells list UI the way sorcerer's bloodline spells are (a real gap, not a false claim).

**Brawler Martial Flexibility**: verified the vendored "Martial Flexibility" class feature already carries the correct `uses.maxFormula` (`3 + floor(@class.unlevel / 2)`, matching RAW's "3 + 1/2 brawler level, minimum 1" — the pool derives for free) — but nothing tracked WHICH feat is currently borrowed. New `live.martialFlexibilityFeatId` (schema, append-only) + `setMartialFlexibilityFeat` (`model/doc.ts`) + a picker UI under the pool's row in `ResourcesPanel.tsx` (`MartialFlexibilityPicker`, restricted to feats tagged "Combat", RAW prereqs NOT validated — soft posture). **Wired the numeric effect for real** (cheap: `resolveFeatEffect`'s lookup is already keyed by feat id/slug, not by "is this in `doc.build.feats`") — a new `collect.ts` block resolves the borrowed feat id and applies its effect if `resolveFeatEffect` finds a STATIC entry (e.g. borrowing Dodge → real +1 dodge AC; verified in `packages/engine/test/martialFlexibility.test.ts`); choice-type feats (e.g. Weapon Focus's weapon pick) are display + note only — no separate "which choice for the borrowed copy" field exists, and reusing `featChoices[featId]` could silently borrow an unrelated owned copy's stored choice.

- **Verified already-shipped**: AC Bonus (BRA) and Bonus Combat Feats (BRA) both carry real vendored `changes` and already flow through the generic class-feature pipeline — no gap. **Flagged, not fixed** (out of scope, pre-existing vendored-data quirk unrelated to this pass): AC Bonus (BRA)'s formula (`clamp(floor((@class.unlevel - 1) / 4), 0, 4)`) evaluates to 0 at class level 4 and only becomes +1 starting at level 5 — one level later than the published table's "4th: AC bonus +1"; documented in `martialFlexibility.test.ts` rather than silently patched.
- **Deferred, note-tier** (situational/no engine target): Brawler's Flurry (full-attack action shape the attacks table doesn't model), Martial Training (feat-prereq counting, not a stat), Brawler's Strike (DR-bypass typing on unarmed strikes), Close Weapon Mastery (weapon-damage-die substitution), Knockout (already a working resource pool from vendored `uses`, no further gap), Maneuver Training (player-chosen combat-maneuver bonus — no existing per-maneuver-choice picker to hang it on), Awesome Blow/Improved Awesome Blow (combat maneuver, situational).

**Schema additions** (`packages/schema/src/character.ts`, append-only): `build.bloodragerBloodline`, `build.bloodragerBloodlineVariant`, `live.martialFlexibilityFeatId`.

**Files touched**: `packages/engine/src/bloodrager-bloodlines.ts` (new), `packages/engine/src/bloodrage.ts` (new), `packages/engine/src/collect.ts` (bloodrager bloodline powers + Martial Flexibility blocks, appended), `packages/engine/src/archetypes.ts` (`collectGrantedFeatures` bloodrager bloodline powers block, appended), `packages/engine/src/resources.ts` (Bloodrage `linkedBuffIds` override, appended import), `packages/engine/src/index.ts` (exports), `apps/web/src/model/casterLevel.ts` (level-gated shape), `apps/web/src/model/doc.ts` (`setBloodragerBloodline`/`setBloodragerBloodlineVariant`/`setMartialFlexibilityFeat`, appended), `apps/web/src/model/spellcasting.ts` (`bloodragerBonusSpellsKnown`, appended), `apps/web/src/model/feats.ts` + `featSlots.ts` (`bloodragerBloodline` slot kind), `apps/web/src/components/builder/BloodragerBloodlinePicker.tsx` (new), `ClassesSection.tsx` (wiring), `apps/web/src/components/tracker/ResourcesPanel.tsx` (synthetic linked-buff fallback + Martial Flexibility picker). New/updated tests: `packages/engine/test/{bloodragerBloodline,martialFlexibility}.test.ts` (new), `apps/web/test/{casterLevel,doc.bloodragerBloodline}.test.ts` (new/extended), `apps/web/test/spellcasting.test.ts` (extended), `apps/web/test/occultCasters.test.ts` (updated for medium's new gated CL).

### Antipaladin cruelties/Fiendish Boon + Ninja tricks + FEAT_POOL_EFFECTS multi-target (issue #65 wave B, 2026-07-08)

Built the deferred choice-bearing subsystems flagged by the "Antipaladin, Ninja, Samurai mechanical audit" entry above (cruelties/Fiendish Boon/aura family for antipaladin were out of scope there; ninja tricks/master tricks were explicitly deferred) plus the `FEAT_POOL_EFFECTS` multi-target gap that same entry's "Also found" note flagged (Extra Lay On Hands not reaching an antipaladin's Touch of Corruption). Same clean-room engine-table + `build.*` field + thin-picker + fixture-test pattern as the witch-hexes/oracle-revelations/alchemist-discoveries entry above.

**Antipaladin — Cruelties** (`build.antipaladinCruelties`, `packages/engine/src/antipaladin-cruelties.ts`): unlike hexes/revelations, the FULL rules text (including all four tiers' cruelty lists) is sitting right in the vendored `class-features.json` "Cruelty" description — no external site needed, a first for this family of tables. Confirmed verbatim: "At 3rd level, and every three levels thereafter, an antipaladin can select one cruelty" (3rd/6th/9th/12th/15th/18th, 6 total by 18th — `model/antipaladinCruelties.ts`'s budget), with the selectable MENU itself expanding at 3rd/6th/9th/12th (17 cruelties total across 4 tiers). No "Extra Cruelty" feat exists anywhere in the vendored slice (confirmed by full-text scan, and pinned with a sanity-check test) — unlike Extra Hex/Extra Revelation, this budget is never feat-boosted. Every cruelty inflicts a condition on Touch of Corruption's TARGET (never a bonus to the antipaladin's own sheet), so — same honesty bar as every hand-authored menu table in this project — all 17 are `displayOnly: true` with a Fortitude-DC `contextNotes` reminder (`antipaladinCrueltyDC` in `tables.ts`, `10 + 1/2 level + Cha mod`); the handful with an in-fiction prerequisite cruelty (Exhausted needs Fatigued, etc.) get a soft prereq note, never a hard block (hybrid prereq posture). `CrueltyPicker.tsx` mirrors `HexPicker`.

**Antipaladin — Fiendish Boon** (`build.antipaladinBoon: "weapon" | "servant"`, gained 5th, chosen once — verified against the vendored "Fiendish Boon" description text): checked how paladin's own Divine Bond is handled today FIRST, per the task brief — it turns out Divine Bond has **zero** hand-authored modeling of any kind: no `build.*` field to record which form was chosen, no detail line, nothing; the weapon math is 100% off-sheet. Mirrored that restraint for the weapon-math itself (no `Change`, no resource pool — the numbers are real but stay a display-only summary, `fiendishBoonLabel`/`fiendishBoonWeaponDetail` in `tables.ts`: `+1` enhancement-equivalent at 5th scaling to `+6` at 20th (`1 + floor((level-5)/3)`, capped), `1`/day at 5th scaling to `4`/day at 17th (`1 + floor((level-5)/4)`, capped) — both verified against the vendored description's exact numbers), but DID add the `build.antipaladinBoon` field Divine Bond lacks, since the task named it explicitly and recording a fixed RAW choice ("once the form is chosen, it cannot be changed") is cheap and matches every other single-fixed-choice field in this schema (`witchPatron`, `oracleMystery`, ...). Servant option: recorded but explicitly deferred to issue #68 (companion stat blocks), surfaced via the same detail line rather than a promise. `FiendishBoonPicker.tsx` mirrors `ArcaneBondPicker`'s two-chip shape (the closest existing precedent for a binary L5-ish class choice).

**Antipaladin — Aura family, audited honestly**: pulled the full vendored description for all five auras (Cowardice L3, Despair-APA L8, Vengeance L11, Sin L14, Depravity L17) plus Unholy Champion L20. Confirmed the task's prior mostly holds — Cowardice/Despair/Vengeance/Sin are all enemies-within-10-ft debuffs (save penalties, DR-bypass-for-attacks) with no number that ever lands on the antipaladin's own sheet, so they stay context-note tier (no code beyond the base classFeature grant, which was already free). **One genuine exception found and modeled**: Aura of Depravity's "an antipaladin gains DR 5/good" at L17, bumped by Unholy Champion's "His DR increases to 10/good" at L20 — a real, unconditional, self-facing static, same shape as the existing hand-authored `barbarianDamageReduction`. Added `antipaladinDamageReduction` (`tables.ts`) and wired it into `defenses.ts`'s `dr`-qualifier pipeline exactly like barbarian DR, including an `antipaladinDamageReductionReplaced` suppression check (`archetypes.ts`) after an archetype audit turned up Insinuator's "Aura of Indomitability" (L17) as a vendored 1:1 swap of Aura of Depravity — the one antipaladin archetype that actually replaces this feature.

**Ninja — Tricks** (`build.ninjaTricks`, `packages/engine/src/ninja-tricks.ts`): unlike cruelties, the vendored data has NO per-trick text at all (`class-features.json` only carries the two generic "Ninja Tricks"/"Master Tricks" stubs) — hand-authored clean-room from Archives of Nethys/d20pfsrd (Ultimate Combat's Ninja page; no Foundry source consulted). Confirmed cadence: "Starting at 2nd level, a ninja gains one ninja trick. She gains one additional ninja trick for every 2 levels attained after 2nd" (2nd/4th/.../20th, 10 total — `model/ninjaTricks.ts`'s budget, +1 per "Extra Ninja Trick" feat occurrence, which IS RAW-repeatable and already listed in `apps/web/src/model/repeatableFeats.ts`). 31 standard UC tricks + 13 UC master tricks (10th level+, chosen "in place of" a normal trick — same non-extra-budget posture witch major/grand hexes use), later-splatbook tricks (Legacy of Dragons, Martial Arts Handbook, ...) out of scope. Two tricks came close to a real static and were explicitly ruled out with reasoning, not silently skipped: Deadly Range's flat +10 ft. ranged sneak-attack range has no engine target (this project doesn't track a "sneak attack range" concept anywhere); Unarmed Combat Mastery's monk-table-at-(level−4) unarmed damage progression is a genuine numeric substitution but the same complexity-vs-scope call this project already made for oracle revelations' Maneuver Mastery/Lore Keeper/Sidestep Secret near-misses (would need new plumbing to grant a non-monk class an effective "monk level"). `NinjaTrickPicker.tsx` mirrors `HexPicker`. Rogue-talent overlap (a ninja can spend a trick pick on a rogue talent, and vice versa for a rogue's advanced talents on a master trick, confirmed both directions via d20pfsrd): represented as note-tier menu entries (`rogueTalent`/`advancedTalents` ids) rather than cross-wired — this project has **no** `build.rogueTalents` picker/budget field at all yet, a pre-existing gap this file doesn't attempt to close.

**`FEAT_POOL_EFFECTS` multi-target shape** (`packages/engine/src/feat-effects.ts`, cross-cutting per the task brief): `FeatPoolEffect.featureTag` widened from `string` to `string | readonly string[]` (a pure type-widen — every existing single-tag entry is still a valid value, zero changes needed to the other 6 entries) and `resources.ts`'s `collectFeatPoolBonuses` updated to record the feat's `maxDelta` under every listed tag rather than just one. First (and, per a full-text audit of the vendored `class-features.json`, only unambiguous) user: `extra-lay-on-hands` now targets `["layOnHands", "touchOfCorruption"]` — confirmed by the antipaladin's OWN vendored Touch of Corruption description, verbatim: "This ability is modified by any feat, spell, or effect that specifically works with the lay on hands paladin class feature. For example, the Extra Lay On Hands feat grants an antipaladin 2 additional uses of the touch of corruption class feature." Since a character only ever has AT MOST one of the two mutually-exclusive class features in practice, recording the bonus under both tags and letting `deriveResourcePools`' per-tag lookup pick the one that actually exists is safe; the doc comment flags the (unmodeled, implausible) double-count edge case of a character somehow having both.

**Schema fields added** (`packages/schema/src/character.ts`, appended after `alchemistDiscoveries`, no restructuring): `build.antipaladinCruelties?: string[]`, `build.antipaladinBoon?: "weapon" | "servant"`, `build.ninjaTricks?: string[]`. `GrantedFeature.origin.kind` (both `archetypes.ts` and `character.ts`'s `DerivedClassFeature.origin`) extended with `"cruelty"` and `"trick"`.

**Files touched**: `packages/schema/src/character.ts` (3 new `build.*` fields + `origin.kind` union), `packages/engine/src/antipaladin-cruelties.ts` (new), `packages/engine/src/ninja-tricks.ts` (new), `packages/engine/src/tables.ts` (`antipaladinCrueltyDC`, `fiendishBoonWeaponDetail`, `fiendishBoonLabel`, `antipaladinDamageReduction`, appended), `packages/engine/src/defenses.ts` (antipaladin DR block, mirrors barbarian's), `packages/engine/src/archetypes.ts` (cruelty/trick grant blocks + Fiendish Boon detail branch + `antipaladinDamageReductionReplaced` + `origin.kind`), `packages/engine/src/feat-effects.ts` (`FeatPoolEffect.featureTag` multi-target widen), `packages/engine/src/resources.ts` (`collectFeatPoolBonuses` multi-tag), `packages/engine/src/index.ts` (exports), `apps/web/src/model/doc.ts` (`setAntipaladinBoon`), `apps/web/src/model/antipaladinCruelties.ts` (new), `apps/web/src/model/ninjaTricks.ts` (new), `apps/web/src/components/builder/{CrueltyPicker,FiendishBoonPicker,NinjaTrickPicker}.tsx` (new), `ClassesSection.tsx` (wiring). New tests: `packages/engine/test/antipaladin.test.ts` (cruelty wiring, Fiendish Boon detail, DR at L17/L20) + `ninja.test.ts` (trick wiring) additions, `packages/engine/test/tracker.test.ts` (multi-target Extra Lay On Hands, both directions), `apps/web/test/{antipaladinCruelties,ninjaTricks,doc.antipaladinBoon}.test.ts` (new).

### Temporary HP infrastructure (issue #67) + Barbarian rage powers (issue #65, 2026-07-08)

Two related pieces: a cross-cutting temp-HP engine concept, and the barbarian rage-power picker previously deferred by the Judgments/Raging Song wave above ("the barbarian's own Rage Powers have no picker in this app yet either").

**Temporary HP** (`live.hp.temp` was previously a manual-only number; nothing could grant it):

- New `tempHp` Change target, collected generically by `collect.ts`'s existing pipeline (buffs/features simply target `"tempHp"` like any other stat) and aggregated by a new `computeGrantedTempHp` in `compute.ts` into `DerivedSheet.hp.grantedTemp: { total, components }` (schema, append-only field on `HitPoints`).
- **RAW verified** (Paizo FAQ, quoting Core Rulebook p. 208 "Combining Magical Effects", via paizo.com/threads/rzs2nses): temporary HP from the SAME source does **not** stack (the higher application wins); temporary HP from **different** sources **does** stack (sum) — the FAQ's own example is an aid spell + energy drain + vampiric touch all adding together, explicitly to prevent an exploit where repeated uses of the same effect (e.g. energy drain) pile up. This is the OPPOSITE of the "take the highest, full stop" guess in the task brief's own "VERIFY" note — the brief was wrong on this point; different-source stacking is real RAW. `computeGrantedTempHp` implements this by grouping collected `tempHp` modifiers by `source` (display name — two instances of the identical buff share the same `source` string even with different `sourceId`/instanceId, which is exactly "same source" in the FAQ's sense), taking the highest value within each group, then summing across groups. Fixture-tested directly (`packages/engine/test/tempHp.test.ts`): two "Aid" instances → highest only; "Aid" + "Vampiric Touch" → sum.
- **Unchained Rage's temp HP** — the vendored "Rage (Unchained)" buff's own `changes[]` never got a `tempHp` Change despite its description text ("You also gain 2 temporary hit points per Hit Die...") and the separate Greater Rage (UC)/Mighty Rage (UC) class features (raising the amount to 3/HD at 11th, 4/HD at 20th) agreeing this is real Unchained RAW — already logged as a known vendored gap in this file's "Vendored-data bugs found" #2 (2026-07-07 barbarian-unchained audit) and left unmodeled there pending "a cross-cutting HP-tracking feature." Built now: new `packages/engine/src/buff-effects.ts` — a hand-authored `BUFF_CHANGE_PATCHES` table keyed by buff NAME (not id, which is a content hash; `ActiveBuff.name` is a stable snapshot regardless of activation path), applied generically in `collect.ts`'s active-buffs loop alongside each buff's own vendored `changes[]`. The patch formula mirrors the buff's own vendored tier-scaling formula exactly (`2 + max(0, floor((@classes.barbarianUnchained.level - 2) / 9))`, evaluating to 2/3/4 at the same breakpoints its attack/damage/Will changes already use) multiplied by `@attributes.hd.total` (TOTAL Hit Dice — `rolldata.ts`'s `@attributes.hd.total = totalLevel(doc)` — matching "per Hit Die" literally, not just barbarian levels, for a multiclass barbarian). Fixture: L5 Con14 barbarianUnchained raging → 10 temp HP (2 x 5 HD); L11 → 33 (3 x 11); L20 → 80 (4 x 20); chained "Rage" (not Unchained) → 0, confirming the patch is keyed strictly by name.
- **Tracker semantics**: `apps/web/src/model/hp.ts`'s `applyDamage` ALREADY consumed temp HP before real HP (pre-existing, verified not added) — no change needed there. New `applyGrantedTempHp(before, after, refData)`: computes `DerivedSheet.hp.grantedTemp.total` for both doc states and (a) raises `live.hp.temp` to the granted total if it's now higher (activation/level-scaling-up ceiling), (b) clears `live.hp.temp` to 0 if the granted total dropped to exactly 0 and was nonzero before (a genuine "last tempHp-granting buff just turned off" transition) — which happens to match Unchained Rage's own RAW wording verbatim ("these temporary hit points... disappear when rage ends"), not just a convenient simplification. Wired into `ResourcesPanel.tsx`'s `LinkedBuffToggle`/`TableBuffToggle` (both now thread `refData` through to `applyGrantedTempHp` after the toggle), and a small `InfoTip` in `HpPanel.tsx` surfaces the current granted-total ceiling next to the manual Temp HP field. **Documented punts** (this tracker keeps ONE unified `live.hp.temp` pool, not a per-source ledger): a pool mixing buff-granted and unrelated manually-entered temp HP (e.g. a potion) is treated as one blob — full deactivation clears the manual portion too; partial deactivation (one of several simultaneous tempHp sources drops while another remains) doesn't shrink the pool, it's left at its stale-high ceiling; a granted total that scales down while still active never lowers an already-higher pool; Unchained Rage's own "not replenished within 1 minute of a prior rage ending" clause isn't modeled (no cooldown timer exists in this tracker).

**Rage Powers** (`build.ragePowers`, shared by `barbarian` and `barbarianUnchained`):

- New `packages/engine/src/rage-powers.ts` — 30 hand-authored entries (23 Core Rulebook + 7 curated Advanced Player's Guide: Superstition, Witch Hunter, Good For What Ails You, Internal Fortitude, Sixth Sense, Spell Sunder, Swift Foot), verified against d20pfsrd.com/aonprd.com (Powerful Blow, Superstition, Guarded Stance, Renewed Vigor fetched and quoted directly; the rest from the well-established SRD text). Rage powers are NOT part of the vendored Foundry pack (only a generic "Rage Powers" stub `ClassFeature`, no per-power breakdown), same posture as `witch-hexes.ts`/`alchemist-discoveries.ts`.
- **Cadence verified** (aonprd.com class tables): a barbarian gains a rage power at 2nd level and every two levels thereafter (2nd, 4th, ..., 20th — 10 total by 20th) for BOTH chained and Unchained — Pathfinder Unchained's own "Rage Powers" class feature restates rather than replaces the existing catalog and cadence. `RagePowerDef.editions` exists as a per-edition availability flag (per the task brief), but every one of the 30 entries defaults to `["barbarian", "barbarianUnchained"]` — no chained-only/unchained-only split was found worth hand-verifying at this table's scope; the field is there for a future entry to narrow.
- **Modeling posture — every entry is `displayOnly: true` with `changes: []`** (0 numeric / 30 note-tier / 0 deferred-without-a-table-entry): every rage power is either a per-rage/per-round ACTIVATED ability (Powerful Blow, Renewed Vigor, Strength Surge, Surprise Accuracy, Guarded Stance, Rolling Dodge) or a bonus that only applies WHILE RAGING specifically (Superstition's save bonus vs. spells, Raging Climber/Leaper/Swimmer's skill bonuses, Swift Foot's speed bonus). Both shapes need a "gate this build choice's Change by whether a specific buff is currently active" mechanism the engine doesn't have — same "near miss, not worth new infra for one table" call `oracle-revelations.ts`/`witch-hexes.ts` already made for their own conditional near-misses (see those files' doc comments). `contextNotes` on each entry carries the exact numbers/scaling/activation cost instead — same honesty bar.
- `apps/web/src/model/ragePowers.ts` mirrors `model/witchHexes.ts`'s budget-math shape exactly: `expectedRagePowerCount` = base progression (`floor((level-2)/2)+1`) + one per "Extra Rage Power" feat (a real, confirmed-existing repeatable APG general feat — "You can gain Extra Rage Power multiple times"), counted by occurrence. `barbarianLevel` SUMS chained + Unchained levels (mirroring `@pf1/engine` `defenses.ts`'s own `barbarianLevel()` for the DR progression) — a character would only ever have one of the two, but summing is correct regardless. Free-choice, soft warning only on overspend, `minLevel` gates soft-warn only — same hybrid posture as every other picker in this app.
- New `RagePowerPicker.tsx` (mirrors `DiscoveryPicker.tsx`), wired into `ClassesSection.tsx` for either `barbarian` or `barbarianUnchained`. Picked powers surface in the sheet's Class Features list via a new `collectGrantedFeatures` block in `archetypes.ts` and a new `origin.kind: "ragePower"` (append-only addition to the existing kind union, in both `packages/schema/src/character.ts`'s `DerivedClassFeature.origin` and `packages/engine/src/archetypes.ts`'s local `GrantedFeature.origin`).

**Files touched**: `packages/schema/src/character.ts` (`HitPoints.grantedTemp`, `build.ragePowers`, `DerivedClassFeature.origin.kind` +`"ragePower"` — all append-only), `packages/engine/src/buff-effects.ts` (new), `packages/engine/src/rage-powers.ts` (new), `packages/engine/src/collect.ts` (buff-patch loop + rage-power loop, both additive), `packages/engine/src/compute.ts` (`computeGrantedTempHp`, wired into `computeHp`'s return), `packages/engine/src/archetypes.ts` (`ragePower` origin.kind + `collectGrantedFeatures` block), `packages/engine/src/index.ts` (exports), `apps/web/src/model/hp.ts` (`applyGrantedTempHp`), `apps/web/src/model/ragePowers.ts` (new), `apps/web/src/components/builder/RagePowerPicker.tsx` (new), `apps/web/src/components/builder/ClassesSection.tsx` (wiring), `apps/web/src/components/tracker/ResourcesPanel.tsx` (`applyGrantedTempHp` wiring into both toggle components), `apps/web/src/components/tracker/HpPanel.tsx` (granted-total InfoTip). New tests: `packages/engine/test/tempHp.test.ts`, `packages/engine/test/ragePowers.test.ts`, `applyGrantedTempHp` coverage appended to `apps/web/test/hp.test.ts`, `apps/web/test/ragePowers.test.ts` (new).

---

## Verification posture (all stages)

`bun run typecheck` is the gate that must stay green. Engine tests are hand-computed fixtures per the convention (`packages/engine/test/`). Each stage adds at least: (a) a model-layer test for the new transition/logic, (b) a regression test asserting a doc without the new field produces byte-identical engine output, and (c) for engine-touching stages, a stacking/compute fixture verifying the new bonuses route through the change-application path correctly. Pipeline changes (Stage 1) regen data via `bun run data:build` and review the diff before committing.
