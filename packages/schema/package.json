{
  "name": "@pf1/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Shared TypeScript types for PF1 reference data, character documents, and derived sheets.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
