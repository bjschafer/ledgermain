{
  "name": "@pf1/engine",
  "version": "0.0.0",
  "private": true,
  "description": "Pure, framework-agnostic PF1 rules engine: formula DSL evaluator, typed bonus-stacking, and compute(doc, refData) -> DerivedSheet.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@pf1/schema": "workspace:*"
  },
  "devDependencies": {
    "@pf1/data-pipeline": "workspace:*",
    "@types/bun": "^1.1.14"
  }
}
