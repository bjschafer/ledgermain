- Doesn't seem to be a way to pick human racial ability bonuses.
- can't seem to set max hp, seems to just default to level * hit die max
- no 4th level ability score advancement
- the input for ability scores is whack. i tried to backspace out 10 and type in 16, and it wouldn't backspace the one, so 16 then turned into the max val of 50
- visual assymetry between right-hand pane, some are taller than others
- more consistent and clear representation of class skills. right pane has diamond, skills list has `class`
- no indication of untrained skills
- skills that *aren't* untrained can still be rolled, and should be calculated based on ability scores
- filter feats based on whether or not they're legal, and based on class (general vs combat vs metamagic vs...)
- hero points support
- spells has a lot of room for improvement. 
    - how many spells can you learn at a given level? wizard should at least have default guidance (e.g. $int_mod + n per level up)
    - spell prep vs known is unclear
