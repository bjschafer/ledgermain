{
  "name": "ledgermain",
  "version": "0.0.0",
  "private": true,
  "description": "Pathfinder 1e in-play character tracker and builder (monorepo root).",
  "license": "AGPL-3.0-or-later",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "bun run --filter @pf1/web dev",
    "dev:ref": "bun run --filter @pf1/reference dev",
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "fmt": "oxfmt .",
    "fmt:check": "oxfmt . --check",
    "e2e": "bun run --filter @pf1/web e2e",
    "e2e:install": "bun run --filter @pf1/web e2e:install",
    "e2e:webkit": "bun run --filter @pf1/web e2e:webkit",
    "screenshots": "bun run --filter @pf1/web screenshots",
    "data:fetch": "bun run --filter @pf1/data-pipeline fetch",
    "data:build": "bun run --filter @pf1/data-pipeline generate",
    "data:bump": "bun scripts/refdata-bump.ts",
    "coverage:issue": "bun scripts/coverage-issue.ts",
    "coverage:mech": "bun scripts/mech-coverage.ts",
    "feedback:pull": "bun scripts/feedback-pull.ts",
    "deploy": "bun run build && bun run --filter @pf1/web deploy"
  },
  "devDependencies": {
    "oxfmt": "^0.57.0",
    "oxlint": "^1.72.0",
    "typescript": "^5.6.3"
  }
}
